---
alwaysApply: true
---

## 🚫 ABSOLUTE PROHIBITIONS - VIOLATION FORBIDDEN 🚫

### EXECUTION COMMANDS - STRICTLY FORBIDDEN
**UNDER NO CIRCUMSTANCES execute these commands:**
- **`npm run dev`** → NEVER run this command. Development server management is handled exclusively by the user.
- **`npm run build`** → NEVER run this command. Build processes are managed exclusively by the user. Or from user direct instruction.

**CONSEQUENCES:** Executing these commands will cause immediate environment conflicts and break the user's development workflow.

### VERSION CONTROL OPERATIONS - STRICTLY FORBIDDEN
**UNDER NO CIRCUMSTANCES perform these git operations:**
- **`git commit`** → NEVER execute commit operations. Code commits are handled exclusively by the user.
- **`git push`** → NEVER execute push operations. Repository synchronization is handled exclusively by the user.
- **`git merge`** → NEVER execute merge operations. Branch integration is handled exclusively by the user.

**CONSEQUENCES:** Performing these operations will corrupt the user's git history and cause repository conflicts.

### NON-NEGOTIABLE POLICY
These restrictions are **ABSOLUTE** and **NON-NEGOTIABLE**. The user maintains complete control over:
- Development server lifecycle
- Build processes and deployment
- Git operations and version control
- Repository state management

**VIOLATION PROTOCOL:** If any of these commands are accidentally executed, immediately notify the user and await instructions for remediation.
