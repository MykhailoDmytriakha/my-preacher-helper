# BUGS.md — трекер багов

> **Что здесь лежит:** только то, что **сломано сейчас**. Сломано/неверно → сюда, с якорем `file:line` + severity (P0/P1/P2/P3). «Хорошо бы добавить» → `FEATURE_CONCEPTS.md`.
>
> **Починил → УДАЛИ запись целиком.** История починенного живёт в git — `git log -p BUGS.md` и сообщения коммитов; дублировать её здесь нельзя, иначе секция перестаёт отвечать на вопрос «что сломано».
>
> **Форма записи — коротко.** Заголовок с ID и симптомом · строка якорей · две-три строки «ожидалось / получилось / чем сейчас является». Держись примерно в 300 знаках прозы на запись. **Карта класса — таблицей, в лимит не входит:** она чек-лист закрытия, и пока в ней есть непочиненные строки, запись не удаляется.
>
> **Чего здесь НЕ должно быть:** гейты с числами тестов · regression proof · живые замеры «до/после» · отвергнутые варианты · журналы ревью · стадия «починено локально». Всё это принадлежит `.sessions/` и сообщениям коммитов. Трекер отвечает на «что сломано», а не «как чинили».
>
> **Спорный статус — в «Требует перепроверки», а не в «открыто».** Если одна запись говорит «открыт», а работа рядом говорит «закрыт», это не состояние бага, а неизвестность: её проверяют кодом, а потом переносят.

---

## 🔴 P1 — открыто

### BUG-20260912-sermon-church-not-saved · Церковь, введённая в редакторе проповеди, не сохраняется
**P1** — проповедник указывает общину, которой готовит проповедь, поле молча исчезает, и календарь потом объявляет «церковь не указана» · `frontend/app/services/sermons.client.ts:227` · `frontend/app/services/sermons.client.ts:253` · `frontend/app/utils/mutationDefaults.ts:634` · `frontend/app/components/EditSermonModal.tsx:225`
Ожидалось: церковь, названная в «Редактировать проповедь», сохраняется на проповеди, а планируемая дата, созданная в той же форме, принадлежит этой церкви. Получилось: белый список записи ядра проповеди (`SermonCoreUpdate`) не содержит `church`, и `updateSermonViaClient` не пишет это поле — patch с церковью уходит в запись без неё. Оптимистичный кэш церковь рисует, потому она мелькает и пропадает после обновления. Отдельно: обе дороги редактирования создают новую планируемую дату с жёсткой заглушкой `buildUnspecifiedChurch(...)`, игнорируя только что введённую церковь, — это и есть «церковь не указана» в календаре.
**Найден.** 2026-09-12 · пользователь (прод, `4c6360a`, `/sermons/:id`, SW активен) — воспроизведение агентом на dev-стенде.
Как должно работать для человека: *зачем* — проповедник часто знает общину задолго до даты, и это отдельный факт от «где я её прочитал». *Обязана срабатывать* — названная в редакторе церковь остаётся на проповеди после перезагрузки; если в той же форме задана и дата, эта планируемая дата в календаре показывает названную церковь. *Обязана молчать* — правка одного заголовка не переписывает церковь; уже названная в календаре церковь конкретной даты не затирается. *Что видит* — чип общины в шапке проповеди и её название в карточке календаря вместо «церковь не указана».

**Класс (механизмом).** Поле проповеди, добавленное в модель и в форму, но не внесённое в белый список того единственного писателя, что решает, каким ключам разрешено доехать до документа; и рядом — «церковь новой планируемой даты берётся из константы, а не из формы, которая её только что спросила».
| место (`file:line`) | вердикт | почему |
|---|---|---|
| `frontend/app/services/sermons.client.ts:227` (`SermonCoreUpdate`) | поражено | тип не перечисляет `church`, ключ не доезжает |
| `frontend/app/services/sermons.client.ts:253-275` (`updateSermonViaClient`) | поражено | нет ветки записи `church`; обе дороги правки идут сюда, офлайн-очередь тоже |
| `frontend/app/utils/mutationDefaults.ts:634` (dashboard-правка) | поражено | новая планируемая дата создаётся с заглушкой вместо введённой церкви |
| `frontend/app/components/EditSermonModal.tsx:225` (fallback-правка, `/sermons/:id`) | поражено | то же самое на второй дороге |
| `frontend/app/utils/mutationDefaults.ts:630` (существующая планируемая дата) | поражено | `updatePreachDate` шлёт только дату и статус: у даты, где церковь не названа, она такой и остаётся |
| `frontend/app/api/sermons/route.ts:180` (создание на сервере) | не поражено | белый список сервера поле содержит и пишет |
| `frontend/app/api/sermons/[id]/route.ts` (правка на сервере) | не поражено | серверной правки ядра проповеди не существует — только GET и DELETE |
| `frontend/app/utils/mutationDefaults.ts:558,578` (создание проповеди) | не поражено | использует `input.church ?? buildUnspecifiedChurch(...)` |
| `frontend/app/utils/calendarEntries.ts:53` (чтение календарём) | не поражено | читает `preachDate.church` по замыслу; лечится в писателе, а не здесь |

**Класс-2 (механизмом), найден владельцем 2026-09-12 при приёмке.** Окно даты строится только из самой даты и никогда из проповеди, которой она принадлежит: названная у проповеди община для него не существует, и человека просят ввести её заново.
| место (`file:line`) | вердикт | почему |
|---|---|---|
| `frontend/app/components/calendar/PreachDateModal.tsx:44` (начальная церковь) | поражено — источник | берёт церковь только из `initialData` |
| `frontend/app/components/dashboard/OptionMenu.tsx:232` («отметить как проповеданную») | поражено | не передаёт общину проповеди; плюс сам гасил заглушку — вторая копия правила «не названа» |
| `frontend/app/(pages)/(private)/calendar/page.tsx:318` (дата из календаря) | поражено | синтетическая дата рождается с пустой церковью при доступном `selectedSermon.church` |
| `frontend/app/components/calendar/PreachDateList.tsx:189` | не поражено | компонент не отрисовывается ни одним экраном (мёртвый код) |
| `frontend/app/components/sermon/SermonFormDialog.tsx` | не поражено | там община вводится, начального значения ниоткуда не берёт |

**Корень.** Белый список `SermonCoreUpdate` и тело `updateSermonViaClient` не знают про `church`.
**Фикс (направление).** Поле внесено в белый список и в сборку записи (`collectSermonCoreWrite`); правило церкви для планируемой даты — «заполняй несказанное, не переписывай сказанное» — живёт одним местом в `utils/church.ts`. Отвергнуто: выводить церковь проповеди из `preachDates[].church` (это разные факты, одна и та же проповедь ездит по разным общинам) и писать церковь безусловно при каждой правке (тогда правка заголовка отказывала бы чужой правке церкви).
**Стадия.** на проде, ждёт верификации — `db9334da`, Vercel Production build success 2026-09-12. Проверять: чип общины после перезагрузки · сохранение церкви без даты · подпись карточки в календаре · «отметить как проповеданную» у проповеди без дат.

### BUG-20260908-cached-response-counts-as-live-connection · Ответ из кэша приложения засчитывается как живая связь
**P1** — на неисправной сети приложение выглядит рабочим, а действия молча не проходят · `frontend/app/utils/apiClient.ts:88` · `frontend/app/sw.ts:18`
Ожидалось: «связь есть» означает, что запрос дошёл до сервера. Получилось: установленное приложение отдаёт GET-запросы из своего кэша, и такой ответ неотличим от настоящего — он помечает сервер достижимым, если устройство при этом заявляет сеть (портал кафе, Wi-Fi без маршрута).
Ручная проверка от этого защищена (`HEAD` с `cache: no-store`), обычные запросы — нет. Как должно работать для человека: индикатор связи не должен обещать связь, когда данные берутся с диска. Чинить: помечать ответы, пришедшие из кэша service worker, и не считать их доказательством достижимости.

### BUG-20260908-unhealthy-server-enables-ai · Нездоровый сервер включает AI-функции
**P2** — человек запускает действие, которому сказали «готово», и получает отказ · `frontend/app/providers/ConnectionProvider.tsx:38` · `frontend/app/api/health/route.ts:3`
Ожидалось: «AI готов» означает, что действие пройдёт. Получилось: доступность тяжёлых функций приравнена к наличию связи, а проверка здоровья — безусловный локальный ответ, который ничего не знает ни о провайдере AI, ни о расшифровке, ни о правах. Ответ 503 во время деплоя тоже считается связью.
Как должно работать для человека: кнопка обещает ровно то, что произойдёт. Чинить: развести «связь есть» и «тяжёлые функции доступны», а проверку здоровья сделать содержательной.

### BUG-20260908-online-declared-without-proof · Возврат сети объявляется онлайном без подтверждённого запроса
**P2** — принятый размен, не поломка: помечено, чтобы решение не потерялось · `frontend/app/utils/connectivity.ts:62`
Возврат устройства в сеть даёт «онлайн», пока ни один запрос этого не подтвердил. Требовать подтверждение нельзя в лоб: server-first чтения выключены, пока приложение считает себя офлайн, значит подтверждению неоткуда взяться и приложение замерзает. Ревьюер предлагает третье состояние, которое разрешает одну узкую проверку, не разрешая обычную работу.
Как должно работать для человека: на портале кафе иконка не должна гаснуть до первого реально дошедшего запроса, но и приложение не должно застревать в офлайне при живой связи. Чинить: отделить «чем гейтим работу» от «что показываем как доказанное».

### BUG-20260908-firestore-failures-invisible-to-connectivity · Отказ основного транспорта данных не виден индикатору связи
**P1** — на портале кафе иконка молчит, хотя данные не идут · `frontend/app/hooks/useDocumentFreshness.ts:328` · `frontend/app/utils/connectivity.ts`
Ожидалось: если данные не ходят, человек видит это в одном месте. Получилось: Firestore — отдельный транспорт; его отказы и переход на кэш меняют только плашку свежести и не доходят до модуля связи, а часть запросов идёт мимо `apiClient` через прямой `fetch`.
Как должно работать для человека: индикатор связи отражает, идут ли данные вообще, а не только запросы одного клиента. Чинить: сообщать в модуль связи об отказах Firestore-слушателя и о прямых `fetch`-путях.

### BUG-20260908-captive-wifi-reads-as-online · Подключённый Wi-Fi без выхода в интернет приложение считает рабочим
**P1** — человек видит доступные AI-кнопки над связью, которая никуда не ведёт · `frontend/app/utils/apiClient.ts:46` · `frontend/app/providers/ConnectionProvider.tsx:56`
Ожидалось: если сервер недостижим, приложение говорит об этом иконкой связи. Получилось: `navigator.onLine` у портала кафе или гостиницы равен `true`, стартовой проверки при монтировании нет, и до первого неудачного запроса приложение считает себя онлайн.
Как должно работать для человека: иконка перечёркнутого Wi-Fi обязана появляться, когда сервер недостижим по любой причине, и молчать, когда связь есть. Чинить: одна проверка `/api/health` при первом монтировании провайдера, если устройство заявляет сеть, а доступность ещё ничем не подтверждена; следить, чтобы медленная сеть не давала ложного «офлайн».

### BUG-20260908-apiclient-marks-online-on-any-http-status · Ответ 5xx засчитывается как «связь есть»
**P2** — расхождение между тем, что показывает интерфейс, и тем, что реально доступно · `frontend/app/utils/apiClient.ts:113`
Ожидалось: «связь есть» означает, что приложением можно пользоваться. Получилось: любой HTTP-ответ, включая 500 и 503 во время деплоя, помечает связь рабочей, потому что для транспорта это доказательство доставки.
Ручная проверка связи уже сужена (`probeConnectivity` считает не-`ok` недоступностью), общий путь — нет. Как должно работать для человека: во время деплоя интерфейс не должен обещать работающие AI-функции. Чинить: развести «сеть доставила ответ» и «сервером можно пользоваться» — второе не должно выводиться из первого.

### BUG-20260815-migration-left-the-counter-behind · Переезд текста плана поменял содержимое, не двинув версию
**P1** — законная запись в план может отклоняться бесконечно · `frontend/scripts/migrate-plan-text.js:390` · `frontend/app/utils/readFreshness.ts:41`
Ожидалось: переезд формата хранения человек не замечает. Получилось: миграция записала `planText`, не тронув `rev` и `updatedAt` — старый кэш не проигрывает по свежести, ориентир записи пуст, защита отказывает в сохранении навсегда.
Чинить: один раз «тронуть» переехавшие документы (`rev.plan = increment(1)` + свежий `updatedAt`) и дописать бамп в сам скрипт. Не сделано: это запись во все проповеди боевой базы — нужен человек.

### BUG-20260815-offline-queued-text-invisible-in-other-tabs · Офлайн-правка видна только во вкладке, где её сделали
**P1** — молчаливая потеря своего текста · `frontend/app/services/sermons.client.ts` → `pendingPlanTextNodeIds` · `frontend/app/services/outboxReplay.client.ts:189`
Ожидалось: написанное офлайн видно в любой вкладке этой проповеди. Получилось: очередь отдаёт только ID узлов, но не сам текст, поэтому соседняя вкладка показывает старое и человек пишет поверх собственной правки; доигрывание затем снимает проверку по этому же полю.
Одновкладочный случай закрыт (`usePendingPlanCells.ts`), осталась межвкладочная половина. Чинить: отдавать из очереди текст, а не только ID, плюс слушать `storage`-событие.

### BUG-20260815-durable-draft-fails-silently-when-storage-refuses · «Долговечный» черновик молча перестаёт быть долговечным
**P1** — предусловие защиты нарушено, и никто об этом не узнаёт · `frontend/app/utils/durableDraft.ts:55,79`
Ожидалось: негде сохранить черновик — человек узнаёт об этом до того, как потеряет текст. Получилось: в privacy-режиме или при исчерпанной квоте `saveDraft` возвращает `void` и тихо сдаётся, а экран и защита продолжают считать, что копия есть.
Чинить: `saveDraft` возвращает успех/неуспех, `useDurableDraft` поднимает наверх, экран предупреждает один раз за сессию.

### BUG-20260815-draft-recovery-offers-a-number-not-the-text · Возврат черновика предлагается вслепую
**P1** — можно вернуть вчерашнее поверх более свежего серверного текста · `frontend/app/(pages)/(private)/sermons/[id]/plan/PlanDraftRecoveryBar.tsx`
Ожидалось: видно, КАКИЕ карточки и КАКОЙ текст возвращается и чем он отличается от серверного. Получилось: плашка показывает только количество карточек; человек жмёт «Вернуть», старое становится несохранённой правкой поверх нового, а следующий уход со страницы её автосохраняет.
Уже сделано: предлагаются только узлы, которые ещё есть в структуре.

### BUG-20260815-refresh-hides-dirty-text-of-a-removed-node · «Загрузить свежую» прячет несохранённый текст вместе с удалённым пунктом
**P1** — текст остаётся в памяти, но добраться до него нельзя · `frontend/app/(pages)/(private)/sermons/[id]/plan/manual/page.tsx` · `useManualConspectus.ts`
Ожидалось: узел, который правят прямо сейчас, исчез в другом месте — текст показан и предложен к сохранению. Получилось: карточка рисуется строго из `sermon.outline`, поэтому она пропадает вместе с узлом, а несохранённый текст висит в состоянии без единого способа его увидеть и гибнет при размонтировании.
Живьём не воспроизводился — прослежено по коду.

### BUG-20260813-outbox-terminal-refusal-never-resolves · Офлайн-правка, отклонённая по правам, висит «в очереди» вечно
**P1** — правка молча не сохраняется никогда, текста при этом нигде не видно · `frontend/app/services/outboxReplay.client.ts:184-204` · `seriesMembership.client.ts:230`
Ожидалось: окончательный отказ переводит запись в терминальное состояние и приводит к решению — показать текст, дать скопировать, дать удалить намерение. Получилось: терминальными стали только устаревшая версия и «документ не найден»; `permission-denied`, `unauthenticated` и `invalid-argument` остаются `pending` и переигрываются каждую минуту.
Половина класса закрыта, осталась половина. Один человек, одно устройство, работа офлайн — обычный сценарий этого продукта.

---

## 🟠 P2 — открыто

### BUG-20260911-service-orders-drag-pickup-drop · Порядки служений плохо захватываются и скачут при отпускании
**P2** — мешает расставить служения · найдено владельцем 2026-09-11 · `frontend/app/(pages)/(private)/care/orders/page.tsx:519`.
Включить перестановку и потянуть за ручку: захват срабатывает ненадёжно, завершение идёт скачком. Ожидалось: ручка уверенно захватывает карточку, отпускание плавно ставит её на выбранное место; обычный тап, прокрутка и отмена порядок не меняют. Причина проверяется в браузере.

| Место | Вердикт | Почему |
|---|---|---|
| `care/orders/page.tsx` — ручка и сортируемая строка | Поражено | Touch-жест перехватывает прокрутка; позиция обновляется после начала завершения |
| `SeriesItemCard.tsx`, `FlowItemRow.tsx` | Не поражено | `touch-none` и синхронная перестановка до сохранения уже есть |

### BUG-20260911-service-orders-write-lock · Одна запись служения блокирует следующие действия на iPad
**P2** — нельзя продолжить перестановку или удаление пунктов · найдено владельцем 2026-09-11 · `frontend/app/hooks/useServiceOrders.ts` · `frontend/app/services/serviceOrders.service.ts`.
Ожидалось: после сохранения стрелки и удаление снова доступны, при отказе есть явный ответ. Получилось: молчащий Firestore SDK удерживает ожидание записи и замок всех следующих действий. Действия должны завершаться через доступный канал, без повторной отправки неопределённо завершённой записи.

| Место | Вердикт | Почему |
|---|---|---|
| Перестановка служений, изменение шагов и названия | Поражено | Ожидание SDK удерживает общий замок или очередь |
| Создание и удаление служения | Поражено | Тот же единственный канал записи |
| Сохранение через сервер приложения | Требуется | Проверка владельца, атомарность и версии обязательны также без SDK |

### BUG-20260911-service-orders-missing-freshness · Изменения служения на другом устройстве не показывают стандартную плашку
**P2** — человек продолжает работать со старой версией · найдено владельцем 2026-09-11 · `frontend/app/(pages)/(private)/care/orders/[id]/page.tsx`.
Ожидалось: чужое изменение вызывает стандартную плашку; свежая версия принимается явно, собственные сохранения не тревожат. Получилось: редактор не подключён к проверке актуальности. При недоступном SDK проверка должна работать через сервер приложения, сохраняя новые локальные правки.

| Место | Вердикт | Почему |
|---|---|---|
| Редактор служения | Поражено | Нет `useDocumentFreshness` и подтверждённого снимка редактора |
| `useDocumentFreshness` — независимый канал для serviceOrders | Требуется | Тот же молчащий SDK не докажет актуальность |
| Молитвы и проповеди | Не поражено отсутствием плашки | Используют стандартный механизм |

### BUG-20260911-list-reads-single-channel · Списки читаются одним каналом и без срока — на молчащем транспорте висят вечно

**Severity:** P2 — раздел выглядит пустым или вечно загружается; текст не теряется.
**Якоря:** `frontend/app/services/sermons.client.ts:115` (список проповедей) · `frontend/app/services/prayerRequests.client.ts:54` · `frontend/app/services/planTemplates.client.ts:33`.
**Ожидалось / получилось:** список приходит или честно отказывает / `getDocs` не отвечает ни данными, ни ошибкой, и ожидание ничем не ограничено.
**Воспроизведение:** устройство, где транспорт Firestore молчит (замер с iPad владельца 06.09: ноль серверных ответов у SDK, 205 мс у HTTP), плюс коллекция, которой нет в локальной копии этого устройства.
**Доказательства:** `BUG-20260906-sermon-read-hangs` (починен `4f098b62`) — тот же механизм на одиночном чтении проповеди; известное поведение SDK: [firebase-js-sdk#6515](https://github.com/firebase/firebase-js-sdk/issues/6515).
**Эффект:** на таком устройстве список не появляется; спасает только локальная копия с прошлых заходов, поэтому новая коллекция поражена сильнее всего.
**Пользовательский контракт:** список либо приходит, либо говорит, что не прочитался, и даёт повторить. Вечное ожидание — не состояние.
**Класс:** все списки владельца — **починены** 2026-09-11 одной общей дорогой: `app/services/ownerListRead.client.ts` (срок 2,5 с, затем чтение через `GET /api/owner-list` с белым списком коллекций; первый ответивший канал выигрывает). На неё переведены проповеди, молитвы, шаблоны планов, календарь и служения. План проповеди и даты проповеди теперь читают сам документ проповеди, у которого защита уже была (`sermons.client.ts:161,175`).
**Остаётся:** одиночная молитва `prayerRequests.client.ts:75` — срок поставлен, второго пути нет (серверного чтения одного документа для молитв в приложении не существует); чтения ВНУТРИ записи — `atomicUpdate.client.ts:111`, `prayerRequests.client.ts:95,195`, `sermons.client.ts:1170` — это другой класс: виснет не экран, а запись, и лечение там иное.
**Статус:** списки закрыты; открыто на одиночную молитву и на класс чтений внутри записи.


### BUG-20260906-groups-access-waits · Группы остаются в ожидании проверки доступа
**P2** — раздел скрыт за skeleton при живом приложении · `frontend/app/(pages)/(private)/groups/page.tsx:31,52,128` · `frontend/app/services/userSettings.service.ts:39,424`
Ожидалось: сохранённая настройка доступа используется при открытии, ожидание ограничено. Получилось: отдельное чтение Firestore без deadline удерживает `accessLoading` — в отчёте 2026-09-06 результата нет 14,8 секунды; общий кэш настроек не используется. Аналогичная отдельная проверка живёт в `DashboardNav.tsx:85`.
Корень не искали.

### BUG-20260905-flaky-suites-block-build · Одни и те же тесты падают через раз под полной параллельностью и валят билд
**P2** — гейт врёт в обе стороны: деплой падает без причины, а настоящая регрессия тонет в шуме · `frontend/package.json` — `test:build` с `--maxWorkers=100%`
Ожидалось: дважды запущенная сборка одного коммита даёт один результат. Получилось: на `6ca321cc` первый прогон — 3 сьюта / 4 теста красные, второй того же коммита — 539/539 зелёные; изолированно всё проходит стабильно. Частота ~2 из 5.
Корень не исследован.

**Класс (механизмом).** Тест зависит от времени или общего состояния, а не только от входа; проявляется под конкуренцией за CPU, когда таймеры и `waitFor` не успевают в свои окна.
| место | вердикт | почему |
|---|---|---|
| `__tests__/components/AddSermonModal.test.tsx` | поражено | падал под нагрузкой, изолированно зелёный |
| `__tests__/components/settings/ModelSelector.test.tsx` | поражено | то же |
| `app/(pages)/(private)/studies/__tests__/page.test.tsx` | поражено | то же, два теста про сворачивание карточек |
| `__tests__/components/PlanTemplatesSection.test.tsx` | поражено | тот же вечер, другой коммит |
| остальные 535 сьютов | не проверено | за вечер ни разу не падали, но выборка мала |

### BUG-20260905-ai-chain-never-retries-same-model · Транзиентный отказ провайдера сразу становится отказом человеку
**P2** — секундная икота провайдера стоит человеку целого прогона · `frontend/app/api/clients/structuredOutput.ts:118-127` · `ai/providerAdapters.ts:97` · `ai/tierPolicy.ts:156`
Ожидалось: вердикт `retrySameProvider` означает «повтори ту же модель», и транзиентный 503/429/таймаут гасится внутри бюджета запроса. Получилось: `runWithFallback` умеет только сдвигать индекс на следующую цель, а у обычного тарифа список целей из одного элемента — значит ни одной повторной попытки. Имя вердикта описывает поведение, которого в коде нет.

**Класс (механизмом).** Вердикт классификатора не исполняется исполнителем: `classifyError` различает три исхода, `runWithFallback` — два.
| место | вердикт | почему |
|---|---|---|
| `app/api/clients/structuredOutput.ts:118` | поражено → корень | `retrySameProvider` и `tryNextTarget` обрабатываются одинаково |
| `app/api/clients/studyNoteCut.structured.ts:196` | не поражено | держит собственный повтор — заплатка на один маршрут |
| `app/api/sermons/[id]/audio/generate/route.ts:190` | не поражено | свой цикл попыток с backoff по чанкам |
| остальные вызовы `callWithStructuredOutput` | поражено | повтора нет ни у кого |

### BUG-20260905-scratch-keyboard-drag-lifts-offscreen · Набросок, поднятый с клавиатуры, улетает за экран и стрелками не двигается
**P2** — клавиатурный перенос наброска мёртв; обходной путь есть — меню «Положить в…» · `frontend/app/components/plan-editor/OutlineBoard.tsx` — `liftedNoteId`, `DragOverlay`, `KeyboardSensor`
Ожидалось: Space поднимает копию под ручкой, стрелки двигают её по 25 px, Space кладёт. Получилось: копия появляется с центром на y = −42 px, над окном, и ArrowDown её не двигает; Escape отменяет корректно.
Корень не исследован. Направление: замерить `collisionRect` при клавиатурной активации, вероятно нужен `sortableKeyboardCoordinates`-подобный getter и отказ от скрытия исходника для клавиатуры.

**Класс (механизмом).** Позиция копии при активации без указателя.
| место | вердикт | почему |
|---|---|---|
| `OutlineBoard.tsx` — наброски (KeyboardSensor) | поражено | замер выше |
| `OutlineBoard.tsx` — пункты и подпункты с клавиатуры | не проверено | тот же сенсор, но исходник не прячется |
| страница структуры (`useStructureDnd.ts`) | не проверено | KeyboardSensor не подключён |

### BUG-20260905-second-tab-keeps-deleted-doc-editable · Вторая вкладка продолжает предлагать редактировать удалённую проповедь
**P2** — реже, чем удаление со своей страницы, но последствие то же: человек диктует в документ, которого нет · `frontend/app/hooks/useDocumentFreshness.ts:311` · `frontend/app/hooks/useSermon.ts:116`
Ожидалось: вкладка узнаёт, что проповедь удалили в другом месте, и перестаёт предлагать её править. Получилось: редактор жив, единственный сигнал — жёлтая плашка «Свежесть неизвестна» с кнопкой «Оставить своё», то есть предложение записать локальную копию в удалённый документ. Нового чтения не происходит, поэтому отказывать нечему.
Различитель уже есть — `lastServerProofRef`: отказ ПОСЛЕ подтверждения сервера в этой сессии значит «доступ пропал», отказ до первого подтверждения — нет.

**Класс (механизмом).** Живой слушатель узнаёт об отказе, но экран этого не отражает — в отличие от пути чтения, где отказ имеет приоритет над кэшем.
| место | вердикт | почему |
|---|---|---|
| `app/hooks/useDocumentFreshness.ts:311` | поражено | error-ветка ставит `unknown`, не различая «связи нет» и «доступа больше нет» |
| все экраны, зовущие `useDocumentFreshness` | не проверено | хук общий: правка меняет поведение везде сразу и требует своего прочёса |

### BUG-20260902-ssr-renders-english-labels-then-swaps · Сервер отдаёт английские подписи, клиент русские, React ругается на hydration
**P2** — лишняя перерисовка на каждом заходе и постоянная красная ошибка в консоли, за которой перестают замечать настоящие · `frontend/app/components/ui/FoldableMarkdown.tsx:87`
Ожидалось: разметка от сервера совпадает с тем, что рисует клиент. Получилось: `aria-label` приходит с сервера по-английски, на клиенте по-русски — «A tree hydrated but some attributes… didn't match».
Область шире одной точки: механизм задевает ЛЮБУЮ строку из `t()`, отрисованную на сервере. Прежде чем чинить — прочесать класс и решать на уровне инициализации языка, а не заплаткой в компоненте.

### BUG-20260815-refresh-says-done-without-review · «Посмотреть изменения» ничего не показывает
**P2** — экран объявляет обновление завершённым, хотя видимый текст остался несохранённым и построен на старом ориентире · `frontend/app/(pages)/(private)/sermons/[id]/plan/manual/page.tsx` → `handleRefresh:338`
Ожидалось: кнопка либо показывает, что изменилось в другом месте, либо не называется «посмотреть». Получилось: при несохранённых правках она делает то же, что «Загрузить свежую» — перечитывает проповедь; правимая ячейка остаётся своей, плашка исчезает, показывается «обновлено», а сравнения человек не увидел.

### BUG-20260815-take-theirs-drops-the-only-copy · «Взять их версию» удаляет единственную копию отклонённого текста, даже если чужая не загрузилась
**P2** — потеря текста, но только по осознанному нажатию и только при неудачной загрузке · `frontend/app/components/OutboxConflictBanner.tsx:118`
Ожидалось: сначала показать чужую версию, потом предлагать расстаться со своей. Получилось: успешно завершённый `invalidateQueries()` принимается за доказательство загрузки, а библиотека проглатывает отказ и для приостановленного запроса тоже резолвится успешно — запись из очереди удаляется, чужой версии на экране так и не появилось.
Тест `__tests__/components/OutboxConflictBanner.test.tsx:247` зелёный по неверной причине: он заставляет `invalidateQueries` отклониться, а библиотека так не делает.

### BUG-20260814-legacy-persisted-error-unclassified · Отказ, сохранённый предыдущей сборкой, после обновления читается как обычный сбой
**P2** — один раунд человек видит неверную подпись и неверную кнопку · `frontend/app/utils/queryPersister.ts:91`
Ожидалось: восстановленная из IndexedDB ошибка классифицируется так же, как живая. Получилось: ошибку, записанную сборкой без маркера, `structuredClone` уже лишил собственных полей в момент записи — восстанавливать нечего, и бейдж показывает общий текст «не сохранилось» с кнопкой «Повторить».
Кодом сейчас НЕ чинится: поля потеряны при записи чужой сборкой. Закроется само — первое же «Повторить» рождает новую, уже классифицированную ошибку. Удалять, когда ветка чтения legacy-ошибок перестанет иметь смысл.

### BUG-20260813-late-refusal-silent-after-navigation · Отказ, пришедший после ухода с экрана, не докладывает никто
**P2** — человек не узнаёт, что его правка отклонена; текст физически не потерян · `frontend/app/utils/recoverableWrite.ts` → `useWriteRecovery`
Ожидалось: отказ доходит до того, кто начал запись. Получилось: докладчик живёт в хуке сущности, а хук монтируется страницей — ушёл со страницы, докладчика нет.
**Осознанно оставлен открытым.** Общий докладчик уровня приложения строили и сняли целиком: пять витков ревью находили в НЁМ по 5-6 дефектов, включая чтение чужого текста после смены аккаунта. Утечка чужого текста дороже тишины. Правильный ход — не наблюдатель поверх, а доставка: отказ едет вместе с записью в долговечный черновик с пометкой «отклонено».

### BUG-20260813-queued-mutation-accepts-before-durable-owner · «Принято очередью» может прозвучать раньше, чем очередь взяла запись
**P2** — принято как осознанная граница контракта, а не дефект · `frontend/app/utils/recoverableWrite.ts:114` · `frontend/app/providers/QueryProvider.tsx:19`
Ожидалось: слово «очередь взяла» опирается на факт владения — мутация на паузе и сохранена. Получилось: принимается по истечении такта; теряется запись при аварийном закрытии вкладки в доли секунды полёта.
Закрывать правильнее долговечным черновиком для экранов с дорогим текстом, а не сигналом старта мутации. Граница описана в `frontend/docs/recoverable-writes.md`.

### BUG-20260810-audio-chunks-whole-array · Правка текста аудио-чанка может пропасть при переключении источника
**P2** — теряется ручная правка, но текст машинный и восстанавливается перегенерацией · `frontend/app/components/audio/StepByStepWizard.tsx:406` (`syncChunksToDb`) · `app/api/sermons/[id]/audio/chunks/route.ts:54-62`
Ожидалось: правка чанка переживает переключение режима. Получилось: `syncChunksToDb` отправляет PUT с целым массивом из клиентского кэша, не перечитывая базу; сервер кладёт его как есть, и правка между сборкой кэша и отправкой стирается. Ни транзакции, ни `rev`, ни базы-опоры ни в одном из трёх писателей.
Воспроизведение агентом НЕ проводилось — вывод из разбора кода.

### BUG-20260810-auth-mirrors-in-localstorage · Два самодельных зеркала сессии в localStorage: вечный спиннер и протухшая личность
**P2** — не теряет данные, но подвешивает вход и уже порождал P1-класс · `frontend/app/components/ProtectedRoute.tsx:30-69` · `frontend/app/hooks/useAuth.ts:14-44` · `PublicRoute.tsx` · `useResolvedUid.ts` · `queryKeys.ts`
Ожидалось: состояние входа определяется одним источником — ответом Firebase Auth. Получилось: источников три (сам Auth, ключ `firebase:authUser:`, `guestUser`). Отсюда вечный спиннер, когда ключ есть, а пользователя нет, и протухшая личность, потому что `guestUser` пишется не только для гостя.
Класс уже чинили коммитом `e00d53f6` — зеркало убрали не всё, и класс вернулся. Живьём не воспроизводился. ⛔ Переезд Auth на IndexedDB сюда НЕ входит: выгоды не подтвердились, а удаление опции persistence даст in-memory и потерю сессии при перезагрузке.

### BUG-20260810-freshness-ignores-dirty-draft · Плашка свежести не знает про несохранённые правки и может снести их молча
**P2** — редкий сценарий, тяжёлый исход · `frontend/app/(pages)/(private)/sermons/[id]/page.tsx:1907` (`dirty={false}`) · `frontend/app/components/DataFreshnessBanner.tsx:13`
Ожидалось: пока на экране есть несохранённое, «Загрузить свежую версию» предупреждает об этом или недоступно. Получилось: плашка всегда вызывается с `dirty={false}`, а «текущее состояние экрана» берётся из кэша запроса, тогда как грязный черновик живёт отдельно — в модальном окне, редактируемом заголовке, автосохраняемой заметке.
Компонент умеет dirty-режим, его никто не включает. Живьём не воспроизводился.

### BUG-20260809-studies-search-lag · Поиск по заметкам тормозит на первых буквах и не понимает запрос из нескольких слов
**P2** — поиск непригоден там, где нужнее всего: когда заметок много · якоря не установлены — искать страницу изучений и её поле поиска
Ожидалось: поиск идёт незаметно, а запрос из нескольких слов находит заметку, где эти слова есть — необязательно подряд. Получилось: ввод первой буквы запускает поиск, второй снова, «начинает тормозить»; «Бог возлюбил вечную» не находит стих, где все три слова есть, но не подряд.
Воспроизведение со слов владельца, агентом не проверялось. Перед фиксом снять замер на реальном объёме заметок.

### Хвост тяжёлых AI-промптов не влезает и в 60 секунд
**P2** — вызов, убитый потолком, оставляет человека с тихим спиннером · три роута стоят на `maxDuration: 60` и вылезают за него
Замер по `ai_prompt_telemetry` (507 событий, инструмент `frontend/scripts/measure-ai-latency.mjs`): `plan_point_content` p90 38,0 с, max 106,1 с · `compose_plan_from_scratch` max 460,3 с · `thought` max 60,5 с. Эффект сильнее таблицы — ошибка выжившего: телеметрия писалась ПОСЛЕ возврата модели, поэтому убитые вызовы событий не оставляли.
Слепота измерения закрыта турникетом: запись отмечается на входе (`phase:'started'`). После выкатки цифры надо пересчитать по честному знаменателю. Потолком задачу не закрыть — нужно резать запрос на части и склеивать на клиенте, либо streaming; минимум — не глотать таймаут, а говорить «слишком большой текст, разбейте».

### Правило `advancesRevision` — заготовка, а не «кнопка»: включение по чек-листу положит приложение
**P2** — риск сейчас ноль, опасность появляется в момент, когда кто-то последует чек-листу буквально · `frontend/firestore.rules:52-56`
Ожидалось: чек-лист «ACTIVATION CHECKLIST (owner's call)» читается как «раскомментировать и выкатить». Получилось: функцию неоткуда вызвать — ни в одном `allow update` её нет, то есть мёртвый код; а подключённая «в лоб» она даст `permission-denied` на большинстве записей, потому что `request.resource.data` — полное будущее состояние документа, и правка мыслей не двигает `rev.core`.
Отдельно: `userSettings.service.ts` пишет через `setDoc(merge)` и счётчик не двигает вообще — правило, распространённое на `users`, полностью сломает сохранение настроек. Чинить через `diff(resource.data).affectedKeys()`, `users` вывести из-под правила, прогнать на эмуляторе.

### B9 · TOCTOU перебора usage-лимита при конкурентном burst
**P2** — P1 по классу, но не срочно: перерасход самоограничивается · `frontend/app/api/clients/usageLimits.server.ts:105,120`
Ожидалось: проверка лимита и его инкремент — одна неделимая операция. Получилось: `assertAiUsageAvailable` читает-проверяет ДО провайдер-вызова, `consumeUsage` инкрементит ПОСЛЕ отдельной транзакцией без ре-проверки; два запроса на 99/100 оба проходят.
Пять потребителей одного примитива: `structuredOutput.ts:280`, `audio/generate:380`, `studies/transcribe:145`, `thoughts/route:233`, `thoughts/transcribe:144`. **Рефрейм владельца:** жёсткий блок ровно на лимите НЕ нужен — нужна грация: действие доигрывается, мягкий блок новых, hard-cap на бо́льшем пороге, тёплое сообщение. Атомарность перестала быть критичной; сам механизм — фича F7 в `FEATURE_CONCEPTS.md`.

### RMW-класс · lost-update вне транзакции: онлайн-гонка закрыта, офлайн-replay и серверный путь открыты
**P2** — реальная гонка, но низкая вероятность; самый реалистичный триггер — офлайн-буфер доигрывается, пока человек правит вживую
Механизм: код читает документ, меняет ВЕСЬ массив в памяти из stale-read и перезаписывает целиком; `writeBatch` не даёт read-isolation. Семь клиентских сайтов переведены на `runTransaction` через `app/services/atomicUpdate.client.ts` — покрыто только безопасное к повтору. Три update-пути оставлены нетранзакционными сознательно: SDK повторяет callback даже после успешного коммита, и повтор наложил бы устаревший payload.
Открыто: воскрешение при повторе ADD (нужен tombstone) · офлайн-replay (durable-outbox с ID-операциями + rebase) · гонка двух быстрых сохранений (сериализация per-`{sermonId,thoughtId}`) · серверный путь: `series/[id]/route.ts:41,61-67`. Прод-путь preachDates идёт через Admin `sermons.repository.ts:274,320,363` — stale RMW, клиентские конверсии там не вызываются.

### Cross-account утечка persisted-кэша (общий компьютер)
**P2** — после logout данные предыдущего аккаунта могут отрисоваться следующему человеку на том же компьютере · `frontend/app/utils/queryPersister.ts:7` · `frontend/app/hooks/useAuth.ts:39-55`
Воспроизведено вживую: вышли из аккаунта A, вошли B, открыли прямой ссылкой проповедь A → на экране содержимое A, при этом `/api/me/entitlement` вернул 401. Источник — persisted React Query cache в IndexedDB, один глобальный ключ на все аккаунты; `handleLogout` его не чистит. Firestore Rules тут бессильны: они гейтят серверное чтение, а не локальную копию.
Ключ детали разведён по владельцу (`queryKeys.ts` → `['sermon', uid, id]`) — это второй слой, не закрытие. ⛔ НЕ «чистить кэш при смене uid» — пробовали и откатили: удаляло persisted paused-мутации, то есть несинхронизированные офлайн-правки. Правильное направление — **разделять, а не стирать**: партиционировать ключ персистера по uid, свой QueryClient на владельца, не восстанавливать persist-провайдер до резолва auth.

### Редактор заметки не пересоздаётся при смене документа: на экране может быть текст чужой заметки
**P2** — если переход достижим из UI, это P1: правка кладёт текст, производный от A, в документ B · `frontend/app/(pages)/(private)/studies/[id]/page.tsx:134,144,703`
Ожидалось: смена `noteId` пересоздаёт состояние редактора. Получилось: инициализация выполняется только пока `!isInitialized`, а флаг заводится один раз и при смене `noteId` не сбрасывается; ключа, который заставил бы React пересоздать страницу, тоже нет.
Не регрессия, пред-существует. Не проверено живьём: достижим ли переход заметка→заметка без размонтирования. Чинить `key={noteId}` либо явным сбросом — с обязательным сохранением черновика покидаемой заметки ДО сброса.

### Долговечный черновик теряет последние ~250 мс при переходе между документами
**P2** — заявленный инвариант «черновик есть ⟺ текст не подтверждён» в этом окне нарушается · `frontend/app/utils/durableDraft.ts` + `useDurableDraft.ts`
Ожидалось: уход с `/studies/a` на `/studies/b` сбрасывает черновик покидаемого документа. Получилось: хук не размонтируется, меняется только ключ; ни `pagehide`, ни `visibilitychange` не срабатывают, выполняется лишь очистка таймера, которая ОТМЕНЯЕТ отложенную запись.
⛔ Прямолинейный фикс проверен тремя витками ревью и порождает НОВЫЕ способы потерять текст: безусловная запись затирает более свежий черновик другой вкладки · compare-and-set поверх `localStorage` не атомарен · владение слотом устаревает по построению · «пустой слот = убрали намеренно» ложно при вытеснении по квоте · ABA. Менять КОНСТРУКЦИЮ: IndexedDB с транзакцией либо Web Locks, владение токеном писателя, слушать `storage`.

### Подтверждённый черновик может остаться и позже предлагаться как «несохранённый»
**P2** — давно сохранённый текст предлагается поверх более свежего с другого устройства
Ожидалось: черновик ретируется, когда запись подтверждена. Получилось: если запись прошла, а вкладка упала до `markSaved`, черновик остаётся равным серверному значению; пока текст совпадает — тишина, после более поздней правки с другого устройства он всплывает как «несохранённый».
⛔ «Ретировать черновик, равный открывающему значению» откачено: заметка может открыться из оптимистичного кэша (`useStudyNotes.ts:38`), и тогда «равен тому, что вижу» значит «оба не подтверждены» — ретирование удаляет единственную копию. Ретировать имеет право только тот, кто слышал подтверждение сервера; нужны метаданные версии в самом черновике.

### Соседи по классу «удаление с одного клика»
**P2** — один клик стирает текст без подтверждения и без undo · `frontend/app/components/PointNote.tsx:127-138` · `frontend/app/components/plan-editor/OutlineBoard.tsx:321`
Прочёсано при фиксе набросков, два выживших. Крестик на заметке-напоминании пункта плана стирает ВЕСЬ текст напоминания; в комментарии написано «undo-able upstream», но апстрим — это только save/cancel редактора плана, а при автосохранении текст уходит совсем. Удаление подпункта плана спрашивает подтверждение ТОЛЬКО когда к нему привязаны мысли (`count > 0`) — пустой подпункт исчезает с одного клика вместе со своим текстом.
Не починено заодно: оба живут в редакторе плана со своим устоявшимся ритмом правки; расширять гейт туда — заметное изменение, а не молчаливый багфикс. Решение за владельцем.

### Audio-метринг · остаток: тихая потеря чанка OpenAI
**P2** — упал чанк → батч падает → экспорт падает, файла нет · `generateChunkAudio`, `Promise.all` fail-fast, `audio/generate/route.ts:377`
Retry в аудио-пути нет вовсе; он есть только у мыслей в `thought.structured.ts`. Частота низкая (ошибка провайдера или чанк >4096), но человек остаётся без результата. Продуктовое решение — F8 в `FEATURE_CONCEPTS.md`.
Также отложено: `audioSecondsUsed` метрится, но не энфорсится (билинг отложен, интерим-кэп 1 aiCall на экспорт); Google-1750-путь за флагом `GOOGLE_SMALL_CHUNKING`, живая проверка на слух не сделана.

### Audio-export UI · Free видит одну модель, а «качество» вводит в заблуждение
**P2** — регулятор в интерфейсе есть, но не управляет ничем · `frontend/app/components/audio/StepByStepWizard.tsx:186,225-236,515,741` · `audio/generate/route.ts:244,258-261`
Каталог TTS — три модели (`functionCatalog.ts:31-33`), но wizard сидит из entitlement и пиннит free к единственной дозволенной; владелец хочет, чтобы free хотя бы ВИДЕЛ другие как upsell. Тумблер «Стандартное / Высокое качество» на OpenAI-пути модель не меняет: wizard хардкодит `gpt-4o-mini-tts`, роут игнорирует `quality`, а легаси `tts-1` — старее, не «выше качеством».
Качество даёт разная модель, а не тумблер у одной. Убрать тумблер либо связать с реальным выбором — в рамках фичи F1.

### Тесты фиксируют форму реализации, а не болезнь
**P2** — зелёный набор такого рода не отсутствие покрытия, а активная ложь: он закрывает вопрос и прячет мёртвую фичу
Wiring-наборы plan/prayer/membership давали 25/25 зелёных при десяти живых P0: plan-тест не нажимает «оставить моё» и не моделирует набор текста в полёте; prayer отдельно доказывает «resolved submit закрывает», а страница отдельно глотает stale; membership не ставит две операции в один series. Нет проверок после реального reload, отказа статуса, перекрывающихся сохранений, ретраев транзакции, кросс-вкладочных сценариев.
За петлю поймано трижды: плашка настроек была мертва при зелёном гейте, счётчик серии не покрыт вовсе, `expect.any(...)` на решающем аргументе. Чинить: каждый тест на суть проверять мутацией, проводку покрывать отдельно от функции.

---

## 🔵 P3 — открыто

### BUG-20260911-collapsed-card-renders-unbounded-search-snippets · Свёрнутая карточка изучения в поиске растёт на несколько экранов
**P3** — не порча данных, но свёрнутая карточка перестаёт быть свёрнутой, и всё, что стоит под превью, уезжает за край экрана · `app/utils/searchUtils.ts:36-72` · вызов `app/(pages)/(private)/studies/StudyNoteCard.tsx:200`
Ожидалось: свёрнутая карточка в списке показывает короткую выжимку — один-два фрагмента, чтобы список оставался списком и глаз сравнивал карточки между собой. Получилось: `extractSearchSnippets` не ограничивает число диапазонов, а карточка зовёт его с окном 300 знаков, поэтому заметка с десятком разнесённых совпадений рисует десяток блоков по ~600 знаков каждый.
Воспроизведение: открыть `/studies`, ввести в поиск слово, которое многократно встречается в длинной заметке и разнесено дальше 320 знаков друг от друга; свёрнутая карточка этой заметки занимает несколько экранов.
Эффект: подвал карточки (стихи, теги, дата, кнопки «поделиться» и «копировать») уезжает под простыню фрагментов; чтобы скопировать заметку, надо прокрутить её всю.
Как эта фича должна работать с точки зрения пользователя: в режиме поиска свёрнутая карточка обязана показать, ПОЧЕМУ она нашлась — счётчик совпадений и один-два фрагмента с подсветкой — и обязана молчать про остальные: полный разбор он получает, раскрыв карточку или открыв заметку. Список существует, чтобы выбирать из него, а не читать его.
Найдено независимым ревью (Codex) при переносе кнопок карточки в подвал 2026-09-11; дефект пред-существующий, перенос лишь сделал его заметным.

---

### BUG-20260813-detail-caches-not-account-scoped · На общем компьютере следующий вошедший может увидеть данные предыдущего
**P3** — понижено владельцем 2026-08-14 по факту эксплуатации: один человек — одно устройство, второй аккаунт в том же браузере практически не встречается · `usePrayerDetail.ts:38` · `useGroupDetail.ts:49` · `useSeriesDetail.ts:116` · персистенция на неделю в `QueryProvider.tsx:109`
Ожидалось: владелец входит в ключ кэша, поэтому кэш другого аккаунта недостижим по построению — как уже сделано для проповеди (`utils/queryKeys.ts:20`). Получилось: ключи детали собираются без uid.
Дефект пред-существующий. Не починено: ключи собираются литералами в 27 местах, менять форму надо ОДНИМ конструктором и с живой проверкой смены аккаунта — иначе расколотый кэш даст порчу хуже утечки.

---

## 🟠 Решение владельца — архитектура

> Пусто.

## 🔍 Требует перепроверки

> Пусто. Пять записей закрыты 2026-07-28 проверкой по якорям: механизмы, которых им не хватало, в коде есть и покрыты тестами.
>
> ⚠️ Доказательство там структурное (код + тесты), а не живое репро двумя вкладками. Если потеря правки всплывёт снова — запись заводится заново, а не воскрешается.

---

## 🟡 Открыто, но не «код-фикс»

### referral Sybil — фейк-аккаунты стакают промо
**P1 (PLAUSIBLE)** · `frontend/app/.../referral.server.ts` → `computeReferralPromotion:25-29`
Награда +30 дней tier1 за КАЖДОГО зарегавшегося и заклеймившего, кумулятивно. Защищает: email_verified + аккаунт младше 24 ч + клейм один раз + не self-referral. Sybil стоит N верифицированных email и N аккаунтов за сутки.
Решение владельца: не авто-блок, а human-in-the-loop → фича F6 (на третьем реферале warning-флаг, письмо владельцу, контрол «отключить промо» в админке). Здесь остаётся как открытая security-заметка.

### purge legacy share-links — data-cleanup
Код-ревалидация сделана: `share/notes/[token]` GET проверяет `note.userId === shareLink.ownerId`. Остаётся разовая чистка невалидных legacy share-links в проде — data-операция, не код.

### env-var cleanup — гигиена
Аудит Vercel-переменных, прочистка мёртвых `#`-строк в `.env.local` и `.env.example`. **Значения секретов и ключей не трогать.** Детали — `FEATURE_CONCEPTS.md` → C1.

---

## ⚪ Осознанно НЕ чиним (решения владельца)
- **Порядки служения: запись, которая никогда не ответит, глушит чтения этой вкладки.** Забор записи (`serviceOrderWriteFence.ts`) считает, сколько записей идёт прямо сейчас, и чтение, пересёкшееся с записью, не публикуется. Пока счётчик не вернулся к нулю, вкладка показывает последний известный список и не узнаёт об изменениях с других устройств. Текст при этом не теряется. Лечить таймером НЕЛЬЗЯ: снятие по истечении срока разрешает устаревшему чтению затереть медленную, но настоящую запись. Правильное лечение — явное состояние «сохранение ещё идёт» и завершаемая граница записи, и оно живёт в том же узле, что и журнал операций. Два мелких следствия того же зависания: baseline-записи закрытых сеансов не снимаются (они стоят за хвостом очереди) и три попытки перепроверить «служение удалено?» тратятся впустую, потому что каждое чтение пересекается с той же зависшей записью; текст при этом не теряется, экран честно говорит «ещё не пришло сюда».
- **Порядки служения: смена маршрута обходит очередь и замок «одна структурная правка за раз».** Редактор пересоздаётся по ключу маршрута, и очередь записей вместе с состоянием «идёт перестановка» живут в экземпляре хука. Ушёл со страницы и сразу вернулся — новый экземпляр ничего не знает о записи, которая ещё летит: можно начать вторую структурную правку, и сериализация, ради которой замок вводился, в этот момент не действует. Транзакция на сервере всё равно атомарна, поэтому документ не портится; страдает только локальное зеркало старого экземпляра, а он уже размонтирован. Лечение — держать очередь и замок рядом с забором записи (`serviceOrderWriteFence.ts`), по ключу владелец+служение, и брать «занято» оттуда.
- **Порядки служения: слова живут в памяти страницы до подтверждения сервера.** Один узел, четыре следствия: (1) `pagehide`/unmount ЗАПУСКАЕТ запись, но транзакции нужен сервер — процесс, убитый в ту же секунду, уносит текст; (2) браузер online, а Firestore недостижим (captive portal, умирающая связь) — запись падает громко, текст остаётся в поле, но reload его теряет; (3) отказ в сохранении названия оставляет напечатанное только в DOM; (4) удаление шага, пересчитанное на свежем документе, уносит слова, дописанные туда другим устройством. Экран в каждом случае ГОВОРИТ об отказе — молчаливой потери нет. Лечение у всех четырёх одно: журнал операций на уровне шага (durable intent до записи, снятие только по подтверждению). Заплатка в одном месте не сходится — Codex три раунда подряд выдавал P1 в этом же узле; узел вынесен в отдельную работу сознательно.
- **Порядки служения: два устройства правят ОДИН И ТОТ ЖЕ шаг** — побеждает последний. Транзакция пересчитывает патч на свежем документе, поэтому разные шаги одного служения сливаются верно; совпадение поля в один и тот же момент — та же мерка, что и у мысли: система для человека, который через часы продолжил на другом устройстве. Настоящее лечение — журнал операций на уровне шага, отдельная работа.
- **Порядки служения офлайн: правка шага уходит в родную очередь Firestore** (`atomicUpdate.client.ts:41` — транзакции офлайн не работают), то есть last-write-wins при возврате связи. Это поведение всего приложения, а не этой секции; отдельная дорожка в `FIRESTORE_SYNC_RESEARCH.md`.
- **Порядки служения: создание, удаление, «завести типовые» и разрядка рангов офлайн запрещены** — не «не успели», а решение. `addDoc` офлайн не отвечает вовсе (кнопка крутилась бы вечно), а очередь на удаление вернулась бы через час и унесла то, что тем временем написало другое устройство. Всё, что пастор ПИШЕТ, офлайн доступно.
- **Порядки служения: два устройства жмут «завести типовые» в одну секунду → двойной набор.** Список перечитывается перед записью, поэтому окно — момент нажатия, а не «пока открыта страница». Детерминированные id закрыли бы это ценой худшего отказа: `setDoc` по известному id ЗАМЕНЯЕТ, и такой seed, проскочивший на умирающей связи, стёр бы уже написанные слова. Дубликат видно и он удаляется; молчаливую замену — нет.
- **Порядки служения: удаление не сверяет ревизию** — пастор может удалить обряд, в который другое устройство только что дописало слова. Диалог подтверждения есть, ревизионного предусловия нет; так же устроены все остальные удаления в приложении.
- **Порядки служения: перестановка — последний писатель побеждает** (`serviceOrders.client.ts:191`). Речь о ПОРЯДКЕ карточек, не о тексте: расходится расстановка, а не слова.
- **Настройки не входят в схему версий** — каждый писатель в `userSettings.service.ts` отправляет только изменённое поле через `setDoc(merge)`. Два устройства над разными настройками не пересекаются по построению; одна и та же настройка — это тумблер, значение которого человек видит и возвращает. Скрытого текста там нет, защита добавила бы только ложные конфликты.
- **Одна и та же секция плана с двух устройств — последний писатель побеждает.** Разные секции больше не сталкиваются (каждая пишется отдельным вложенным путём `plan.<секция>`), но внутри одной секции заявленной версии нет.
- **Правка той же мысли с двух устройств в пределах окна ретрая транзакции** (доли секунды) — цена защиты выше цены риска. Мерка владельца: система для человека, который через ЧАСЫ продолжил на другом устройстве, а не для скриптов, ловящих доли секунды.
- **share/notes viewCount** инкремент (`share/notes/[token]/route.ts:34`) — аноним накручивает чужой счётчик-тщеславие, ноль импакта.
- **share-links GET mismatch** (`share-links/route.ts:16`) — игнорит mismatched query `userId`, но возвращает СВОИ линки звонящего, не жертвы → не уязвимость, косметика.
