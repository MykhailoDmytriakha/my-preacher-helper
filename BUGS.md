# BUGS.md — трекер багов

> **Правило:** сломано/неверно → сюда (open, с якорем `file:line` + severity P1/P2). «Хорошо бы добавить» → `FEATURE_CONCEPTS.md`. Починил → перенеси в «Исправлено» кратко (детали живут в git-истории и памяти, не дублируем сюда).

---

## 🔴 Открытые баги — нужно починить

### Три AI-endpoint'а живут на дефолтных 10 секундах Vercel — P1, РАЗОБРАТЬСЯ ПО ПОЛНОЙ (2026-07-25)
**Был.** `frontend/vercel.json` поднимает `maxDuration: 60` поимённо, роут без записи получает **дефолт Hobby = 10 секунд**. Три AI-роута в списке отсутствуют:

| Роут | Что делает | Точка в UI (проверено живьём) | Вход в LLM |
|---|---|---|---|
| `app/api/insights/plan/route.ts` | `generateSectionHints` — подсказки «что положить во вступление / основную / заключение» (`insights.sectionHints`) | «Размышления над проповедью» → блок **«Предположенный план»**, иконка обновления; плюс 4-й шаг «Сгенерировать все инсайты» (`KnowledgeSection.tsx:225`, `:781`) | ВЕСЬ текст проповеди — самый тяжёлый |
| `app/api/sermons/[id]/generate-outline-points/route.ts` | `generateSermonPoints` — 3-5 пунктов плана для ОДНОЙ секции | Страница структуры, режим фокуса → сайдбар «Пункты плана» → кнопка **«Создать»** (`Column.tsx:1297`, активна только когда у секции ещё нет пунктов) | содержимое одной секции |
| `app/api/sermons/[id]/brainstorm/route.ts` | `generateBrainstormSuggestion` — одна подсказка-толчок, когда застрял | Классический режим → кнопка **«Идеи»** → панель → **«Создать»** (`ClassicThoughtsPanel.tsx:297`) | маленький вход, короткий выход |

**Не легаси.** Все три достижимы кликами (проверено в браузере на `test sermon 1`, dev-аккаунт) и трогались активной работой: `c75f6b37`/`c9e376b0` (монетизация + usage-лимиты, 14-15 июля), `805dfc6c` (freshness, 25 июля).

**Эффект.** Клиент по `category: 'ai'` ждёт **90 секунд** (`app/utils/apiClient.ts:19`) — сервер убивает функцию на десятой. Спиннер крутится над мёртвым запросом. У `insights/plan` хуже всего: `insights.service.ts:135` глотает ошибку и возвращает `null` ⇒ **тихий отказ**, блок подсказок просто не появляется. И он последний в цепочке «Сгенерировать всё», где первые три шага (`topics`/`verses`/`directions`) уже имеют 60s — то есть цепочка систематически доезжает до финиша и там умирает.

**Фикс (направление).** По три строки в `frontend/vercel.json` + строки в `__tests__/config/vercel-functions.test.ts` (там уже заведён гейт `>= 60`). `maxDuration` — потолок, а не резерв: платится фактическое время, поднятие лимита не удорожает быстрые ответы.

**Разобраться по полной (когда вернёмся).** Поднять лимит — необходимое, но не достаточное: (1) убрать тихий `null` в `generateThoughtsBasedPlan` — отказ должен быть виден человеку; (2) сверить клиентские таймауты с серверными по ВСЕМ AI-путям (сейчас разнобой: 55s у compose-plan против 90s дефолта); (3) решить, не пора ли гейтить наличие роута в `vercel.json` тестом-сканером по всем `app/api/**/route.ts`, которые импортируют AI-клиент, — поимённый список уже дважды протёк.

**Родня.** Тот же класс уже починен для `app/api/sermons/[id]/compose-plan-from-scratch/route.ts` — коммит `9b978c5e` (боевой таймаут в проде 2026-07-25 22:31, «Task timed out after 10 seconds»).

### Защищённая запись ЛОМАЛА офлайн-сохранение — P0, было хуже HEAD — ИСПРАВЛЕНО (2026-07-25)
**Был.** `conflictSafeUpdate` всегда шёл через `runTransaction`. По документации Firebase транзакции ОФЛАЙН НЕ РАБОТАЮТ — обычные записи ставятся в очередь и доигрываются при реконнекте, транзакции просто падают (https://firebase.google.com/docs/firestore/manage-data/enable-offline). До защиты офлайн-правка заметки/проповеди/серии сохранялась при возврате связи; после — падала.
**Эффект.** Механизм, созданный чтобы НЕ терять текст, сам терял офлайн-правку. Классическое «хуже HEAD».
**Фикс.** `frontend/app/services/conflictSafeUpdate.client.ts`: недостижимость сервера (`unavailable` / `deadline-exceeded` / «client is offline») → деградация ровно в до-защитное поведение — обычная очередная запись с `increment(1)`, не `await` (офлайн `updateDoc` не резолвится). Отказ (`StaleWriteError`) и любая настоящая ошибка НИКОГДА не проглатываются. Возвращается `expectedRevision+1` — безопасная сторона: если кто-то всё же записал, следующее сохранение получит ОТКАЗ, а не затрёт.
**Тесты.** 4 теста в `conflictSafeUpdate.client.test.ts`; мутация «убрать запасной путь» роняет 1, мутация «глотать и отказ тоже» роняет 5. Плюс `useGroupDetail.offline.test.tsx` теперь честно моделирует падение транзакции офлайн и доказывает путь насквозь.

### После защищённой записи перечитывался документ — экран получал ДО-коммитное состояние — P1, ИСПРАВЛЕНО (2026-07-25, найдено зондом живьём)
**Был.** `frontend/app/services/sermons.client.ts:221` после успешной транзакции делал `getSermonByIdViaClient(...)` и возвращал прочитанное. Зонд в браузере показал: сразу после коммита `getDoc` отвечает из локальной реплики, которая ЕЩЁ НЕ ДОГНАЛА собственную запись — вернулись текст «телефона» и версия 5, хотя в базе уже лежала версия 6 с моим текстом.
**Эффект (наблюдался живьём трижды).** Верная запись выглядела проваленной: в базе мой стих, на экране — чужой, до перезагрузки. Человек в такой ситуации набирает текст заново. Плюс возвращённая ДО-коммитная версия делала следующую правку того же человека «устаревшей» ⇒ ложный отказ на своей же второй правке.
**Фикс (класс, а не экземпляр).** Защищённая запись возвращает то, что закоммитила, и НИКОГДА не перечитывает: `sermons.client.ts` (+ `rev.core = committed`), `groups.service.ts` (+ `rev.content`), `series.service.ts` (+ `rev.meta`); `studies.service.ts` так делал с самого начала — это и был образец. Из `SermonHeader.handleKeepMine` пере-чтение убрано.
**Тесты.** 2 теста в `app/services/__tests__/sermons.client.test.ts` (один прямо моделирует отстающую реплику) + переписанный тест `SermonHeader`; мутация «вернуть перечитывание» роняет их.
**Живьём.** Четвёртый прогон на `test-fresh-sermon`: отказ → «Оставить мой текст» → мой стих на экране БЕЗ перезагрузки, плашка свежести погасла, в базе `verse="ФИНАЛЬНЫЙ ПРОГОН"`, `rev.core=6`.

### Независимый Popper (Codex gpt-5.6-sol/xhigh, 2026-07-25): REJECT — 3 P0, 10 P1, 1 P2
Полный отчёт: `.claude/codex-logs/2026-07-25_22-19-50_freshness-popper-final.final.txt`.

**ЗАКРЫТО в этом витке:**
- ✅ P0 «офлайн-fallback теряет текст» — мой же сегодняшний фикс оказался дырой: незащищённая last-write-wins запись + ЛОЖНЫЙ успех ⇒ редактор ретировал долговечный черновик, а при реконнекте очередь затирала чужое. **Откачено целиком** (`conflictSafeUpdate.client.ts`): офлайн теперь честный отказ, ничего не пишется, черновик жив; экспортирован `isUnreachableWriteError`, чтобы UI мог сказать «нет связи», а не «конфликт». Офлайн-тесты переписаны: раньше они УДОСТОВЕРЯЛИ опасное поведение, теперь его запрещают (мутация «вернуть fallback» роняет 2).
- ✅ P1 «взять чужую версию гасит конфликт при провалившейся загрузке» — группа и серия судят по РЕЗУЛЬТАТУ `refetch()` (он резолвится с ошибкой, а не бросает); молитва — по состоянию запросов.
- ✅ P1 «persisted-replay шаблона плана теряет версию» — `mutationDefaults.ts` пробрасывает `expectedRevision`.
- ✅ P1 «группа выбрасывает закоммитированную версию ⇒ ложный отказ на своей же 2-й правке» — версия держится в ref (не зависит от круга через кэш) + пишется в оба кэша.
- ✅ P1 «молитва гасит конфликт в момент клика» — гасится в `onSuccess`, по факту коммита.

**ОТКРЫТО (не чинил сейчас, направление указано):**
- 🟡 **ПОДГОТОВЛЕНО, АКТИВАЦИЯ ЗА ВЛАДЕЛЬЦЕМ (P0) · `frontend/firestore.rules`** — правило `advancesRevision(aggregate)` написано и задокументировано прямо в файле, но оставлено ЗАКОММЕНТИРОВАННЫМ: включение — решение о раскатке, а не правка кода. В момент выкатки любой клиент, не двигающий счётчик, начнёт получать permission-denied — включая старые PWA-установки, ещё не подхватившие новый бандл. В файле лежит чек-лист активации: (1) новый бандл прожил достаточно, чтобы SW обновил старые установки; (2) писатели через Admin SDK проверены — они правила ОБХОДЯТ и обязаны двигать счётчик сами; (3) выкатить правила и следить за permission-denied в логах.
- ✅ **ЗАКРЫТО (P0) · дашбордный persisted-писатель проповеди** — теперь шлёт хирургический патч `{title, verse}` и заявляет версию; три писателя статуса «отпроповедано» шлют только `{isPreached}` (раньше отметка статуса ревертила чужие правки заголовка/стиха/подготовки). Тесты: `__tests__/utils/dashboardSermonReplay.test.ts`, мутация «вернуть снимок целиком» роняет оба.
- ✅ **ЗАКРЫТО (P1) · текст ответа молитвы под защитой и в детекторе** — `setPrayerStatusViaClient` принимает версию агрегата `status` и идёт защищённым путём (версия протянута через сервис, хук, страницу и persisted-replay). Селектор свежести теперь смотрит `answerText` и ТЕКСТ обновлений (`id:text`), а не их количество — два «отвеченных» с разных устройств больше не проходят молча.
- ✅ **ЗАКРЫТО (P1) · отклонённый текст переживает перезагрузку** — общий `app/hooks/usePersistedConflict.ts` зеркалит ожидающий конфликт в долговечное хранилище (`conflict:<агрегат>`) и поднимает при монтировании. Подключено: проповедь, серия, группа, шаблоны планов. Молитва подключена: хук принимает `activeDocId` (страница передаёт id открытой молитвы), и слот конфликта становится долговечным; списковые экраны id не передают — они текст не редактируют.
- ✅ **ЗАКРЫТО (P1) · у шаблонов планов теперь полный выбор** — отказ поднимает `SaveConflictBanner` с показом отклонённого имени, «оставить моё» пере-отправляет с серверной версией, «взять чужое» судит по результату обновления. Конфликт долговечен. Тесты + мутация «только тост» роняет оба.
- ✅ **ЧАСТИЧНО ЗАКРЫТО (P1) · «Сохранено» до подтверждения** — `updatePrayer` теперь `mutateAsync`: страница действительно ждёт исход и не показывает «Обновлено» до записи; офлайн промис просто не резолвится (честно), а отказ доходит до вызывающего. Группа тоже закрыта: `useGroupDetail` считает записи «в полёте» (`pendingWrites`), страница показывает «сохраняю», пока счётчик > 0 — офлайн так и остаётся «сохраняю», что и есть правда. Мутация «не считать» роняет тест.
- ✅ **ЗАКРЫТО (P1) · детектор смотрит на СОДЕРЖИМОЕ** — `app/utils/contentFingerprint.ts` (стабильный отпечаток с нормализацией порядка ключей; `undefined` = отсутствие, как в Firestore). Проповедь теперь следит за мыслями, планом-конспектом, планом, подготовкой и набросками; группа — за блоками, потоком и датами встреч; серия — за самими элементами (перестановка ловится); молитва — за текстом ответа и текстом обновлений. Лишних чтений нет: слушатель и так получает документ целиком. Мутация «считать длину» роняет 3 теста.
- ✅ **ЗАКРЫТО (P1) · членство в серии транзакционно** — ОНЛАЙН чтение и запись идут в ОДНОЙ `runTransaction` (SDK перезапускает callback, если целевой документ изменился), поэтому «оба прочитали [A,B], один добавил C, другой переставил» больше не теряет C. ОФЛАЙН сохраняется прежний путь чтение+batch: транзакция без сервера невозможна, а этот писатель обязан работать офлайн. Добавлен собственный агрегат `rev.items`, так что перестановка (длина не меняется) видна и счётчику. Мутация «всегда старый путь» роняет 2 теста; отдельный тест закрепляет офлайн-ветку.
- **P1 · долговечный черновик лосси при нескольких вкладках и переполнении квоты** (уже описано выше отдельной записью).

**Возражение по премиссе (принято к сведению).** Счётчик как токен сравнения корректен, но гарантия держится на дисциплине ВСЕХ писателей; единой авторитетной границы записи в проекте нет. Хэш эту проблему не решает (его так же можно не проставить). Предложено: разнести независимо редактируемые части по документам либо ввести строгое владение полями, и провести все защищённые мутации через одну границу `{baseVersion, patch, operationId}`; офлайн — durable outbox, повторяющий ТОТ ЖЕ CAS.

### ⛔ ВТОРОЙ независимый Popper (2026-07-26): REJECT — 8 P0, 6 P1, 1 P2. ТРЕНД СЛОМА ВЫРОС → ЭСКАЛАЦИЯ
Отчёт: `.claude/codex-logs/2026-07-25_23-43-30_freshness-popper-round2.final.txt`. Первый прогон: 3 P0 / 10 P1. Второй: 8 P0 / 6 P1. **P0 стало БОЛЬШЕ — по правилу процесса это стоп и решение владельца, а не третья волна правок.**

**ЧЕТЫРЕ РЕГРЕССИИ, внесённые МОЕЙ волной, — ПОЧИНЕНЫ (не закоммичено по просьбе владельца):**
- ✅ Конфликт шаблонов планов не восстанавливался и мог стереть правку: ключ строился из `conflictTemplateId`, который стартует `null`, ставится в одном батче с самим конфликтом и неизвестен при монтировании. Теперь ОДИН слот на пользователя (`plan-templates`), а id шаблона едет ВНУТРИ полезной нагрузки.
- ✅ Отказ на ответе молитвы закрывал модалку по ложному успеху и терял текст: `setStatus` возвращался в момент отправки. Теперь `mutateAsync`, страница ловит отказ и НЕ закрывает модалку.
- ✅ Фантомная плашка на СТАРЫХ сериях: локально `items` выведены из `sermonIds`, удалённо их нет — отпечатки не совпадали никогда, «изменено на другом устройстве» не гасилось ничем. Обе стороны нормализуются одинаково.
- ✅ Членство в серии теряло очередь, когда `navigator.onLine` ЛЖЁТ (captive portal): транзакция падала, оптимистичный UI показывал успех, инвалидация стирала. Возвращён запасной batch по `isUnreachableWriteError`; настоящая ошибка по-прежнему всплывает.

**ОСТАЛОСЬ ОТКРЫТЫМ — это архитектура, а не правки (все «merely unfixed vs HEAD», кроме отмеченного):**
- ✅ **ЗАКРЫТО (P0) · пара «текст редактора ↔ версия»** — версия ФИКСИРУЕТСЯ в момент открытия формы (`editBaseRevision` на странице серии и молитвы) и передаётся в сохранение; живое значение остаётся запасным вариантом только для записей, собранных прямо сейчас. Мутация «брать живую версию» роняет тест.
- **(историческое) P0 · пара «текст редактора ↔ версия» рвалась** (`useDocumentFreshness.ts:68` + открытые модалки серии/молитвы): `knownRef` следует за живым объектом запроса, а модалка держит СТАРЫЙ текст ⇒ сохранение уходит со СВЕЖЕЙ версией и старым текстом, и CAS его ПРОПУСКАЕТ. Направление: версия обязана фиксироваться вместе со снимком текста в момент открытия формы (неизменяемая пара), а не браться из кэша в момент отправки.
- 🟡 **МЕХАНИЗМ ПОСТРОЕН, ПОДКЛЮЧЕН ЧАСТИЧНО (P0) · офлайн больше не пишет вслепую.** `app/services/writeOutbox.client.ts` хранит НАМЕРЕНИЕ (патч + агрегат + версия, от которой построен текст); `outboxReplay.client.ts` при возврате связи прогоняет каждое намерение через ТОТ ЖЕ `conflictSafeUpdate` с ИСХОДНОЙ версией. Отказ ⇒ запись помечается `conflicted` и НЕ переотправляется автоматически (переотправка со свежей версией и была бы тихим затиранием). Вызывающий получает типизированную `OfflineQueuedError` — «сохраню, когда будет связь», а не «сохранено». Доигрывание вешается хуком `useOutboxReplay` (при монтировании + на событие `online`). 6 тестов, две мутации краснеют.
  ✅ **ПОДКЛЮЧЕНЫ ВСЕ ШЕСТЬ ПИСАТЕЛЕЙ**: заметка, проповедь (`core`), серия (`meta`), группа (`content`), молитва (`core` и `status`), шаблон плана — каждый передаёт `outboxRoute`.
  ✅ **UI ЕСТЬ И ОН ОБЩИЙ**: `app/components/OutboxConflictBanner.tsx` смонтирован в приватном layout — доигрывает очередь при входе и на событие `online`, показывает отклонённый текст и даёт выбор. Перезапись происходит ТОЛЬКО по явному нажатию и заявляет версию, увиденную при отказе; при провале перезаписи запись НЕ удаляется. 5 тестов, две мутации краснеют.
- 🟡 **ПОЛОВИНА ЗАКРЫТА (P0) · счётчик не авторитетен.** ✅ Серверные писатели выровнены: `thoughts-by-section` писал напрямую мимо счётчика — теперь двигает `rev.thoughts`; `updateSermonOutline` двигает `rev.outline`; три писателя дат проповеди — `rev.preachDates`. Мутация «снова без агрегата» роняет тест. ⚠️ ОСТАЁТСЯ и это НЕ код: правило в `firestore.rules` не взведено, поэтому СТАРЫЙ клиент на телефоне по-прежнему может менять данные, не двигая счётчик. Взведение = решение о раскатке (чек-лист в самом файле правил).
- ⚠️ **ЧАСТИЧНО НЕВЕРНО (P0) · полномассивные RMW** — проверено по коду: добавление мысли и добавление даты встречи идут через `atomicUpdate` (транзакция + `retryTransientAsQueuedWrite`), то есть уже защищены. НЕ защищены два: `updateThoughtViaClient` (намеренно — слияние полного объекта от вызывающего небезопасно при повторе, задача #18) и `writeScratchNotesViaClient` (полный массив набросков). Направление: для набросков — тот же `atomicUpdate`; для мысли — передавать дельту, а не весь объект.
- ✅ **ЗАКРЫТО (P0) · вытеснение по квоте не трогает конфликты** — записи `conflict:` исключены из вытеснения (ординарный черновик дублирует текст, видимый в редакторе; запись конфликта — последняя копия отклонённого текста), а исчерпание места теперь громко логируется, а не молчит. Мутация «снова вытеснять» роняет тест.
- ✅ **ЗАКРЫТО (P0) · слот конфликта больше не стирается чужой вкладкой** — `usePersistedConflict` помнит СВОЙ конфликт и гасит слот только через `clearDraftIfMatches`. Мутация «стирать безусловно» роняет тест.
- **(историческое) P0 · долговечный черновик агрессивен при quota**: `durableDraft.ts:65,173` при исключении сносит половину ключей и молчит, если повтор тоже не удался. Теперь через него ходят и конфликты ⇒ радиус поражения вырос.

- **P1 · автосейв группы** может конфликтовать сам с собой (перекрывающиеся сохранения переиспользуют одну подтверждённую версию), а поздний успех гасит конфликт более поздней правки.
- **P2 · тесты фиксируют ФОРМУ реализации**, а не болезнь: нет ремонта после реального reload, отказа статуса, перекрывающихся сохранений, ретраев транзакции, captive-portal, quota и кросс-вкладочных сценариев.

**Вердикт по премиссе (второй раз подряд).** Клиентские слушатели + необязательный клиентский CAS не могут доказать свойство «ВСЕГДА/НИКОГДА». Нужны: одна авторитетная граница записи для ВСЕХ писателей, неизменяемая пара `{текст редактора, базовая версия}` и durable outbox, доигрывающий через тот же защищённый протокол.

### ✅ ЖИВАЯ ПРОВЕРКА ОФЛАЙН-ОЧЕРЕДИ (2026-07-26, браузер + живая база, `test-fresh-sermon`)
Полный цикл на финальном коде рабочего дерева:
1. Страница открыта на версии 4 → браузер переведён в офлайн → правка стиха «ПРАВКА ИЗ ДОРОГИ».
   → В базу НИЧЕГО не ушло. В очереди одно намерение: `status:'pending'`, `baseRevision:4`, патч со стихом. Текст остался на экране.
2. «Телефон» пишет свой стих и версию 5.
3. Связь возвращается (`online`) → доигрывание идёт через ТУ ЖЕ проверку версии → **ОТКАЗ**: запись помечена `conflicted`, `actualRevision:5`, в базе цел текст телефона, на экране панель «Ваша правка не сохранена» с моим текстом.
4. «Оставить мой текст» → в базе `verse="ПРАВКА ИЗ ДОРОГИ"`, `rev.core=6`, очередь пуста, панель ушла.
Следы убраны, аудит по пяти коллекциям: тестовых документов 0.

**Попутно понято про стенд:** доигрывание намеренно идёт БЕЗ офлайн-маршрута, поэтому подделка `navigator.onLine` его не обманывает — оно честно пробует транзакцию. Это и доказало счастливый путь в первом заходе (намерение встало → доиграно → закоммичено). Чтобы поймать ОТКАЗ, «телефон» должен успеть записать между постановкой в очередь и доигрыванием.

### 📏 ЗАМЕР ЧТЕНИЙ ПЕРЕСЧИТАН (2026-07-26, по живой базе, после всех правок)
Скрипт: `scratchpad/measure-reads.js` (читает реальные коллекции, не угадывает).
База: **201 документ, 15 владельцев**, на владельца медиана **1**, максимум **155**.

Модель одного дня одного человека (щедрая): открыл 12 документов (каждый — 1 чтение на подписку) + по 3 удалённых изменения на открытый документ (3 чтения) + 4 загрузки списков + 1 переподключение с доигрыванием.
- **Слушатели свежести: 48 чтений/чел/день** — это ЦЕНА МЕХАНИЗМА.
- Списки: 5 чтений/чел/день при медианном владельце.
- **Итого при 30 активных: 1590 чтений/день = 3,2% лимита Spark.** Порог 20% не превышен → оптимизация не требуется.

**Худший случай честно:** если КАЖДЫЙ из 30 нагружен как самый крупный владелец (155 документов) — 24 690 чтений/день = **49,4%**. Но узкое место там **не механизм свежести**, а загрузка СПИСКОВ (775 из 823 чтений на человека). Направление, если это когда-нибудь понадобится: пагинация/курсоры на списках; подписки трогать не нужно.

**Что НЕ стоит чтений:** пульс очереди раз в минуту читает только localStorage; Firestore он касается лишь когда есть намерение, и это ЗАПИСЬ. Инвалидация после доигрывания сужена до затронутой коллекции — раньше `invalidateQueries()` без ключа перечитывал все открытые запросы (пик чтений из-за одной проповеди).

### ЧЕТВЁРТЫЙ независимый Popper (2026-07-26, с ЧЕЛОВЕЧЕСКОЙ меркой в мандате): 5 P0 / 8 P1 / 1 P2
Отчёт: `.claude/codex-logs/2026-07-26_01-*_freshness-popper-round4.final.txt`. **Опроверг три моих утверждения — и был прав.**

**⚠️ ГЛАВНЫЙ УРОК ВИТКА: я отчитался о фиксе, которого НЕ БЫЛО.** Заявил, что при недостижимом сервере (captive portal) намерение ставится в очередь. Проверка по коду: вокруг транзакции вообще не было `catch` — мой более ранний откок снял `try`, и правка «поставить в очередь» ушла в никуда, а я этого не перепроверил. Прокси («я это писал») вместо якоря («вот строка»). Отсюда правило: после КАЖДОЙ правки, о которой отчитываюсь, — grep по факту в файле.

**ЗАКРЫТО в этом витке:**
- ✅ **P0 · captive portal теперь ставит намерение** — `try/catch` вокруг транзакции восстановлен; недостижимость → очередь + `OfflineQueuedError`; отказ и настоящие ошибки не проглатываются.
- ✅ **P0 · отпечаток содержимого едет в очереди** — `OutboxEntry.expectedFingerprint`; доигрывание передаёт его, значит офлайн-правка защищена от писателя, не двинувшего счётчик, ровно как прямое сохранение.
- ✅ **P1 · собственные последовательные правки больше не конфликтуют сами с собой** — доигрывание помнит, что уже закоммитило в этом проходе (по документу+агрегату), и перебазирует следующее намерение; отпечаток при этом снимается (содержимое изменили МЫ). Мутация «убрать перебазирование» роняет тест. ЭТО важнейший пункт по человеческой мерке: ложный отказ на своей же правке приучает жать «оставить моё» не читая.
- ✅ **P1 · «поставлено в очередь» больше не показывается как ОШИБКА** — иначе человек жмёт «сохранить» ещё раз, и каждое нажатие плодило новое намерение с той же базовой версией.

**ОСТАЛОСЬ ОТКРЫТЫМ (с якорями и направлением):**
- ✅ **ЗАКРЫТО (P0) · офлайн-членство серии ставит ОПЕРАЦИЮ, а не массив** — `enqueueMembershipTransforms` хранит сам transform (add/remove/reorder), `replayMembershipOutbox` при реконнекте применяет его к тому, что РЕАЛЬНО лежит, через ту же транзакцию. Прежний путь строил весь список из кэшированного чтения и через часы стирал то, что добавило другое устройство. Путь «массив из кэша» удалён целиком. Доигрывание вызывается оттуда же, откуда очередь записей (`OutboxConflictBanner`). Тесты: «офлайн ставит операцию» + «доигрывание сохраняет чужой элемент D»; мутация «молча ничего» роняет оба.
- ✅ **ЗАКРЫТО (P0) · отпечаток подключён ко ВСЕМ защищённым писателям** — заметка, группа, молитва (текст и статус) считают его из СОХРАНЁННЫХ значений тех же полей, что перезаписывают; проповедь — из значения поля на момент начала правки.
- ✅ **ЗАКРЫТО (P0) · правка мысли стала транзакционной** — это был исходный баг владельца в миниатюре: поправил мысль A с телефона, мысль B с ноутбука через час — и массив ноутбука, собранный из того, что он прочитал ДО, молча возвращал старую A. Теперь слияние считается ВНУТРИ каждой попытки транзакции из СВЕЖЕ прочитанного документа: целевая мысль идемпотентна, соседние всегда актуальны. Прежнее возражение («перезапуск применит устаревшую нагрузку») верно лишь когда слияние берётся из снимка массива у вызывающего — при чтении внутри попытки его нет. Остаётся узкое: правка ТОЙ ЖЕ мысли с двух устройств в пределах окна ретрая — доли секунды, против потери соседей через часы. Плюс серверный зеркальный PUT (`api/thoughts/route.ts`) теперь двигает `rev.thoughts` — он идёт с правами Admin и правила его не поймали бы. Мутация «убрать счётчик» роняет 2 теста.
- ✅ **ЗАКРЫТО (P1) · у содержимого РЕШАЮЩИЙ голос** — если поля, которые мы перезаписываем, байт-в-байт те, с которых начинали, запись проходит, что бы ни говорил счётчик; если нет — отклоняется, как бы счётчик ни успокаивал. Счётчик один на весь агрегат, поэтому раньше «телефон отметил проповедь прочитанной» ⇒ отказ на сохранении ЗАГОЛОВКА. Ложные отказы приучают жать «оставить моё» не читая — по человеческой мерке это дороже, чем кажется. Мутация «счётчик решает первым» роняет тест.
- ✅ **ЗАКРЫТО (P1) · после доигрывания представления перечитываются** — `replayOutbox` возвращает список ЗАТРОНУТЫХ документов, и при `replayed > 0` баннер инвалидирует кэш. Иначе своя же отложенная правка ложилась на сервер, экран держал старое значение, и слушатель сообщал «изменено на другом устройстве» — предупреждение о собственном сохранении.
- ✅ **ЗАКРЫТО (P1) · доигрывание ставит СВЕЖУЮ дату** — понедельничный `updatedAt`, применённый в пятницу, топил документ в сортировке «недавние»: собственная работа выглядела нетронутой. Мутация «вернуть исходную дату» роняет тест.
- ✅ **ЗАКРЫТО (P1) · удалённый документ превращается в видимый выбор** — «not found» помечает намерение конфликтным, текст показывается человеку, и ретраи прекращаются (раньше мёртвая цель долбилась до 1440 раз в сутки). Мутация «снова ретраить» роняет тест.
- ✅ **ЗАКРЫТО (P1) · шаблоны: после разрешения показывается СЛЕДУЮЩИЙ конфликт** (`advanceToNextStoredConflict`), а «оставить моё» перецеливается на версию, увиденную при повторном отказе — иначе кнопка не срабатывала никогда.
- ✅ **ЗАКРЫТО (P1) · подавление холодного старта больше не глушит НАСТОЯЩЕЕ «не знаю»** (`useDocumentFreshness.ts`). Было: пока слушатель ни разу не ответил, состояние молчит — чтобы не мигать плашкой на каждом открытии страницы. Но ровно так же молчит и подключение, которое умерло на час: человек час смотрел на экран, про который приложение не знало ничего, и был уверен, что видит свежее. Теперь молчание становится новостью двумя путями: таймер на 15 секунд (`silenceIsNews`, снимается при размонтировании) и немедленно, если браузер сам сообщает `navigator.onLine === false`. Тесты: «признаёт неизвестность, когда молчание достаточно длинное» + «говорит неизвестность сразу, когда браузер сообщает об отсутствии связи» — 13/13.
- ✅ **ЗАКРЫТО (P2) · серверные отцепления серии двигают счётчик членства** — ТРИ писателя `items` в `app/api/repositories/series.repository.ts` (транзакционное отцепление при удалении проповеди `:95`, `removeSermonFromAllSeries` `:135`, `removeGroupFromAllSeries` `:163`) теперь добавляют `'rev.items': FieldValue.increment(1)`. Они идут с правами Admin, поэтому правила их не поймали бы никогда, а открытый экран серии продолжал заявлять версию, которой у сервера уже нет, — и его следующее сохранение получало разрешение затереть отцепление. **Важно про тесты:** первый прогон был ложно-зелёным — 14/14 проходили и с фиксом, и с мутацией «снять счётчик», то есть счётчик не был покрыт вообще. Добавлены три утверждения (по одному на писателя), после чего мутация роняет ровно 3 теста.

### ТРЕТИЙ независимый Popper (2026-07-26): 7 P0 / 3 P1 → закрыто 8, осталось 2
Отчёт: `.claude/codex-logs/2026-07-26_00-52-51_freshness-popper-round3.final.txt`. Шесть из десяти — регрессии моей же волны.

**МЕРКА ОТБОРА (задана владельцем 2026-07-26):** система для ЧЕЛОВЕКА, который поработал на одном устройстве и через часы продолжил на другом, — не для скриптов, ловящих доли секунды. Находки, требующие одновременного набора текста в двух местах, сознательно НЕ чиним: цена защиты выше цены риска, и именно охота за долями секунды породила предыдущие регрессии.

**ЗАКРЫТО в этом витке (всё — человеческий масштаб):**
- ✅ Очередь была ОДНИМ слотом: id выводился из документа+агрегата, поэтому вторая офлайн-правка того же агрегата (заголовок в одной вкладке, стих в другой) стирала первую ДО всякого сервера. Теперь `newIntentId()` на каждое намерение, доигрывание идёт от старых к новым.
- ✅ Провал записи в хранилище выдавался за «поставлено в очередь»: `enqueueWrite` теперь возвращает результат, и без реальной записи вызывающий получает обычный отказ, а не обещание, которое некому исполнить.
- ✅ Намерение принадлежало ВЛАДЕЛЬЦУ ДОКУМЕНТА из кэша: на общем браузере аккаунт B, правя устаревшую копию документа A, ставил намерение под A — и оно проигрывалось в СЕССИИ A. Теперь берётся uid вошедшего, и при несовпадении не ставится вовсе.
- ✅ Captive portal (`onLine` врёт, транзакция падает): раньше текст оставался только в DOM. Теперь тоже ставится в очередь и доигрывается через ту же проверку.
- ✅ Запасной batch членства серии УДАЛЁН: он перестраивал массив из КЭШИРОВАННОГО чтения и мог удалить то, что добавило другое устройство. Настоящий офлайн по-прежнему идёт очередным batch (как всегда и было), а при лгущем `onLine` ошибка честно всплывает.
- ✅ Незавершённые записи очереди больше не висят молча: показываются отдельной строкой, плюс пульс раз в минуту (событие `online` не приходит, когда оживает captive portal или когда связь вернулась при скрытой вкладке).
- ✅ «Оставить моё» перецеливается: если между отказом и нажатием документ ушёл дальше, номер обновляется, иначе кнопка никогда бы не сработала.
- ✅ Состояние «свежесть неизвестна» стало ВИДИМЫМ (жёлтая плашка, отдельные слова, без кнопки «загрузить свежую» — грузить нечего).
  ⚠️ И тут же найдена СВОЯ ошибка: начальное состояние хука — `unknown`, значит плашка мигала бы при КАЖДОМ открытии любой страницы. Под человеческой меркой это худший баг: приложение кричит «волки», человек приучается не читать. Исправлено — до первого ответа слушателя молчим; «неизвестно» показывается только когда мы УЖЕ знали и связь пропала (кэш-снимок или мёртвый слушатель). Мутация «снова тревожить на старте» роняет 2 теста.

**ОСТАЛОСЬ (2):**
- **P0 · правило Firestore не взведено** — решение о раскатке, чек-лист в `frontend/firestore.rules`. Кнопка владельца.
- ✅ **ЗАКРЫТО · слот конфликта шаблонов теперь на КАЖДЫЙ шаблон** + поиск при монтировании (`findDraftDocIds`), так что после перезагрузки отклонённая правка находится сама. По дороге вскрылась та самая ловушка, о которой писал ревьюер: ключ строился из состояния, которое ставится в ТОМ ЖЕ батче, — сеттер теперь принимает явный адрес документа. Мутация «убрать явный адрес» роняет тест «два отклонённых шаблона + перезагрузка».

### 🔑 Приватный ключ service-account утёк в лог сессии — P1, действие владельца (2026-07-25)
**Что случилось.** Скрипт-аудитор делал `JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT)`, а переменная хранится в **base64**. Node напечатал упавшую строку целиком в вывод ⇒ ключ `firebase-adminsdk-fbsvc@my-preacher-helper` (id `91831cd4eeb3916b4473e32944fe6b479123a6c8`) лежит в транскрипте сессии на диске.
**Эффект.** Admin-ключ обходит правила безопасности Firestore — это полный доступ ко всем данным всех пользователей.
**Действие (кнопка владельца).** Google Cloud Console → IAM → Service Accounts → Keys: удалить старый, создать новый, обновить `.env.local` и переменную в Vercel.
**Урок для скриптов.** Разбор секрета оборачивать в `try/catch` и НИКОГДА не давать исходной строке попасть в stdout/stderr — падение парсера печатает секрет.
### Ревизия-защита: 2 P0 закрыты, ОСТАЛЬНЫЕ ОТКРЫТЫ — P1 (Popper 2026-07-25, `.claude/codex-logs/2026-07-25_17-15-36_popper-final.final.txt`)
**Вердикт NO-GO. Ключевой инвариант, который был НАРУШЕН:** `expectedRevision` обязана описывать ИМЕННО ТОТ текст, который сохраняется, а не «самую свежую версию, о которой слышал клиент».
**✅ Закрыто.** (1) Слушатель двигал `serverRevisionRef` вперёд, пока в редакторе оставался СТАРЫЙ текст → устаревший текст уходил с СВЕЖЕЙ версией, и защита его пропускала, уничтожая более новую. Теперь снимок пишет в отдельный `remoteRevisionRef`, а версия сохранения двигается только вместе с текстом (`applyRemote`) или при подтверждённом сохранении. (2) Конфликт не останавливал автосейв — правки продолжали лететь в документ, о котором известно, что он новее. Теперь `saveBlocked` глушит автосейв до выбора человека. Оба закреплены тестами (12 в `conflictSafeUpdate.client.test.ts`).
**⏳ ОТКРЫТО — P0 по классификации Popper:**
- **офлайн-доигрывание срезает версию:** persisted-мутация принимает только `{id, updates, userId}` (`mutationDefaults.ts:424`) → после перезагрузки та же правка идёт обычным `updateDoc`, не двигает `rev` и может затереть новое;
- ✅ **ЗАКРЫТО частично — «двигают ВСЕ».** Добавлен `revisionBump(aggregate)` (`conflictSafeUpdate.client.ts`): атомарный `increment(1)` в обычной `updateDoc`-нагрузке, поэтому работает И офлайн (транзакция — нет). Незащищённые пути ЧЕТЫРЁХ клиентских сервисов (заметка · проповедь `core` · серия `meta` · группа `content`) теперь двигают счётчик, даже когда версия не заявлена ⇒ клиентский счётчик больше не лжёт. Закреплено тестом с мутацией.
- ✅ **ЗАКРЫТО — сервер тоже двигает счётчик.** `updateSermonData` принимает имя агрегата и добавляет `FieldValue.increment(1)` (`sermons.repository.ts:53`); план/конспект помечен как `plan` (`:160`), серверные записи мыслей — как `thoughts` (`thoughts/route.ts:57`, `:378`). Ожидания в трёх наборах тестов приведены к новому контракту.
- ✅ **ЗАКРЫТО — офлайн-доигрывание больше не срезает версию.** Persisted-дефолт заметки принимает и пробрасывает `expectedRevision` (`mutationDefaults.ts:424`), поэтому правка, доигранная после перезагрузки, проходит ту же проверку, что и живая.
- ✅ **ЗАКРЫТО — проигрывание СЕРИИ тоже несёт версию** (`mutationDefaults.ts`, ключ обновления серии): тот же класс, что у заметки, чинился как класс, а не как случай.
- ✅ **ЗАКРЫТО — insight-роуты помечают агрегат** (`insights` в пяти роутах), поэтому серверная запись инсайтов больше не оставляет счётчик застывшим.
- ✅ **ЗАКРЫТО частично — отказ на проповеди и серии больше НЕ безликий.** `SermonHeader` и `useSeriesDetail` ловят `StaleWriteError` и показывают отдельное сообщение («Не сохранено — запись изменили на другом устройстве…», ключ `freshness.staleSaveToast`, три локали) вместо общей ошибки сохранения. Текст остаётся в поле, так что ничего не теряется.
- ⏳ **ОСТАЁТСЯ:** полноценный ВЫБОР при конфликте («оставить моё / взять чужое» + повторная отправка) есть только у заметки — у проповеди и серии пока лишь внятное уведомление; молитвы, шаблоны плана и настройки в схему версий не входят; группа сознательно не подключена (см. FRESHNESS_PLAN §2и).
- **группа:** экспортируемый `updateGroup` (`groups.service.ts:254`) вообще не принимает версию → защита недостижима из боевого кода (осознанно, см. FRESHNESS_PLAN §2и);
- **создание заметки** безусловно удаляет черновик под ключом `new` после асинхронной записи (`studies/[id]/page.tsx:241`) → текст, набранный во время создания, теряется;
- **две вкладки делят один слот черновика**; **вытеснение по нехватке места** удаляет чужие неподтверждённые черновики.
**⏳ P1:** защищённая запись использует транзакцию, а транзакции офлайн НЕ работают (HEAD ставил `updateDoc` в очередь) → у заголовка/стиха и серии офлайн-правка теперь может пропасть при уходе со страницы — **хуже HEAD**; `refreshSeriesDetail` не бросает при неудачном refetch, поэтому плашка серии всё ещё может погаснуть после неуспеха; серия возвращает СТАРУЮ версию после успешного коммита.
**Направление.** Прежде чем расширять защиту: (а) провести версию через persisted-replay, (б) заставить ВСЕХ писателей агрегата инкрементить счётчик (иначе он лжёт), (в) вернуть офлайн-путь (не транзакция, а очередь + отложенная сверка), (г) ключ черновика на писателя/версию.


### Защита от затирания: живой отказ ДОКАЗАН на трёх сущностях; охват неполный — P2, ЧАСТИЧНО (2026-07-25)
**Работает.** `frontend/app/services/conflictSafeUpdate.client.ts` — отказ в устаревшей записи по счётчику `rev.<агрегат>`; миграция не нужна (отсутствие = 0). 8 тестов, 3 краснеют при снятии сравнения. Протянуто до редактора заметки; при отказе — выбор «оставить моё / взять чужое», причём «оставить моё» ОТПРАВЛЯЕТ заново, а не просто гасит плашку.
**Починено по ходу.** Версия бралась ТОЛЬКО из слушателя, поэтому сохранение до первого серверного снимка уходило незащищённым (`rev` оставался пустым — воспроизведено двумя вкладками). Теперь версия читается и из самой загруженной заметки (`StudyNote.rev`, `studies/[id]/page.tsx`). Проверено живьём: `rev` растёт `0 → 1 → 2`, то есть ОБЕ вкладки теперь пишут через защищённый путь.
**✅ ЖИВОЙ ОТКАЗ ПОЛУЧЕН 2026-07-25.** После того как версия сохранения перестала подтягиваться слушателем, опыт наконец воспроизвёлся: заметка на `rev.note=3`, «телефон» пишет `rev=4`, правка с ноутбука → **в базе осталось `"ТЕКСТ С ТЕЛЕФОНА"`, `rev=4`**, правка НЕ записана. На экране — «Ваша правка не сохранена» с кнопками «Оставить мой текст / Взять чужую версию», текст в поле цел, черновик в хранилище есть. Это тот самый исходный баг владельца, показанный уже НЕ разрушительным.
**⚠️ Воспроизводится нестабильно:** ещё две попытки после этого прошли БЕЗ отказа — вкладка успевала узнать свежую версию (перезапрос при возврате фокуса) до срабатывания отложенного сохранения. То есть окно конфликта в живом приложении узкое, и это хорошо; но повторяемого стенда у меня нет.
**✅ ПОЛНЫЙ ЦИКЛ ДОКАЗАН ЖИВЬЁМ (второй заход, `test-keepmine`).** Заметка на `rev.note=7` → «телефон» пишет заголовок и `rev=8` → правка с ноутбука ОТКЛОНЕНА (в базе цел заголовок телефона, `rev=8`), показан конфликт + плашка свежести, мой текст в поле → нажатие «Оставить мой текст» → **в базе мой заголовок, `rev=9`**. То есть: устаревшая запись не проходит, человек видит выбор, а осознанная перезапись выполняется. Ключ к воспроизведению: НЕ перезагружать вкладку между записью «телефона» и правкой — иначе страница узнаёт свежую версию и конфликта не возникает.
**✅ СЕРИЯ — живьём ПОДТВЕРЖДЕНА (`test-series-refusal`).** `rev.meta=5` → «телефон» пишет заголовок и `rev=6` → правка через модалку с ноутбука → **в базе цел заголовок телефона, `rev=6`**, устаревшая запись НЕ прошла. На экране — плашка «Изменено на другом устройстве · Эту серию отредактировали в другом месте». ⚠️ Оговорка: отдельного сообщения «Не сохранено…» в этот момент не наблюдалось — предупредила плашка свежести; то есть человек НЕ остаётся в неведении, но конкретно тост об отказе на серии не подтверждён.

**❌ ПРОПОВЕДЬ — вердикт «подтверждена» ОТОЗВАН (2026-07-25, тот же день).** Я записал `test-sermon-verse` как успех на двух уликах: стих телефона цел + `rev=5` не сдвинулся + на экране плашка. Улики НЕ различают «отказ сработал» и «отправки не было». Разбор кода показал, что различитель есть и он отрицательный: при отказе `SermonHeader.tsx:95` делает `return`, `EditableVerse` закрывает поле и откатывает текст, и обязательно всплывает тост `freshness.staleSaveToast`. В опыте **поле осталось открытым с моим текстом, тоста не было** ⇒ запись не отправлялась, предупредила плашка ОБНАРУЖЕНИЯ. Засчитывать нельзя. Урок: «в базе чужое цело» — прокси, а не якорь; якорь отказа = наблюдаемая реакция ОТКАЗА (тост/конфликт), а не отсутствие изменения.
**✅ ИТОГ РУЧНОЙ ВАЛИДАЦИИ: ТРИ сущности живьём.** (1) **Проповедь** — полный цикл на ФИНАЛЬНОМ коде (`test-fresh-sermon`, 2026-07-25): версия 4 → «телефон» пишет стих и версию 5 → правка стиха с ноутбука ОТКЛОНЕНА с панелью «Ваша правка не сохранена» + показом моего текста → «Оставить мой текст» → в базе мой стих, `rev.core=6`, на экране он же без перезагрузки. (2) **Заметка** — полный цикл (rev 8→9) ранее в той же сессии; путь `studies.service` с тех пор не менялся. (3) **Серия** — ПЕРЕПРОВЕРЕНА живьём на финальном коде (`test-fresh-series`): версия 4 → «телефон» пишет название и 5 → правка через модалку ОТКЛОНЕНА с панелью «Ваша правка не сохранена» и показом моего текста → «Оставить мой текст» → в базе `title="СЕРИЯ С НОУТБУКА"`, `rev.meta=6`, на экране оно же. Пробел закрыт.
⚠️ Ловушка постановки опыта: модалка серии не отправляла форму, пока пустое обязательное «Книга/Тема», а программный ввод в контролируемое поле не регистрировался — выглядело как «механизм не сработал». Ставить опыт настоящей клавиатурой и заполнять обязательные поля.
Попутно живьём подтверждено: обнаружение (плашка «изменено на другом устройстве»), «текст под руками не подменяется» (на экране оставался мой «Исходный стих», пока я не решил иначе), различимость плашек данные/программа по текстам.
⚠️ На серии предупреждает ПЛАШКА СВЕЖЕСТИ; отдельный тост «Не сохранено…» там не наблюдался — знание доходит, но не тем путём, который я добавлял.

### Отказ на проповеди МОЛЧА теряет набранный заголовок/стих — P0, хуже обещанного (2026-07-25)
**Был.** `SermonHeader.tsx:90-99` и `:112-121` при `StaleWriteError` показывают тост и делают `return` — то есть для `EditableTitle`/`EditableVerse` сохранение выглядит УСПЕШНЫМ (`onSave` резолвится). Дальше `EditableTitle.tsx:35-39` / `EditableVerse.tsx:34-38`: `setIsEditing(false)` → эффект `if (!isEditing) setEdited*(initial*)` откатывает поле к серверному значению.
**Эффект.** Набранный человеком заголовок или стих исчезает с экрана, черновика у этих полей нет, а тост при этом обещает «Ваш текст на месте» — прямое нарушение цели «НИКОГДА не теряет текст молча». Комментарий в коде («the typed text stays in the input») тоже неверен.
**Фикс (направление).** Отказ должен подниматься в состояние конфликта, которое ДЕРЖИТ отклонённое значение и предлагает выбор (оставить моё / взять чужое), как у заметки.

**(историческое) Проповедь — живьём НЕ подтверждена.** Попытка поставить тот же опыт (`rev.core` 2 → телефон пишет 3 → правка заголовка) сорвалась на механике редактируемого заголовка: поле закрывалось раньше, чем ввод доходил. В базе остался заголовок телефона, но отличить «отказ» от «отправки не было» я не могу ⇒ засчитывать нельзя. Проповедь покрыта только тестами (`SermonHeader` шлёт версию третьим аргументом, 25 тестов; сервис её пробрасывает, 3 теста).
**Рецепт для ручной проверки** (работает, проверен на заметке): создать документ с `rev`, открыть страницу, НЕ перезагружать, изменить документ снаружи с инкрементом `rev`, затем править в открытой вкладке.

**Историческая справка.** Кнопка «Оставить мой текст» после исправления (она берёт ТЕКУЩУЮ версию сервера, чтобы перезапись была намеренной, — раньше повторная отправка уходила со старой версией и отклонялась снова). Исправление типизировано и рассуждением обосновано, но живого подтверждения нет: повторно поставить отказ не удалось.
**Старое (до фикса) — ОТКАЗ не наступал ни разу.** Слушатель во второй вкладке успевает подтянуть правку первой, и она перестаёт быть устаревшей — окно гонки закрывается раньше, чем его удаётся создать вручную. Отказ доказан только тестами примитива.
**ЧЕТЫРЕ попытки поставить живой отказ — ВСЕ прошли без отказа, и это ВЫВОД, а не неудача.** (1) две вкладки, правка A → правка B: `rev` 0→1→2, обе записи защищены; (2) то же после переноса чтения версии в загрузку заметки: тот же результат; (3) заметка создана сразу с `rev=5`, сервер уведён вперёд до 6 «за спиной» вкладки, затем сохранение из неё: `rev` 6→7; (4) самый жёсткий: сервер уведён до 11 ДО перезагрузки вкладки, затем правка сразу после монтирования редактора — `rev` 11→12. Во всех трёх случаях слушатель успевал донести свежую версию за 1-3 секунды — БЫСТРЕЕ, чем человек успевает набрать и дождаться отложенного сохранения (1,5 с).
**Что это значит.** В живой вкладке с работающим слушателем отказ практически недостижим: приложение держит вкладку актуальной, и конфликта не возникает. Отказ — это сеть безопасности для случаев, когда слушателя НЕТ или он молчит: офлайн, усыплённая фоновая вкладка, сохранение до первого серверного снимка, отказ прав.
**Как доказать (направление).** Ставить опыт именно в этих условиях: вкладка без сети (DevTools → Offline) или с намеренно отключённым слушателем; либо тест уровня страницы с подменённым слушателем, проверяющий, что сохранение уходит с непустой версией И что при расхождении показывается выбор.
**ОХВАТ ЗАКРЫТ 2026-07-25 (вторая половина сессии).** Счётчик двигают ВСЕ писатели своих агрегатов; версию ЗАЯВЛЯЮТ (отказ включён) заметка, проповедь, серия, ГРУППА, МОЛИТВА и ШАБЛОН ПЛАНА. Общая панель выбора — `frontend/app/components/SaveConflictBanner.tsx` (одна на всё приложение, не копипаста): заметка, проповедь, серия, группа, молитва. Шаблон плана предупреждает тостом (его правка остаётся на экране в `draftStructure`).
**Настройки — исключены ОСОЗНАННО, по устройству, а не по недоделке.** Каждый писатель в `frontend/app/services/userSettings.service.ts` (строки 129, 163, 180, 194, 208, 247, 306, 352, 367, 382, 420) отправляет ТОЛЬКО изменённое поле, а запись идёт `setDoc(..., {merge:true})` (`:54`) — то есть по полю. Два устройства, меняющие РАЗНЫЕ настройки, не пересекаются по построению; одна и та же настройка — это переключатель, значение которого человек видит и может вернуть, скрытого текста там нет. Защита добавила бы только ложные конфликты на тумблере.
**(старое) Осталось по слою (уточнено после валидации).** Счётчик двигают ВСЕ писатели агрегата — клиентские сервисы (заметка/проповедь/серия/группа), Admin-репозиторий, роуты мыслей и инсайтов, persisted-replay. Версию ЗАЯВЛЯЮТ (то есть отказ включён) заметка, проповедь и серия. Полный интерфейс выбора — только у заметки; проповедь и серия предупреждают плашкой. Группа сознательно не заявляет версию: запись fire-and-forget проглотила бы отказ (сперва канал, потом защита). Молитвы, шаблоны плана и настройки в схему не входят вовсе.



### B9 · TOCTOU перебора usage-лимита при конкурентном burst — P1
**Суть.** Проверка лимита и его инкремент — РАЗНЫЕ шаги. `assertAiUsageAvailable` читает-проверяет ДО провайдер-вызова, `consumeUsage` инкрементит ПОСЛЕ отдельной транзакцией, БЕЗ ре-проверки (`usageLimits.server.ts:105`, `:120` слепой `current+amount`). Два одновременных запроса на 99/100 оба проходят check → оба исполняются → usage уходит за квоту.
**Масштаб — СИСТЕМНЫЙ, 5 потребителей одного примитива:** `structuredOutput.ts:280`, `audio/generate:380`, `studies/transcribe:145`, `thoughts/route:233`, `thoughts/transcribe:144`.
**РЕАЛЬНЫЙ ИМПАКТ (оценка 2026-07-14).** Бьёт только при ИСТИННОЙ одновременности (окно check→increment = мс). Последовательные вызовы считаются корректно (каждый перепроверяет). Обычный UI-юзер: перерасход ~0; две вкладки/двойной клик → 1-2; целенаправленный параллельный burst → больше, но каждый жжёт реальный вызов провайдера и **самоограничивается** (после лимита всё блокируется). Малая база + щедрые квоты (100–5000) → **P1 по классу, но НЕ срочно**; чинить при работе над примитивом, не пожар.
**РЕФРЕЙМ (владелец 2026-07-14): грация, не жёсткий блок → фича.** Владелец НЕ хочет атомарного блока ровно на лимите. Пример: транскрибация = 2 списания (STT+полировка) → одно действие на 99 даёт 101, и это ОК «по благодати». Нужно: (1) действие ДОигрывается (не падает на полпути), (2) мягкий блок НОВЫХ действий за лимитом, (3) hard-cap на бо́льшем пороге (напр. limit+10%), (4) в настройках/админке при перерасходе — тёплое сообщение + СТИХ про благодать. ⇒ B9 из «гонки на устранение» → в **grace-band фичу** (`FEATURE_CONCEPTS.md` F7). Атомарность перестаёт быть критичной (малый overshoot — by design); остаётся hard-cap + сообщение.
**Фикс (направление, из Popper-петли):**
- **Дискретный `aiUsed` (+1, известен заранее):** atomic **reserve+refund ДО провайдера** (проверка+инкремент в одной `runTransaction`; бросок → 429 ДО стрима; refund при провале провайдера). ⚠️ простой rethrow НЕ проходит: `callWithStructuredOutput` (17 вызовов через 8 wrappers) глотает исключение в `{success:false}`/`[]`/500 — надо явно мапить `UsageExhaustedError`→429 на границе роута.
- **Seconds-метры (transcription, audio — сумма известна ПОСЛЕ):** assert(оценка) ДО + consume-actual ПОСЛЕ, БЕЗ throw (иначе после успешной генерации 500 → повторная оплата провайдера). Минимальный overshoot при burst допустим (щедрые квоты). Reserve-before НЕ рекомендован (плохо стыкуется с audio per-batch, риск refund-on-crash).

### RMW-класс · lost-update read-modify-write вне транзакции — P1 → **ONLINE-гонка закрыта 2026-07-24; офлайн-replay, same-item merge и серверный путь ОТКРЫТЫ**
> **Статус 2026-07-24 (формулировка исправлена после независимого ревью — прошлая версия «клиентская часть ЗАКРЫТА» была ПЕРЕЗАЯВКОЙ).**
> ✅ **Что реально закрыто:** **7** клиентских сайтов переведены на `runTransaction` через утилиту `app/services/atomicUpdate.client.ts` → пока оба устройства онлайн, конкурентная правка больше не затирается (транзакция = compare-and-set; ручной `revision` не нужен). Покрыто ТОЛЬКО то, что безопасно повторить: **добавления** (thought create — теперь insert-if-absent · preachDate add по client-id · prayer update по updateId · group meetingDate add по mintedId) и **удаления** (thought · preachDate · group meetingDate). ⛔ **Три update-пути (`updateThoughtViaClient`, `updatePreachDateViaClient`, `updateGroupMeetingDateViaClient`) СОЗНАТЕЛЬНО оставлены НЕ транзакционными** (как в HEAD): они мержат ПОЛНЫЙ объект от вызывающего, а Firestore внутри повторяет callback транзакции (до 5 попыток) даже когда коммит УЖЕ прошёл, а ответ потерялся — повтор наложил бы устаревший payload на более новую правку, т.е. транзакция ДОБАВИЛА бы порчу, которой в HEAD нет. Их безопасность требует приёма минимального field-patch вместо целого объекта (см. ОТКРЫТО #2). Скалярные патчи (`updatePrayerRequest`, `setPrayerStatus`, `series.service`, `studies.service` updateNote) не трогали — field-disjoint. ⚠️ **НЕ покрыты и остаются тем же RMW-классом:** scratch-массив (`sermons.client.ts` add/update/delete/applyToOutline) — вызывающий собирает весь `scratch[]`, пишется обычным `updateDoc`.
> ⚠️ **Деградационный fallback (НЕ «полная durability»):** транзакция требует сервер и НЕ попадает в нативную очередь Firestore, а `navigator.onLine` врёт (captive portal / flaky wifi) → голая транзакция теряла бы правку там, где старый `updateDoc` доезжал из очереди. Теперь на ПЕРЕХОДЯЩЕЙ ошибке (`unavailable`/`deadline-exceeded`) — fallback на queued-путь. ⚠️ Это **условный** best-effort, НЕ гарантия: fallback сначала делает `getDoc`, а он сам может упасть, если сервер недостижим И документа нет в локальном кэше → тогда в очередь не попадает ничего и правка всё равно теряется. LWW-риск тоже сохраняется; `internal` НЕ проглатывается (сломанный инвариант должен всплыть), как и `permission-denied`/`aborted`/прикладные ошибки. ⚠️ **Окно потери НЕ закрыто:** SDK ретраит транзакцию до 5 раз (RPC timeout 15с), поэтому при плохой сети правка живёт в памяти десятки секунд — убитая в это время вкладка теряет её. Полное закрытие требует durable-outbox (ОТКРЫТО #1). Также `deadline-exceeded` может прийти ПОСЛЕ успешного коммита → retry обязан быть идемпотентным (7 транзакционных сайтов — insert-if-absent и filter-по-id, пересчитываемые на СВЕЖИХ данных; `groups.meetingDates` add исправлен искать по `mintedId`, а не только по внешнему `data.id`).
> ❌ **Изоляция аккаунтов — ПОПЫТКА ОТКАЧЕНА 2026-07-25 (остаётся ОТКРЫТОЙ, см. отдельную запись ниже).** Пробовал чистить React-Query-кэш + IDB-persister при смене uid. Независимое ревью показало три P0, из которых решающий: **при выходе из аккаунта это УДАЛЯЛО persisted paused-мутации → безвозвратная потеря несинхронизированных офлайн-правок** (включая «вышел и зашёл тем же аккаунтом»). Лечил утечку ЧТЕНИЯ — внёс потерю ЗАПИСИ, то есть хуже исходной болезни. Плюс подход не решал главного: **Firestore держит СВОЙ отдельный `persistentLocalCache`**, которого `queryClient.clear()` не касается вовсе, и `PersistQueryClientProvider` начинает восстановление ДО того, как известен владелец (late-restore может гидратировать данные A в сессию B). Правильное решение — не «чистить общий клиент», а **разделять хранилище по uid** (партиционированный ключ persister + отдельный QueryClient на владельца + owner/auth-epoch в конверте + гейт восстановления до резолва auth) и отдельно решать Firestore-кэш (memory-cache по умолчанию либо координированный multi-tab terminate/clear с сохранением pending-записей).
> ❌ **ОТВЕРГНУТО в ходе ревью:** переводить записи на нетранзакционный путь при `visibilityState === 'hidden'`. `hidden` — это ещё и фоновая вкладка/заблокированный экран, поэтому это тихо возвращало lost-update-гонку любому, кто просто переключил вкладку. Подтверждено замером: у фоновой вкладки `visibilityState === 'hidden'`.
> ⏳ **ОТКРЫТО #0 — «воскрешение» при повторе ADD (найдено ревью, УНАСЛЕДОВАНО от HEAD, не регрессия):** ADD закоммитился → ответ потерян → другое устройство УДАЛИЛО элемент → повтор (внутренний SDK-ретрай или наш queued-fallback) видит свежий снимок без этого id и вставляет элемент снова. `insert-if-absent` не различает «никогда не добавляли» и «добавили, потом удалили». **Сравнение с HEAD (якорь `git show HEAD:...sermons.client.ts`):** HEAD делал `getDoc` → `updateDoc(весь массив)`, и его повтор ТОЖЕ воскрешал удалённое, **плюс затирал добавленное другим устройством** (перезапись всего массива устаревшим). Наш вариант этот класс СУЖАЕТ (бьёт только по своему элементу), но не закрывает. Полное закрытие = tombstone/ledger применённых операций (различать «удалено» от «не было») — та же инфраструктура, что нужна durable-outbox.
> ⏳ **ОТКРЫТО #1 — офлайн-replay (тот же класс, P0 по данным):** правка, сделанная ОФЛАЙН, кладётся в очередь как **устаревший полный массив**; при реконнекте она приходит последней и затирает то, что за это время закоммитило другое устройство. Транзакция защищает только от записей ДО своего коммита. Лечится durable-outbox с ID-операциями (интент до оптимистичного UI) + rebase на реконнекте, либо нормализацией горячих массивов в отдельные документы. См. `FIRESTORE_SYNC_RESEARCH.md` §6, §11a.
> ⏳ **ОТКРЫТО #2 — same-item merge:** `updateThoughtViaClient` принимает ПОЛНЫЙ stale `Thought` от вызывающего (`useSermonActions.ts:263-273`), поэтому при правке РАЗНЫХ полей одной мысли на двух устройствах stale-поля перетирают чужие (напр. `isLocked`). Лечится приёмом минимального field-patch вместо целого объекта.
> ⏳ **ОТКРЫТО #3 — гонка двух быстрых сохранений:** debounce 500ms коалесцирует только ДО запуска; две сохранялки в полёте могут закоммититься в обратном порядке (старый черновик победит). Нужна сериализация per-`{sermonId,thoughtId}` + отбрасывание устаревших операций.
> ⏳ **ОТКРЫТО #4 — серверный путь (Admin SDK):** сайты ниже в `*.repository.ts` / `api/**/route.ts` + cross-doc. **Важно:** прод-путь preachDates идёт через `preachDates.service.ts` → `/api/sermons/:id/preach-dates` → Admin `sermons.repository.ts:274,320,363` (stale RMW), поэтому три клиентские конверсии preachDates **в проде не вызываются** — оставлены как корректная реализация на случай перевода UI на клиентский путь. Плюс `firestore.client.ts:112` из списка ниже — **файла больше нет** (запись устарела). `seriesMembership.client.ts` — `writeBatch` (атомарен по записи, но без read-isolation) — остаётся known-risk.
> Проверки: 453 suites / 3855+ tests зелёные, tsc 0, eslint 0; регрессионные тесты на суть **проверены на способность краснеть** (сломанная транзакционность → падают ровно они). Живьём на localhost: add 16→17 и delete 17→16 персистентны после reload, 0 ошибок консоли, Security Rules чтению внутри транзакции не мешают.
**Суть.** Код читает документ, фильтрует/меняет ВЕСЬ массив в памяти из stale-read и `writeBatch`/`update` перезаписывает его целиком. `writeBatch` ≠ read-isolation → при конкурентном same-owner редактировании между read и commit правка теряется (или остаётся dangling-ссылка). Это НЕ точечный баг — это паттерн по всему коду.
**Сайты:**
- `series.repository.ts:112` (sermon-DELETE, detach серий) · `series.repository.ts:139` (удаление группы, live из `groups/[id]/route.ts:33`)
- `series/[id]/route.ts:41` + `:61-67` (detach при concurrent move очищает свежий `seriesId`)
- `studies.repository.ts:30` (`deleteNote`) · `studies.repository.ts:120` (`updateMaterial`, cross-doc RMW)
- `firestore.client.ts:112` / tag-cascade → перезапись целых stale `thoughts`
- `sermons.repository.ts:320` → перезапись целого `preachDates`
- `audio/chunks/[index]/route.ts:38` → перезапись целого `audioChunks`
**РЕАЛЬНЫЙ ИМПАКТ (оценка 2026-07-14).** Реальная гонка, но НИЗКАЯ вероятность (нужны 2 write на один док в окне ~<1с от одного владельца). Самый реалистичный триггер — **офлайн-буфер записи доигрывается при reconnect, пока юзер правит вживую**; реже — две вкладки/двойной клик. Вред — тихая потеря правки / dangling. **НЕ делать big-bang по всем 9.** Cherry-pick «дёшево + горячий путь»: ✅ `deleteNote`, series detach (`arrayRemove` почти drop-in) · ⏸️ оставить known-risk: `updateMaterial` cross-doc, tag-cascade, preachDates (редко + дорого, нужна общая транзакция).
**СЛОЖНОСТЬ ФИКСА (оценка 2026-07-14).** Одиночный-док RMW → `runTransaction`/`arrayRemove`: **низко, ~30-60 мин/сайт** (фидлится только реалистичный concurrency-тест). Cross-doc (`deleteNote`+`updateMaterial` в одной транзакции через 2 коллекции): **средне-высоко, ~1-2 дня**. Итог: 2-3 дешёвых на офлайн-пути ≈ **полдня**; весь класс ≈ **2-3 дня**.
**Фикс.** `runTransaction` (read-modify-write внутри) ИЛИ `FieldValue.arrayUnion/arrayRemove` где достаточно операции над элементом (шаблон уже в репо: `studies.repository.ts:88,98`). ⚠️ `deleteNote`+`updateMaterial` — arrayRemove НЕ закрывает race (concurrent add после query → dangling), нужна ОБЩАЯ transactional isolation обоих путей.

### Audio-метринг · неверный учёт озвучки — P1/P2 (фикс = full-stack фича)
**B6 перекос списаний по провайдеру (P1-по-импакту — бьёт на КАЖДОМ OpenAI-экспорте).** Чанки режутся одинаково, но число HTTP-запросов разное: Google = **1 запрос** (сервер регруппирует, `StepByStepWizard.tsx:524`), OpenAI = `ceil(чанки / 3)` запросов (`GENERATION_BATCH_SIZE=3`, `:535`). А `consumeAiUsage` = **+1 за КАЖДЫЙ запрос** (`route:380`). Итог: 15 чанков → OpenAI **5 списаний**, Google **1**. Это главный реальный кусок.
**B7 провал чанка (P1 по классу, но РЕДКИЙ).** Retry в аудио-пути НЕТ (`generateChunkAudio` кидает при провале; `Promise.all` fail-fast, `route:377`; retry есть только у МЫСЛЕЙ `thought.structured.ts`, не тут). Упал чанк → батч падает → экспорт падает, файла нет, а ранние батчи уже списаны → заплатил за неполученное. Частота низкая (ошибка провайдера / чанк >4096).
**Batch-snapshot (near-theoretical, НЕ чиним пока не увидим).** `route:205` перечитывает `audioChunks`, браузер шлёт offset из раннего снапшота (`StepByStepWizard.tsx:503`) → правка текста РОВНО во время секундного авто-цикла батчей. Практически невозможно (между батчами нет действий юзера). Оценка 2026-07-14: переоценено, понижено.

### Audio-export UI · Free видит одну модель + «качество» вводит в заблуждение — P2 (UX, скрин 2026-07-14)
**(a) Free показывает только OpenAI/gpt-4o-mini-tts.** Каталог TTS = 3 модели (`functionCatalog.ts:31-33`: Google gemini-3.1-flash-tts [default], Google gemini-2.5-flash-tts, OpenAI gpt-4o-mini-tts), но wizard сидит из entitlement (`StepByStepWizard.tsx:225-236`) и пиннит free к единственной дозволенной; роут гейтит по tier (403 «not allowed for this plan», `route.ts:244`). Владелец: free должен хотя бы ВИДЕТЬ другие провайдеры/модели (upsell), а не одну. → часть **F1** (per-tier выбор моделей) в `FEATURE_CONCEPTS.md`.
**(b) «Стандартное / Высокое качество» у gpt-4o-mini-tts — мёртвый toggle + неверно.** `quality` (`StepByStepWizard.tsx:186,741`) на OpenAI-пути НЕ меняет модель: wizard хардкодит `model: OPENAI_TTS_MODEL='gpt-4o-mini-tts'` (`:515`), роут берёт `selectedModel = requestedTarget.modelId`, игнорируя `quality` (`route.ts:258-261`). Легаси `getTTSModel(standard→gpt-4o-mini-tts, hd→tts-1)` не используется, и tts-1 — СТАРЕЕ, не «выше качеством». Концептуально: качество даёт РАЗНАЯ МОДЕЛЬ, не тумблер у одной. Фикс: убрать toggle ИЛИ связать с реальным выбором модели (в рамках F1).
**РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-07-14).** Считать по ДЛИТЕЛЬНОСТИ реально сгенерированного аудио (секунды), суммарно по чанкам, **только успешные**. Растворяет B6 (additive-by-work) + B7 (частичный провал честен) by construction.
**Фикс = full-stack фича:** новое измерение `audioSecondsPerPeriod` в `tierPolicy.ts` + `audioSecondsUsed` в usage; длительность через `music-metadata` (уже установлен, `audioServerUtils.ts:7`); worker-pool с per-chunk status (не blanket `allSettled`, не отдавать усечённый batch как готовый); `Promise.allSettled`; +8 поверхностей entitlement (нормализация, `/api/me/entitlement`, `useUserEntitlement`, usage-виджет, admin-схема, клиентский гейтинг). **Owner-решения при старте:** значения тарифов, политика частичного провала экспорта, приемлемость текст-оценки как fallback.
**СТАТУС (2026-07-14, автономная audio-фича, деплой):** ~~B6 двойной счёт~~ **ИСПРАВЛЕН** (`consumeAiUsage` 1/экспорт; бонус — закрыт пред-существующий OpenAI-списание-на-каждый-батч). **B7** — метринг чинён (allSettled + сумма успешных секунд через `audioDurationMetering.server.ts` fail-closed); остаётся отложенным «тихая потеря чанка OpenAI» (продуктовое решение → `FEATURE_CONCEPTS.md` F8). audioSecondsUsed метрится, но НЕ энфорсится (билинг отложен; интерим-кэп = 1 aiCall/экспорт). Google-1750-путь за флагом `GOOGLE_SMALL_CHUNKING` (live-слух не сделан). batch-snapshot — near-theoretical, не трогаем.

### Durable draft сам удаляет неподтверждённый текст и не изолирует вкладки — P0 (review 2026-07-25)
**Был/Эффект.** `useDurableDraft` читает recovered draft, но через 250 мс запускает `persist` с текущим серверным значением; совпадение с `confirmedRef` вызывает БЕЗУСЛОВНЫЙ `clearDraft` (`frontend/app/hooks/useDurableDraft.ts:99-109,132-136`). Поэтому просто открыть плашку и не нажать кнопку уже удаляет единственную долговечную копию; в Strict Mode cleanup пустого эффекта вызывает тот же `flush()` немедленно (`:138-160`). Дополнительно обе вкладки используют один ключ: последняя `saveDraft` перезаписывает черновик другой вкладки (`frontend/app/utils/durableDraft.ts:72-80`), а неизменённая вкладка может удалить чужой черновик своим безусловным `clearDraft`.
**Ещё два P0-пути.** `clearDraftIfMatches` делает отдельные read→remove (`durableDraft.ts:126-130`), поэтому другая вкладка может записать между ними и быть удалена; `markSaved` применяет завершившийся async-save к ТЕКУЩЕМУ `keyRef`, а не к ключу, для которого save стартовал (`useDurableDraft.ts:167-171`) — после перехода A→B ответ A может очистить/переназначить baseline B.
**Фикс (направление).** Не один mutable slot на aggregate, а versioned/per-writer entries; recovered draft остаётся durable до server-ack или явного Discard; save/ack несут захваченные `{key,value,operationId}`; ни один unchanged flush не удаляет общий ключ.

### Durable draft при quota/storage error удаляет другие черновики и молчит о потере текущего — P0 (review 2026-07-25)
**Был/Эффект.** Любой `setItem` exception запускает eviction половины ДРУГИХ draft keys (`frontend/app/utils/durableDraft.ts:78-86,173-189`). Даже успешный retry достигается ценой бесследного удаления другого неподтверждённого текста; если retry тоже падает, потеряны и прежние drafts, и текущий, а UI ничего не узнаёт (`:84-86`). Это прямо нарушает «draft существует ⟺ есть неподтверждённый текст».
**Фикс (направление).** Никогда автоматически не удалять unsaved drafts ради другого unsaved draft; вернуть caller-у durable-write status и блокирующий/явный UI-сигнал, если safety copy не записалась.

### Новая заметка очищает draft до server acknowledgement — P0 (review 2026-07-25)
**Был/Эффект.** `useStudyNotes.createNote` вызывает `createNoteMutation.mutate()` и немедленно возвращает optimistic note (`frontend/app/hooks/useStudyNotes.ts:80-98`). `useNoteAutoSave` трактует этот resolved wrapper как подтверждение: вызывает `onSaved`, безусловно удаляет draft ключа `new` и переключает id (`frontend/app/(pages)/(private)/studies/[id]/page.tsx:232-246`). Если Firestore позже отвергнет create, server никогда не подтверждал текст, но долговечной копии уже нет.
**Фикс (направление).** Разделить queued/optimistic и acknowledged; переносить draft `new`→real-id атомарно, а удалять только после backend acknowledgement.

### Group surgical baseline продвигается после fire-and-forget, поэтому rejected поле перестаёт пересылаться — P0, хуже HEAD (review 2026-07-25)
**Был/Эффект.** `performSave` после `await updateGroupDetailRef.current(changed)` сразу записывает новые значения в `baselineRef` (`frontend/app/(pages)/(private)/groups/[id]/page.tsx:262-273`). Но `updateGroupDetail` запускает `updateGroup(...).then().catch()` через `void` и возвращается ДО backend result (`frontend/app/hooks/useGroupDetail.ts:105-118`). Сценарий: title A→B → write rejected → baseline уже B → следующий edit description видит title B как «сохранённый» и больше его не шлёт. HEAD на каждом autosave слал все поля и в этом сценарии переслал бы B снова.
**Фикс (направление).** Продвигать confirmed baseline только в backend-success callback; queued/pending держать отдельно. Rejected patch остаётся dirty и обязан повторяться.

### Nested preparation patch не умеет удалить step — P1, хуже HEAD (review 2026-07-25)
**Был/Эффект.** Для changed key со значением `undefined` код кладёт `preparation.<step>: undefined`, затем `deepCleanUndefined` удаляет САМУ операцию (`frontend/app/services/sermons.client.ts:221-228`). На сервер уходит только `updatedAt`, функция возвращает локальный объект без step, и UI выглядит сохранённым, но после reload удалённый текст возвращается. HEAD заменял весь `preparation` очищенным объектом и такое удаление сохранял.
**Фикс (направление).** Для явного удаления посылать `deleteField()` по nested path; отличать «ключ отсутствует в patch» от «ключ присутствует и должен быть удалён».

### Freshness «Посмотреть изменения» уничтожает dirty-текст заметки — P0, хуже HEAD (adversarial review 2026-07-25)
**Был/Эффект.** При `dirty=true` общий banner меняет только подпись кнопки на «Посмотреть изменения» (`frontend/app/components/DataFreshnessBanner.tsx:45-50,64-71`), но вызывает тот же `onRefresh`. На заметке handler без сравнения заменяет `title/content/tags/scriptureRefs/type` remote-значением (`frontend/app/(pages)/(private)/studies/[id]/page.tsx:807-816`). Через 250 мс `useDurableDraft` видит remote как новое неподтверждённое значение и записывает его поверх localStorage-слота с прежним dirty-текстом (`frontend/app/hooks/useDurableDraft.ts:122-135,158-162`). Итог: действие, названное review, сначала убирает свой текст с экрана, затем уничтожает и его durable-копию. В HEAD этой кнопки не было.
**Фикс (направление).** При dirty никогда не применять remote: отдельный compare/merge UI с копированием обеих версий; durable entry текущей локальной версии остаётся неизменным до явного выбора и server acknowledgement.

### Freshness reload уничтожает незаписанный текст sermon/group/prayer — P0, хуже HEAD (adversarial review 2026-07-25)
**Был/Эффект.** Sermon, group и prayer вызывают `window.location.reload()` из banner (`sermons/[id]/page.tsx:1421-1425`, `groups/[id]/page.tsx:557-560`, `prayers/[id]/page.tsx:214-217`). При этом sermon и prayer безусловно передают `dirty={false}` (`sermons:1417-1420`, `prayers:209-213`), хотя на странице есть незаписанное React-state: `prepDraft`/exegetical plan (`sermons:310,1243-1252`) и тексты трёх prayer-модалок (`CreatePrayerModal.tsx:21-23`, `AddUpdateModal.tsx:23`, `MarkAnsweredModal.tsx:16`). Group dirty-check не включает meeting date/location/audience (`groups:544-555` против state `:104-118`). Reload уничтожает эти значения без durable draft и без подтверждения; HEAD не имел такого действия.
**Фикс (направление).** Удалить hard reload из data-freshness action. Сначала иметь полный dirty registry + durable drafts всех редакторов, затем compare/merge; до этого action только объясняет конфликт и даёт безопасно скопировать локальный текст.

### Recovered «Отклонить» удаляет более новый draft, набранный после открытия — P0 (adversarial review 2026-07-25)
**Был/Эффект.** Recovered offer остаётся на экране, пока пользователь может продолжать печатать. Новый текст через 250 мс перезаписывает тот же storage key (`useDurableDraft.ts:158-162`), но `discardRecovered()` удаляет не захваченную recovered-версию, а безусловно текущее содержимое key (`:198-202`). Сценарий: открыт draft R → введён новый N → debounce сохранил N → нажато «Отклонить» для R → удалён N. При crash/navigation до следующего flush unconfirmed N остаётся без durable-копии.
**Фикс (направление).** Offer несёт immutable `{key, versionId, value}`; discard удаляет только эту версию compare-and-delete, а текущая редактируемая версия хранится отдельной записью.

### Freshness не блокирует same-field autosave — исходная потеря текста остаётся — P0 (adversarial review 2026-07-25)
**Был/Эффект.** Banner только отображается; note autosave продолжает через 1,5 с вызывать `updateNote` (`studies/[id]/page.tsx:250-279`) даже при `freshness.state === 'stale'`, а Firestore update не проверяет revision (`services/studies.service.ts:143-157`). Две вкладки меняют одно поле: поздний stale `content` перезаписывает более новый `content`; banner может быть виден, но запись не остановлена. Surgical fields спасают только разные поля, не одно поле.
**Фикс (направление).** Revision/CAS на каждом writer этого aggregate; stale write отвергается, local durable version сохраняется и показывается compare/merge. До CAS как минимум ставить autosave на паузу при stale/unknown, не выдавая «saved».

### Freshness сравнивает server с живым cache, а не с тем, что реально показывает editor — P1 (adversarial review 2026-07-25)
**Был/Эффект.** Все страницы строят `known` из live React Query entity (`studies:767-786`, `sermons:273-290`, `series:98-119`, `groups:248-267`, `prayers:47-65`), а hook переписывает `knownRef` на каждом render (`useDocumentFreshness.ts:68-74`). При mount/refetch cache может получить server v2, пока локальный editor, инициализированный один раз v1, остаётся v1; затем snapshot v2 сравнивается с уже обновлённым `knownRef=v2` и объявляется fresh (`:108-115`). Это тот же запрещённый live-cache baseline, который surgical diff уже исправляет отдельно.
**Фикс (направление).** Передавать immutable opened/last-explicitly-applied baseline редактора, keyed by `{uid,docId}`, а не query entity.

### Freshness selectors пропускают большинство изменений документа — P1 (adversarial review 2026-07-25)
**Был/Эффект.** Sermon наблюдает только title/verse/число thoughts (`sermons/[id]/page.tsx:272-294`), series — несколько scalar + число items (`series/[id]/page.tsx:90-126`), group — scalar + числа templates/flow (`groups/[id]/page.tsx:241-273`), prayer — scalar + число updates (`prayers/[id]/page.tsx:46-70`). Изменение текста thought/template/flow/update при том же количестве, а также sermon preparation/outline/plan, group meetingDates, prayer tags/answerText и series item order дают тот же selected JSON: hook ставит `fresh`, хотя экран stale.
**Фикс (направление).** Сравнивать монотонную document revision/updatedAt, увеличиваемую всеми writers, либо полный canonical hash всех отображаемых aggregates; счётчик элементов не является freshness token.

### `unknown` визуально выдаётся за fresh, а cache snapshot сохраняет старый `fresh` — P1 (adversarial review 2026-07-25)
**Был/Эффект.** При cache-only snapshot hook оставляет прежний `fresh` неизменным (`useDocumentFreshness.ts:95-99`); при listener error ставит `unknown` (`:121-127`). Но все пять страниц рендерят banner только для `state === 'stale'` (`studies:922`, `sermons:1416`, `series:309`, `groups:541`, `prayers:209`). Поэтому offline, permission-denied и terminal listener error внешне выглядят ровно как подтверждённая свежесть; после уже полученного fresh cache-only emission даже внутреннее состояние остаётся fresh.
**Фикс (направление).** Любой `fromCache` переводит в visible `unknown`; unknown имеет отдельный persistent UI («свежесть не подтверждена») и возраст последнего server proof. Terminal error требует явного retry/re-subscribe.

### Own write может поднять banner; `markSynced` может скрыть чужую stale-версию — P1 (adversarial review 2026-07-25)
**Был/Эффект.** Hook игнорирует pending snapshot, но server-ack приходит с `hasPendingWrites=false` и сравнивается со старым `knownRef` (`useDocumentFreshness.ts:93-120`). Ни один save path не вызывает freshness `markSynced`; он используется только в refresh handlers. Если ack snapshot опередил React Query/onUpdate, собственная запись становится `stale` и banner залипает. Обратная ошибка: `markSynced(value)` переводит любой текущий stale в fresh независимо от того, совпал ли `value` с `remote`, и не обновляет `knownRef` (`:136-142`), поэтому может скрыть другую remote-версию или немедленно снова поднять ту же.
**Фикс (направление).** Own write несёт operation/write id и acknowledged revision; suppress только точное совпадение. `markSynced` меняет baseline лишь при exact remote match и атомарно обновляет known state.

### Logout/account switch не гарантирует detach freshness listeners — P1 (adversarial review 2026-07-25)
**Был/Эффект.** Hook корректно зависит от переданного `uid` (`useDocumentFreshness.ts:76-85,130-134`), но sermon/series/group/prayer передают owner из cached document (`sermons:284-288`, `series:113-117`, `groups:261-265`, `prayers:59-63`), а не текущий auth uid. При logout/account switch cached entity может остаться прежним, prop не меняется, effect не cleanup'ится; Security Rules позже могут завершить listener, но тогда UI лишь скрыто переходит в unknown. Дополнительно detail query keys sermon/group/series не содержат uid (`useSermon.ts:58-68`, `useGroupDetail.ts:41-52`, `useSeriesDetail.ts:82-94`), поэтому старый owner object достижим в новой auth-сессии.
**Фикс (направление).** Передавать current auth uid, требовать `entity.userId === auth.uid`, key всех detail caches/listeners по `{uid,docId,authEpoch}` и detach до показа данных при auth transition.

### Confirmed draft может остаться и позже предлагаться как «несохранённый» — P1 (adversarial review 2026-07-25)
**Был/Эффект.** Если server commit успел, а вкладка упала до `markSaved`, при следующем открытии stored draft равен server value. Страница скрывает offer как identical (`studies/[id]/page.tsx:757-763`), но hook не удаляет recovered entry: `ourDraftRef` у нового instance равен null, поэтому confirmed-equal `persist` ничего не очищает (`useDurableDraft.ts:122-132,138`). После более поздней remote-правки этот старый уже подтверждённый текст снова будет предложен как unsaved.
**Фикс (направление).** После server-authoritative snapshot exact-equal stored version можно пометить acknowledged/retire; хранить version metadata, а не угадывать происхождение по одному mutable value.

### Соседи по классу «удаление с одного клика» — P2, ОТКРЫТО (прочёсано 2026-07-26 при фиксе набросков)
Владелец репортнул потерю НАБРОСКА; по правилу «репортнутый дефект = образец, не граница scope» прочёсаны соседние деструктивные действия. Два выживших:
1. **Крестик на заметке-напоминании пункта плана** (`PointNote.tsx:127-138`, `planEditor.note.delete`) — один клик стирает ВЕСЬ текст напоминания, без подтверждения и без undo-тоста; в комментарии компонента написано «undo-able upstream», но апстрим — это только save/cancel редактора плана, при автосохранении текст уходит совсем. Тот же класс, что и починенный набросок: один клик уничтожает набранный человеком текст.
2. **Удаление подпункта плана без прикреплённых мыслей** (`OutlineBoard.tsx:386-394`) — подтверждение спрашивается ТОЛЬКО когда к подпункту привязаны мысли (`count > 0`); пустой подпункт исчезает с одного клика вместе со своим текстом.
**Почему не починил заодно.** Оба живут в редакторе плана — другой поверхности со своим устоявшимся паттерном (hover-крестик, «подтверждаем только когда отвяжутся мысли»). Расширять гейт туда — заметное изменение ритма правки плана, а не молчаливый багфикс; по контракту «диверг флагни, не молча» решение за владельцем.
**Фикс (направление).** Тот же общий `ConfirmModal` + превью текста; для подпункта — снять условие `count > 0`, для `PointNote` — гейт только когда текст непустой.

---

## 🟡 Открыто, но не «код-фикс»

### referral Sybil — фейк-аккаунты стакают промо — P1 (PLAUSIBLE) → фича F6
**Механика.** Награда = +30 дней tier1 за КАЖДОГО зарегавшегося+заклеймившего, кумулятивно (`referral.server.ts`, `computeReferralPromotion:25-29`). Защищает: email_verified + аккаунт <24ч + клейм один раз + не self-referral. Sybil стоит: N верифиц. email + N аккаунтов за 24ч.
**Решение владельца:** не авто-блок, а human-in-the-loop → **фича F6 в `FEATURE_CONCEPTS.md`** (на 3-м реферале warning-флаг + письмо владельцу + контрол «отключить промо» в админке). Здесь — как открытая security-заметка; сам фикс живёт в F6.

### purge legacy share-links — data-cleanup (не код-баг)
Код-ревалидация уже сделана (`share/notes/[token]` GET ревалидирует `note.userId===shareLink.ownerId`). Остаётся разовая **чистка невалидных legacy share-links** в проде (data-операция, не код).

### env-var cleanup — гигиена (НЕ баг)
Аудит Vercel-переменных + прочистка мёртвых `#`-строк в `.env.local` + `.env.example`. **НЕ трогать значения секретов/ключей.** Детали → `FEATURE_CONCEPTS.md` → C1.

---

## ⚪ Осознанно НЕ чиним (решения владельца)
- **share/notes viewCount** инкремент (`share/notes/[token]/route.ts:34`) — аноним накручивает чужой счётчик-тщеславие, ноль импакта.
- **share-links GET mismatch** (`share-links/route.ts:16`) — игнорит mismatched query `userId`, но возвращает СВОИ линки звонящего, не жертвы → не уязвимость, косметика.

---

## ✅ Недавно исправлено (кратко; детали — git-история + память)
- **Набросок удалялся с одного клика по корзине (2026-07-26, P1, репорт владельца — «случайно нажал, и он удалился»):** удаление теперь ВООРУЖАЕТ, а спускает курок общий `ConfirmModal` (тот же, что на мысли/пункты плана) с превью текста наброска; undo-toast оставлен как вторая страховка. Обе точки удаления сведены в один гейт — кнопка-корзина И очистка текста заметки в `PointNote` (последняя удаляла набросок молча). Ключи `scratch.card.deleteConfirmTitle` / `deleteConfirm` в en/ru/uk. Живьём: корзина → диалог → «Отмена» оставляет набросок → «Удалить» удаляет + toast. Мутация «убрать гейт» роняет 3 теста. Якоря: `ScratchPanel.tsx:748` (`requestDeleteNote`), `:1306` (модалка), `app/components/sermon/__tests__/ScratchPanel.test.tsx:212`.
- **Открытая форма «Написать заметку» не показывала, кто активен, и висела отдельной карточкой (2026-07-26, P2, репорт владельца со скриншотом):** кнопка неактивна = серая; нажал → зелёная «Новая запись» уходит, кнопка растягивается на всю ширину, становится фиолетовой «головой», а панель ввода продолжает её одной фигурой (квадратный верх, общая рамка, нулевой зазор) — режим виден с одного взгляда. Заодно: повторный клик по голове закрывает и **НЕ стирает** набранный черновик (тот же класс «случайный клик съел текст»), убран дублирующий заголовок внутри панели, крестик встал рядом с «Добавить». Прокинут `hideRecordButton` через `AudioRecorderPortalBridge` → `AudioRecorder` → `SplitRecordButton`. Якоря: `ScratchPanel.tsx:991` (голова), `:1038` (панель), `AudioRecorderControls.tsx:533`. test:fast 468/4067 зелёные, tsc 0, eslint 0 errors.
- **Ручная заметка в «Набросках» принудительно однострочная (2026-07-26, P2):** `input type="text"` заменён на `react-textarea-autosize` с `minRows={2}` и неограниченным ростом; `Enter` теперь добавляет строку, отправка остаётся на явной кнопке. Браузер: 56 px пустое поле, 96 px на 4 строках, 136 px на длинном тексте, везде `clientHeight === scrollHeight`; 2 suites / 27 tests. Якоря: `ScratchPanel.tsx:1005`, `ScratchPanel.test.tsx:395`.
- **F7 STAGE C usage-cap UX review (2026-07-15, P2/P3, НЕ закоммичено):** 7 local UI catch больше не показывают generic/raw сообщение поверх глобального тёплого hard-cap toast; voice recovery blob/draft не создаётся на cap; `UsageGraceController` сохраняет предыдущее состояние через transient пустой entitlement snapshot и сбрасывает его только при смене uid/sign-out. Якоря: `CreateThoughtModal.tsx:102`, `studies/[id]/page.tsx:304,349`, `UsageGraceIndicator.tsx:73`; 7 suites / 84 tests, tsc 0, ESLint 0 errors.
- **Track A эпика fix-all-bugs (2026-07-14, НЕ закоммичено):** B1 escape всех user-полей в письме · B2 email из верифиц. токена · B3 image/payload client+server guard · B4 feedback rate-limit (sliding-window) · B5 admin/telemetry `requireAdminEmail`. tsc 0 · lint 0 err · test:fast 3709.
- **Deploy S2 app-wide auth hardening (2026-07-14):** bearer→uid(401)+ownership(403)+метринг по uid на ~47 AI/mutation-роутах; studies/analyze; feedback POST auth; materials↔notes cross-write; sermon DELETE series-cleanup атомарность (residual read-isolation → см. RMW-класс выше). Сводка → [[project_appwide_auth_hardening]].
- **UI/i18n серия (ранее):** БАГ-1..15 — переводы статов/счётчиков, русская плюрализация, локализация дат, line-clamp сниппетов, порядок плана в мобильном, цветовой тинт карточек серий, предзаполнение форм. Все закрыты, детали в git.

### Потеря правки заметки при редактировании из двух вкладок/устройств — P1, ОТКРЫТ (ВОСПРОИЗВЕДЕНО 2026-07-25)
**Эффект.** Правка, сделанная в одной вкладке, БЕССЛЕДНО пропадает, если вторая вкладка (открытая раньше и не видевшая этой правки) сохраняет свою. Это тот же симптом, что и исходный баг владельца, но НА ДРУГОМ пути записи — заметки `studyNotes`, а не массивы в проповеди.
**Живое воспроизведение (тестовый аккаунт, заметка `2a879399-8a8a-41a0-b2ca-c479eb48de2d`):**
1. Обе вкладки открыли одну заметку (текст: «Первая версия… ВТОРАЯ ПРАВКА…»).
2. Вкладка A дописала ` [ПРАВКА ИЗ ВКЛАДКИ A]` → сохранилось.
3. Вкладка B правку A НЕ УВИДЕЛА (живого обновления нет) и дописала ` [ПРАВКА ИЗ ВКЛАДКИ B]` к СВОЕЙ (устаревшей) копии.
4. Итог в БД: `… ВТОРАЯ ПРАВКА … [ПРАВКА ИЗ ВКЛАДКИ B]` — **правки A нет**. Проверено Admin-SDK чтением: `edit from TAB A: NO`, `edit from TAB B: YES`.
**Почему фикс `atomicUpdate` тут НЕ помогает.** Он закрывает гонку внутри МАССИВА (thoughts/preachDates/meetingDates), пересчитывая операцию на свежих данных. Здесь же переписывается ЦЕЛОЕ ПОЛЕ (тело заметки) содержимым редактора — устаревший снимок вытесняет более новый. Это класс «same-item merge» (ОТКРЫТО #2 в RMW-записи), проявленный на уровне документа целиком.
**Фикс (направление).** Для тела заметки: (1) не писать весь текст из памяти вкладки, а вести правку как операции/патч, либо (2) хранить `revision` и при расхождении показывать конфликт вместо тихой перезаписи, либо (3) живое обновление (listener) на открытой заметке, чтобы вторая вкладка не работала со стухшим текстом. Минимально-полезное сейчас — (3)+(2): подтянуть свежее и не дать сохранить поверх более новой версии молча.

### Cross-account утечка persisted-кэша (общий компьютер) — P1, ОТКРЫТ (найдено 2026-07-25)
**ВОСПРОИЗВЕДЕНО ВЖИВУЮ 2026-07-25** (было теорией, теперь факт): в одном браузере вышли из аккаунта A, вошли тестовым аккаунтом B, открыли прямой ссылкой проповедь аккаунта A → **на экране отрисовалось содержимое A** (заголовок, дата, стих), при этом `/api/me/entitlement` вернул **401**, т.е. СЕРВЕР данные НЕ отдавал. Источник — persisted React Query cache в IndexedDB `keyval-store`, ключ `react-query-cache`, поле `"buster": ""` (общий на все аккаунты, без привязки к владельцу). Firestore Rules тут бессильны: они гейтят серверное чтение, а не локальную копию.
**Был/Эффект.** После logout данные предыдущего аккаунта остаются в браузере и могут отрендериться следующему пользователю на ТОМ ЖЕ компьютере (реальный сценарий: общий церковный ПК). Firestore Rules защищают только СЕРВЕРНОЕ чтение — локальный persisted-кэш отдаёт данные без обращения к серверу.
**Три якоря:**
- `frontend/app/hooks/useSermon.ts:59` — `queryKey: ["sermon", sermonId]` **без UID** (списки при этом с UID: `['sermons', uid]` в `useDashboardSermons.ts:50`) → ключ совпадает у разных аккаунтов.
- `frontend/app/utils/queryPersister.ts:7` — ОДИН глобальный ключ IndexedDB `react-query-cache` на все аккаунты.
- `frontend/app/hooks/useAuth.ts:39-55` (`handleLogout`) — чистит `localStorage.guestUser` + `sessionStorage`, но НЕ React Query cache и НЕ IDB-persister.
**Фикс (направление — ИСПРАВЛЕНО после откáта 2026-07-25).** ⛔ НЕ «чистить кэш+persister при смене uid» — это пробовали и ОТКАТИЛИ: при выходе оно удаляло persisted paused-мутации (безвозвратная потеря несинхронизированных офлайн-правок, в т.ч. «вышел и зашёл тем же аккаунтом»), не трогало отдельный Firestore `persistentLocalCache` и не спасало от восстановления «бесхозного» payload до резолва auth. ✅ ПРАВИЛЬНОЕ направление — **разделять, а не стирать**: (1) ключ IDB-persister партиционировать по разрешённому uid (`react-query-cache:<uid>`), (2) свой QueryClient на владельца, (3) owner-uid + auth-epoch внутри persisted-конверта и отбраковка «опоздавшего» restore, (4) НЕ монтировать/восстанавливать persist-провайдер до резолва auth (anonymous uid — тоже полноценный владелец), (5) Firestore-кэш решать ОТДЕЛЬНО (`memoryLocalCache` по умолчанию либо координированный multi-tab terminate/clear, сохраняющий pending-записи), (6) uid в ключ детали (`["sermon", uid, sermonId]`) как второй слой.

### Вечный скелетон вместо ошибки при открытии ЧУЖОГО/удалённого документа — P2 (найдено 2026-07-25 живьём)
**Был/Эффект.** Открытие проповеди, которая НЕ принадлежит текущему аккаунту (прямая ссылка, старая закладка, документ удалён, смена аккаунта), оставляет страницу в бесконечных skeleton-плейсхолдерах. Пользователь не понимает, грузится или сломалось; ошибки не видно.
**Якорь/репро.** Залогинен как A, открыть `/sermons/<id-принадлежащий-B>` → `main` остаётся в `animate-pulse`, счётчик мыслей не появляется, консоль без видимой ошибки (Firestore Rules просто не отдают документ). Воспроизведено на localhost: под аккаунтом `Jhwh42NbpL…` открыт документ тестового аккаунта `CFZ5TEvAjHrPkdwiE6Ug` → бесконечный скелетон; своя проповедь того же вида грузится нормально (13 в списке, деталь ок).
**Фикс (направление).** `useSermon`/`useServerFirstQuery` должны различать «ещё грузим» и «нет доступа/не найдено» (permission-denied / undefined после успешного запроса) и рендерить явное состояние: «Проповедь не найдена или недоступна» + ссылка на список. Проверить те же пути для series/groups/prayers/studies (один класс).

### Заметка: текст пропадает, если закрыть страницу в пределах 1,5 с после набора — P1, ОТКРЫТ (найдено 2026-07-25 чтением кода)
**Был/Эффект.** Автосейв заметки ставит отложенное сохранение на 1,5 с, а cleanup эффекта его ОТМЕНЯЕТ: `const timeoutId = setTimeout(saveChanges, 1500); return () => clearTimeout(timeoutId);` (`frontend/app/(pages)/(private)/studies/[id]/page.tsx:225-232`). Напечатал и ушёл со страницы / закрыл вкладку быстрее 1,5 с → сохранение не выполняется, текст исчезает без единого сигнала. Потеря существует СЕГОДНЯ, независимо от сине-синхронизации и CAS.
**Контраст внутри же проекта.** `useAutoSave` в аналогичной ситуации делает `debouncedSave.flush()` при размонтировании (`frontend/app/hooks/useAutoSave.ts:66-72`) — то есть правильный паттерн в репозитории есть, заметка от него отклоняется. Но и flush не спасает: ошибка сохранения там уходит только в `console.error` + `status='idle'` (`:47-53`), в UI не попадает.
**Фикс (направление).** Долговечный черновик (ключ uid+документ+агрегат), который ложится в переживающее перезагрузку хранилище ДО попытки записи и предъявляется при следующем открытии. Один flush проблему не закрывает: он не спасает от закрытия вкладки и не переживает падение.

### Автосейв группы читал устаревшие refs, спасаясь чужим `await` — P2, ИСПРАВЛЕНО 2026-07-25
**Был/Эффект.** `performSave` в `frontend/app/(pages)/(private)/groups/[id]/page.tsx` читает значения из refs, присваиваемых во время рендера. Сохранение может стартовать, пока событие, его вызвавшее, ещё на стеке — тогда refs держат ДОпрefix-значения. Раньше это не проявлялось только потому, что первой строкой ВСЕГДА шёл `await updateGroupDetail(...)`, и эта уступка планировщику давала React зафиксировать состояние. Скрытая зависимость от порядка операторов.
**Как вскрылось.** При переходе на отправку только изменённых полей запись группы стала пропускаться, когда менять нечего → исчезла и случайная уступка → дата встречи читалась пустой (`date=""`, подтверждено логом в тесте), и добавление даты не выполнялось вовсе. Три теста `group-detail-page.test.tsx` покраснели.
**Фикс.** Явный `await Promise.resolve()` в начале `performSave` с комментарием, объясняющим зачем. Точнее было бы не зависеть от тайминга рендера вообще (передавать значения в сохранение явно), но это большая переделка автосейва — оставлено как направление.

### Долговечный черновик: переход МЕЖДУ документами не сбрасывает недописанный черновик — P2, ОТКРЫТ (найдено 2026-07-25 ревью-чтением)
**Был/Эффект.** `useDurableDraft` пишет черновик с задержкой 250 мс и дополнительно сбрасывает его при `pagehide`, сворачивании вкладки и размонтировании. Но переход с одного документа на другой ВНУТРИ приложения (`/studies/a` → `/studies/b`) хук НЕ размонтирует: меняется только ключ. При этом срабатывает лишь очистка таймера (`frontend/app/hooks/useDurableDraft.ts:126-130`), а сброс висит на эффекте с пустым списком зависимостей (`:133-155`) и не выполняется. Итог: текст, набранный в последние ~250 мс перед переходом, в черновик не попадает.
**Не регрессия.** В HEAD черновиков нет вовсе, так что хуже не стало — но заявленный инвариант «черновик есть ⟺ текст не подтверждён» в этом окне не держится.
**Фикс (направление).** Отдельный эффект на смену ключа, сохраняющий ПРЕДЫДУЩИЙ ключ с его же значением. Ловушка: к моменту выполнения эффекта `valueRef` может уже содержать значение НОВОГО документа — записать его под старым ключом означало бы подсунуть чужой текст. Нужна пара «ключ+значение», зафиксированная синхронно при рендере, и тест именно на переход A→B без размонтирования.

### Freshness-rollout: CAS до durable draft создаёт новые потери текста — P1, ОТКРЫТ (static review 2026-07-25)
**Суть.** Ожидаемый отказ устаревшей записи сейчас приходит ПОСЛЕ того, как несколько UI уничтожили единственную редактируемую копию: молитва вызывает `mutate()` и немедленно резолвит wrapper (`usePrayerRequests.ts:250-265`), после чего страница показывает success и закрывает modal (`prayers/[id]/page.tsx:135-139`); dashboard sermon modal вызывает `onClose()` ДО `await onSaveRequest` (`EditSermonModal.tsx:74-89`), а поздний `onError` восстанавливает целый старый sermon (`mutationDefaults.ts:574-577`). Поэтому включать CAS до durable draft/restore нельзя: чужой текст перестанет затираться, но свой будет теряться молча.
**Дополнительные блокеры протокола.** `groups/[id]/page.tsx:201-209` при любом autosave отправляет сразу все content-поля, а `useGroupDetail.ts:105-118` пишет fire-and-forget; production plan editor сохраняет полный `plan` через Admin API (`planApi.ts:55-67` → `sermons/[id]/plan/route.ts:271-334` → `sermons.repository.ts:158-160`), хотя `plan` отсутствует в предложенном списке aggregate revisions; Firestore rules (`firestore.rules:30-58,70-74,88-95`) проверяют ownership, но не revision, поэтому старые/обходные writers остаются разрешены.
**Гейт исправления.** Сначала durable draft/outbox + восстановление после reload для каждого editor, затем единый протокол для ВСЕХ writers данного aggregate (включая Admin/offline/delete) с version gate/rules, и только потом включение CAS и listener UX.

### CAS заметки доверяет свежей revision при старом тексте и разрешает затирание — P0, ОТКРЫТ (adversarial review 2026-07-25)
**Механика.** `serverRevisionRef` объявлен как версия, от которой построен текст (`studies/[id]/page.tsx:755-760`), но listener безусловно двигает его на версию каждого серверного снимка (`:831-849`), не применяя этот снимок к полям редактора. Поэтому последовательность `редактор=v0 → локально набран L → сервер принимает R/rev1 → listener ставит ref=1, поля остаются L → autosave шлёт L с expected=1` проходит CAS и тихо затирает R (`:267-280`; `conflictSafeUpdate.client.ts:93-100`). Интерфейс конфликта не появляется.
**Минимальный фикс.** Разделить `displayed/baseRevision` и `latestRemoteRevision`; listener не имеет права двигать первую без `applyRemote`. Rebase/«оставить моё» должен быть отдельной явной операцией на точную `actualRevision`.

### Незащищённые писатели проповеди: план, наброски, порядок разделов, подготовка, даты — P1, ОТКРЫТ (найдено 2026-07-26 сплошной проверкой писателей)
**Суть.** Обнаружение до этих полей ДОХОДИТ (плашка «изменено на другом устройстве» их видит через отпечаток), а защита — НЕТ: это записи «поле целиком» обычным `updateDoc`, без заявленной версии, без отпечатка и без канала отказа. По человеческой мерке: телефон утром добавил пункт в план, ноутбук стоял открытым с вечера (или спал), в обед из него сохраняется план, собранный из ВЕЧЕРНЕГО состояния — утренний пункт исчезает молча. Ровно тот случай, что владелец назвал целью («plan, points/subpoints, scratch + order»).
**Якоря писателей.**
- `sermons.client.ts:307-314` `updateSermonOutlineViaClient` — план/пункты целиком. Шесть точек вызова: `sermons/[id]/page.tsx:917`, `sermons/[id]/structure/page.tsx:648`, `components/sermon/SermonOutline.tsx:249`, `components/column/useColumnOutlineState.ts:120`, `components/plan-editor/PlanEditorModal.tsx:162`.
- `sermons.client.ts:315-334` `applyScratchToOutlineViaClient` — план И наброски одной записью.
- `sermons.client.ts:350-358` `writeScratchNotesViaClient` — наброски целиком (add/update/delete).
- `sermons.client.ts:296-305` `updateStructureViaClient` — `thoughtsBySection` + легаси `structure` (порядок разделов).
- `sermons.client.ts:284,288` — `preparation` (шаги подготовки).
- `sermons.client.ts:602,643` + серверный `sermons.repository.ts:295-310` — `preachDates` (то, что видно в календаре).
- `groups.service.ts:245` — `meetingDates` группы.
**Почему это НЕ ложное разрешение (важно для приоритета).** Счётчик эти пути не двигают, но защита давно решает по ОТПЕЧАТКУ перезаписываемых полей, поэтому чужая правка тех же полей отклоняется даже при застывшем счётчике. Опасность здесь другая: сами эти писатели НЕ спрашивают разрешения вообще.
**Фикс (направление).** Не «обвесить шесть точек вызова», а сузить запись: писатель принимает состояние, с которым редактор ОТКРЫЛСЯ (`expectedOutline` / `expectedScratch`), считает из него отпечаток и идёт через `conflictSafeUpdate` с уже существующим агрегатом (`plan` / `scratch` / `structure`). Отказ ОБЯЗАН всплыть выбором (панель `SaveConflictBanner` + долговечный черновик), иначе «затёр чужое» превратится в «молча потерял своё» — это ровно та подмена, за которую предыдущие витки ревью били. Наброски вдобавок стоит перевести на операции по id (add/remove по элементу), тогда целый массив писать не нужно вовсе.

### ~~Persisted replay заметки теряет expectedRevision и обходит CAS~~ — P0, ✅ ЗАКРЫТО (проверено 2026-07-26)
**Как проверено.** `mutationDefaults.ts:463-473` — replay-контракт заметки теперь принимает и передаёт `expectedRevision`; то же у серии (`:303-313`), молитвы (`:329-340`) и статуса «отвечено» (`:353`). Возобновлённая после перезагрузки правка снова идёт через защиту, а не превращается в обычную запись.
**Механика.** Живой mutation принимает и передаёт `expectedRevision` (`useStudyNotes.ts:52-65`), но resumable mutation default после reload деструктурирует только `{id,updates,userId}` и вызывает `updateStudyNote` без версии (`utils/mutationDefaults.ts:424-427`). PersistQueryClient сохраняет и возобновляет paused mutations (`QueryProvider.tsx:79-92`). После reload та же операция становится обычным `updateDoc`, не bump'ит `rev.note` и может молча заменить более новый текст.
**Минимальный фикс.** Включить `expectedRevision` в persisted variable contract и replay; при stale показывать/сохранять конфликтный intent, никогда не downgrade'ить его до unguarded write.

### ~~Revision ledger не является общим протоколом writers~~ — P0, ✅ ЗАКРЫТО ПО СУТИ (проверено 2026-07-26; остаток см. «Незащищённые писатели проповеди» ниже)
**Как проверено.** Незащищённые пути ТОЖЕ двигают счётчик: группа `groups.service.ts:150`, проповедь-ядро `sermons.client.ts:253-256`, серия-мета `series.service.ts:139-140`, мысли `:519`; серверные (Admin) — `sermons.repository.ts` через `updateSermonData(…, aggregate)`, `thoughts/route.ts`, `series.repository.ts:95,135,163`. Главное же: ложное разрешение закрыто не счётчиком, а ОТПЕЧАТКОМ — решает содержимое перезаписываемых полей, поэтому писатель, забывший счётчик, больше не выдаёт мандат на затирание.
**Механика.** Guard группы недостижим через экспорт: `updateGroup` не принимает revision и всегда вызывает private writer без третьего аргумента (`groups.service.ts:254-260`); production hook тоже передаёт только два (`useGroupDetail.ts:105-118`). В sermon `core` guarded только `SermonHeader`, но тот же aggregate меняют без bump `EditSermonModal` (`:97`), `OptionMenu` (`:190,211,263`), sermon page (`:1206`), preparation writer (`sermons.client.ts:229-255`) и server/Admin writer (`api/repositories/sermons.repository.ts:53-64`). Series meta также имеет unguarded writers (`useSeries.ts:102-105`; `utils/mutationDefaults.ts:299-302`). Rules проверяют ownership, но не изменение revision (`firestore.rules:30-53`). Такой счётчик может оставаться прежним после изменения данных, поэтому следующий stale guarded writer получает ложное разрешение.
**Минимальный фикс.** Один обязательный writer protocol на aggregate: любой data-change атомарно bump'ит revision, любой edit intent указывает base revision; старые клиенты/серверные маршруты либо мигрируются, либо временно не смешиваются с CAS-claim.

### ~~Guarded save ухудшает offline queue и series может остаться на старой revision~~ — P1, ✅ ЗАКРЫТО (проверено 2026-07-26)
**Как проверено.** Офлайн-часть: транзакция больше не единственный путь — при офлайне/недостижимом сервере намерение уходит в долговечную очередь (`writeOutbox.client.ts`) и доигрывается через ту же защиту. Версия: сервис возвращает модель с НОВЫМ счётчиком, не перечитывая документ (`series.service.ts:135`, `sermons.client.ts:249`) — перечитывание сразу после коммита отдавало ДО-коммитное состояние.
**Механика.** HEAD использовал `updateDoc`; guarded `SermonHeader` и `useSeriesDetail` теперь используют `runTransaction`, которая офлайн падает и не попадает в нативную очередь. У series helper bump'ит серверный `rev.meta`, но сервис возвращает локальный `current` без нового `rev` (`series.service.ts:122-127`). Затем `refetch()` и invalidation по умолчанию не бросают при сетевой ошибке (`useSeriesDetail.ts:101-109`), поэтому UI может считать сохранение успешным и закрыть modal, сохранив в кэше старую revision; следующая легитимная правка будет отвергнута до успешной синхронизации.
**Минимальный фикс.** До durable intent/outbox не заменять queued `updateDoc` на transaction в офлайн-редакторах; после commit возвращать модель с новой revision и проверять `refetchResult.isError` либо использовать `throwOnError: true`.

### ~~Плашка конфликта заметки не ставит autosave на паузу~~ — P0, ✅ ЗАКРЫТО (проверено 2026-07-26)
**Как проверено.** `useNoteAutoSave` принимает `saveBlocked` и выходит первым же действием (`useNoteAutoSave.ts:227`), страница передаёт туда состояние конфликта (`studies/[id]/page.tsx:812`). Пока человек не выбрал, фоновые сохранения не идут.
**Механика.** При `StaleWriteError` автосейв только вызывает `onConflict()` и возвращается (`studies/[id]/page.tsx:281-289`); родитель превращает это в `setSaveConflict(true)` (`:798-804`). Сам `useNoteAutoSave` не получает `saveConflict` и его effect продолжает запускаться на каждое изменение полей (`:298-304`). Если человек продолжает печатать, новая попытка идёт ДО выбора «оставить моё / взять чужое». Когда listener уже продвинул `revisionRef`, она проходит CAS и молча заменяет remote; когда не продвинул — снова конфликтует.
**Минимальный фикс.** После первого stale блокировать все фоновые save до явного решения; хранить отвергнутый immutable intent `{payload,baseRevision,actualRevision}`. «Оставить моё» отправляет именно этот (или явно подтверждённый текущий) intent один раз против `actualRevision`; дальнейший stale снова требует решения.
