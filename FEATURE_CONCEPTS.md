- иногда пользователь создает огромную `Thought` и в нее ложит все свои наработки и идеи для будущей проповеди, вообще нужна возможно, если к заметки добавить определенный тэг, то у нее появляется опция конвертироватьв в проповедь, на основе заметки создается план проповеди (структура), и мысли. И тоже самое можно сделать с заметкамиа, когда из заметки создается план проповеди (структура), и мысли.

---
- нужно добавить возможность использовать платные модели, потому что бесплатные могут быстро закончиться
- Нужно исследовать на модели который дешевле, для free tier, добавить возможность выбирать модель для paid tier
  → детальная спека (в работе): `frontend/docs/feature-concept-tiers-and-model-selection.md` — tiers (free/paid/admin/promo), usage-лимиты, server-side выбор модели, security, referral, админка, prep-рефакторинг

---

## Монетизация — доделки ПОСЛЕ Ф0–Ф6 (2026-07-13)
> Эпик Ф0–Ф6 + last_seen + referral tracking **построены и верифицированы** (tsc 0 · test:fast 3605 · независимый Popper 0×P0/P1). **`firestore.rules` задеплоены в прод** (`test:rules` 79/0). App-код НЕ в проде (пойдёт на деплое приложения = кнопка владельца). Ниже — что ОСТАЛОСЬ.

### F1. Транскрибация (STT) и озвучка (TTS) — реальный per-tier выбор + прошивка в исполнение (как текст, «один-в-один»)
**Хочет владелец:** сделать транскрибацию и TTS в настройках ТОЧНО как текст-модели — показываются доступные-по-tier модели, юзер выбирает из доступных; **free → только free-модель (дефолт), paid → больше моделей/голосов**. И ТО ЖЕ прошить в поток **экспорта/генерации аудио проповеди** (конвертация в аудио) — выбор должен реально применяться там.
**Сейчас:** выбор транскрибации/TTS в UI показывается, но исполнением ИГНОРИТСЯ (читается env-дефолт). TODO: `openAI.client.ts:43` (транскрибация), `audio/generate/route.ts:187` (TTS). Прошит только текст (`resolveUserTextTargets`).
**Сделать:**
- Прошить потребление `preferredTranscription` в транскрибация-путь (per-user, tier-gated, тот же escalation-guard + fallback-хвост, что у текста → free не падает, preference только в рамках `tier.allowed`).
- Прошить `preferredTts` в аудио-экспорт (`audio/generate`) и в генерацию аудио проповеди — выбор реально меняет исполняемую модель/голос.
- В настройках: транскрибация/TTS → полноценные селекторы (free=дефолт-locked, paid=выбор из каталога); убрать нынешний read-only/«показ-без-эффекта».
**Валидация по языкам — УЖЕ СДЕЛАНА, НЕ переделывать:** качество моделей по en/ru/uk исследовано и записано — `frontend/docs/openrouter-model-research-2026-07-13.md` + журналы `.sessions/` (там есть example). Взять готовое.

### F2. Админка — декомпозиция на ЛЕВЫЕ ТАБЫ + дефолты транскрибации/TTS
**Хочет владелец:** «нельзя всё лупить в одну страницу» — сейчас `/admin` одна длинная страница. Разбить на **левую навигацию/табы** (как в настройках): напр. «Пользователи» · «Модели по умолчанию» · (далее usage/referral-статистика). И в «Модели по умолчанию» добавить выбор дефолта для **транскрибации и TTS** (сейчас только текст — т.к. только он прошит; после F1 добавить все три).
**Сделать:** левая навигация в `/admin`; вынести список юзеров и модели-дефолты на отдельные табы; вернуть транскрибация/TTS дропдауны в дефолты после F1.

### F3. Настройки — ссылка «Админка» ЛЕВОЙ вкладкой
**Хочет владелец:** сейчас переход в админку — внизу «Настроек пользователя», нужно скроллить. Перенести в **левую вкладку** `SettingsNav`: пункт «Admin» → /admin. Показывать только админу (гейт `/api/admin/me`, как сейчас условная ссылка).

### F4. OpenRouter-модели — ВКЛЮЧЕНЫ (ключ выставлен 2026-07-13)
Владелец добавил `OPENROUTER_API_KEY` в `.env.local` + Vercel. OpenRouter-модели (qwen/deepseek/grok-voice) — теперь рабочий платный выбор (не «скоро»). Текст уже прошит; транскрибация/TTS оживут после F1. Проверить живьём, что выбор реально идёт на OpenRouter.

### F5. Прочие residuals (не блокеры)
- **Sybil anti-farm рефералов** — координированные verified-акки <24ч могут фармить промо → App Check / rate-limit для серьёзной программы.
- **TOCTOU soft-cap usage** — assert-available и consume не атомарны на burst → возможен небольшой перерасход при массовом одновременном запуске.
- **Кэш конфига дефолтов** — `config/aiModelDefaults` читается на каждый текст-вызов → process-level memo с коротким TTL (инвалидация на admin-save).
- **Тюнинг free-лимитов** — 100 вызовов / 3600 сек транскрибации — подобрать под реальную нагрузку.
- **Историческая недосчётка рефералов** — рефералы ДО фичи referralEvents (есть `referredBy`, нет события) не попадут в invitedCount → при желании one-time backfill из `referredBy`.
- **Email существующим юзерам** про платные фичи — ops-шаг (провайдер+шаблоны+unsubscribe), не код.
- **P1 «кража чужой квоты»** — **ЗАКРЫТ** (app-wide auth hardening, deploy S2, 2026-07-14; см. `BUGS.md` → «Deploy S2»).

### F7. Grace-band лимит usage — «по благодати» (владелец 2026-07-14)
**Идея.** Не жёсткий блок ровно на лимите, а МЯГКИЙ порог с грацией. Мотив: одно легитимное действие может стоить >1 (транскрибация = STT + полировка = 2 списания), и падать на полпути — плохой UX; дух приложения (помощник проповедника) → благодать уместна как продуктовая черта, не только техника.
**Поведение:**
- Действие, начатое в пределах лимита, **ДОигрывается целиком** (даже если перескочит на 1-2 — напр. 99 → 101).
- Мягкий блок **НОВЫХ** действий, когда лимит достигнут/чуть превышен.
- **Hard-cap** на бо́льшем пороге (напр. `limit + grace`, где grace = 10% или фикс) — дальше реально нельзя запустить новое.
- При перерасходе в настройках/админке — тёплое сообщение + **СТИХ про благодать** (напр.: «даже если мир нечестен — будьте честны и живите по благодати» / «Христос простил нам все долги»). Ротация нескольких стихов.
**Связь.** Заменяет строгий атомарный фикс **B9/TOCTOU** (`BUGS.md`) и закрывает старый residual F5 «TOCTOU soft-cap»: малый overshoot допустим **by design** → атомарность не критична; остаётся только hard-cap + grace-message. Реализация: soft limit (allow до `limit+grace`) + hard limit + UI перерасхода со стихом.

**UX — УТВЕРЖДЕНО владельцем (2026-07-14, демо `.demo/grace-band-usage.html`; рамка: христианин + помощник, НЕ бизнес):**
- **Место:** индикатор в **header** (он почти всегда на экране), во ВСЕХ рабочих режимах. **Скрыт в режиме «проповедовать»** (`/groups/[id]/conduct`) — время служения, ноль meta-UI.
- **Индикатор по нарастающей:** <80% — точки НЕТ вовсе (не создаём тревогу дефицита); 80→100% — еле заметная точка рождается, наливается **янтарём**; на/за лимитом — НЕ красный, а **фиолетово-фуксиевая семья** (токен кнопки «Редактор плана»: `from-violet-600 to-fuchsia-600`, dark `violet-500→fuchsia-500`, `StructureStats.tsx:152`) — «особое/благодатное» состояние, не «опасность». Hover/клик → **tooltip с ПРАВДОЙ** (осталось N из M · обновится DATE), без пугалок.
- **Grace-toast (ОДИН раз на переходе за лимит, потом тихо):** назвать границу + «мы вас покрыли» + «ещё какое-то время будем являть благодать» + «если вам правда нужно — да благословит вас Господь» + ротация стиха. Грация должна ЧУВСТВОВАТЬСЯ как забота, не быть молчаливой.
- **Hard-cap (`limit+grace`):** ⚠️ НЕ писать «лимит исчерпан» — создаёт ложное чувство, что лимит был `limit+grace` изначально. Вместо: «мы сделали для вас больше, чем могли» / «нам важно помнить и о других — чтобы хватило всем» + **мягкая (одна) строка про расширение** (владелец: оставить, «по совести», без давления) + стих. У места действия + зеркало в настройках.
- **Апселл:** только мягкая строка на потолке; на точке/тултипе — НЕТ. Никаких dark-pattern скарсити.
- **Размер грации — 10% плоско (владелец 2026-07-14, заякорено на реальных лимитах `tierPolicy.ts:82-87`):** дискретные (`aiCallsPerPeriod` free 100) → +10%, мин +2; секундные (`transcriptionSecondsPerPeriod` free 3600с=60мин) → +10% = 360с=6мин (владелец: «очень уместно»). Ранний «+120с потолок» отменён (был из неверного числа).
- **⚠️ ПРОБЕЛ (находка 2026-07-14):** аудио-лимита (TTS) в коде НЕТ. `TierLimits` = только `aiCallsPerPeriod` + `transcriptionSecondsPerPeriod`; `audioSecondsUsed` метрится (`usageLimits.server.ts:143 consumeAudioSeconds`), но НЕТ ни `audioSecondsPerPeriod`, ни `assertAudioAvailable` → озвучка сейчас БЕЗ потолка. F7/F8-билд обязан добавить `audioSecondsPerPeriod` в `TierLimits` + assert.
- **РЕШЕНИЕ владельца по аудио-лимиту (2026-07-14): `audioSecondsPerPeriod` = ⅓ транскрибации** (доп.функция «сгенерировал → слушаешь», не ядро). free **1200с=20мин**, tier1 6000, tier2 12000, tier3 30000, tier4 60000. Открыто: какую TTS-модель дефолтить free (дорогая `gemini-3.1-flash-tts` $36/1M vs дешёвая `gpt-4o-mini-tts` ~$17 / `gemini-2.5-flash-tts` $18).
- **Денежный якорь (worst-case, 100% utilization, дефолтные модели; цены `functionCatalog.ts` + `estimateDuration` 150 wpm, ~6.5 симв/слово):** ед. цены — AI-вызов ~$0.0025 (допущение 3k+1.2k tok), STT $0.006/мин, TTS ~$0.035/мин ($36/1M). **free ≈ $1.32/юзер (~$132/100 юзеров)**; tier1 ~$6.6; tier2 ~$13.2; tier3 ~$32.9; tier4 ~$65.8. Аудио — самая дорогая строка даже при 20мин ($0.70 > STT $0.36 > AI $0.26); дешёвая TTS-модель ½-ит аудио. TTS-цены = репо-оценки `est.` (модели новые, не публичный прайс).

### F8. Audio (TTS) метринг + нарезка — РЕШЕНО (владелец, 2026-07-14, после council + независимого Popper)
**Нарезка:** чанки **~1750 символов для ВСЕХ провайдеров** (владелец: проверено ранее, оставляем; A/B-прослушивание СЕЙЧАС не делаем). Обратимо (одна константа) — если Gemini зазвучит «металлом», подкрутить. Смысл чанков: качество (авторегрессионный дрейф) + не превышать лимит + не слишком мелко. *(Popper-остаток: 1750 не мерян именно на Gemini — принято как временная точка.)*
**Google timeout → клиентский батчинг как у OpenAI:** каждый Google-кусок = свой запрос, склейка в браузере. Снять `isBatched = provider !== 'google'` (`route.ts:308`); обработать section-паузу (700ms, `route.ts:152`) на швах батчей.
**Cost-инвариантность (якорь `functionCatalog.ts:31` — биллинг «$/1M chars»):** 10 чанков = 1 большой по ДЕНЬГАМ (платим за символы); параллель влияет только на ВРЕМЯ → чанки = бесплатный выигрыш скорости.
**Метринг:** audio = сгенерированные **секунды** (мерить БЛОБ через `music-metadata`, **fail-CLOSED**; НЕ текст-оценку `estimateDuration`, которую route выбрасывает). text/транскрибация = «1 за операцию» (пока просто). **Хранить СЫРОЕ** (секунды + счётчик операций); cost-множитель применять на ЧТЕНИИ (биллинг/дисплей), НЕ запекать в счётчик → «докрутить точность потом» = поменять множитель, не переписывать историю. Позже: сверять с реальными провайдер-биллами за период → тюнить множители (Gemini 3.1 ≈ 2× per char).
**Overshoot:** секунды известны только ПОСЛЕ генерации → строгий reserve невозможен → record-actual пост-фактум + **grace-band (F7)** поглощает малый перебор. F7 ⟺ audio-метринг — одна связка.
**Планы (позже, продуктовый слой):** prepaid-кредит («$10, трать пока не кончится → free») · месячный · usage-recommendation («по активности — план X: N минут, M операций»). Юзер выбирает.

**СТАТУС РЕАЛИЗАЦИИ (2026-07-14, автономно, НЕ закоммичено):** метринг-ядро + Google client-батчинг (флаг `GOOGLE_SMALL_CHUNKING`) + retry построены; независимый Popper — самые твёрдые атаки held (WAV-склейка PCM-aware, section-пауза на швах ×1, метринг fail-closed/allSettled/1-aiCall-на-экспорт). tsc 0 · lint 0 err · test 3740. **Live-jidoka (реальный экспорт) — приёмочный гейт владельца, ещё не наблюдалось.**
**ОТЛОЖЕННЫЕ грани (закрываются audio-seconds-enforcement/грацией, F7):**
- **Тихая потеря чанка OpenAI** (упал оба retry → аудио без куска, молча) → продуктовое решение владельца: частичное / предупредить / fail. *(Google batch=1 hard-fail → безопасен.)*
- **Интерим-кэп edges:** aiCall на offset0 + assert на каждом батче → (а) на точной границе лимита многобатчевый экспорт рвётся после списания; (б) offset≥1 не списывает → крафт-запрос = бесплатное аудио. Оба закрываются enforcement audio-seconds. Наивный фикс сейчас усугубил бы (б).

### F6. Referral anti-abuse — human-in-the-loop (дизайн владельца, 2026-07-14) — заменяет F5-Sybil
**Не авто-блок, а флаг + ручной разбор:**
- Первые **2 реферала** инвайтера — авто-грант без проверки (как сейчас).
- На **3-м реферале**: (а) флаг `referralWarning` на инвайтере (виден в админке); (б) письмо на `OWNER_EMAIL` «проверь юзера на честность» + контекст (uid · сколько рефералов · кого пригласил).
- **Промо продолжает начисляться** — человек МОЖЕТ законно набрать период; флаг = сигнал на ручной разбор, не авто-стоп.
- **Админка — новый контроль:** «отключить / аннулировать промо» у юзера **+ показывать активность** флагнутого (кол-во проповедей · `last_seen_at` · usage) — чтобы отличить «живого» от «пустышки» одним взглядом.
**Философия приёмки (для ручного суждения):** набрал + пишет проповеди = ок · набрал + ничего не делает = подозрительно · делает глупости = предупреждение → блок. Данные уже есть (`last_seen_at`, счётчики).
**Механика для справки (`referral.server.ts`):** награда = **+30 дней tier1 за КАЖДОГО реально пришедшего, кумулятивно**. Защита сейчас: `email_verified` + аккаунт <24ч + один клейм на invitee. (20 приглашений, 2 регистрации → 60 дней.)

### F7. Audio metering — считать по факту (решение владельца, 2026-07-14)
Мерить **фактически сгенерированное аудио** (только успешные чанки), не «раз на батч». Это надёжность УЧЁТА, не расход клиента. Валидировать чанки ДО fan-out; reserve/consume per успешную провайдер-операцию. Деталь + якорь → `BUGS.md`.

---

## Технический долг / гигиена (класс C — проектный долг, НЕ привязано к конкретной фиче)

### C1. Чистка env-переменных (Vercel + локальный)
**Контекст:** конфиг модели/провайдера раскидан по env, накопились дубли, закомментированные и устаревшие строки. В `frontend/.env.local` наблюдаются: закомментированные `# AI_MODEL_TO_USE=OPENAI`, `# OPENAI_AUDIO_MODEL=whisper-1`, и ПОВТОРЯЮЩИЕСЯ ключи (`OPENAI_AUDIO_MODEL` дважды: `gpt-4o-transcribe` и `whisper-1`; `OPENAI_GPT_MODEL` дважды: `gpt-4o-mini` и `gpt-5-mini`) — неясно, какая строка реально действует.
**Что сделать:**
- Инвентаризация ВСЕХ AI-related env: локальный `frontend/.env.local` + Vercel env (prod/preview/dev). Свести в один список: имя → значение → где используется (`file:line`).
- Убрать дубли и мёртвые/закомментированные строки; оставить по одному источнику истины на ключ.
- Сверить локальный набор с Vercel — не должно быть расхождений, ломающих прод.
- Привести имена к консистентности; задокументировать актуальный набор (напр. `.env.example` с плейсхолдерами, без секретов).
**Осторожно:** не сломать текущую работу (реальный дефолт — `AI_MODEL_TO_USE=GEMINI`, `GEMINI_MODEL=gemini-3.1-flash-lite-preview`). Секреты/ключи НЕ коммитить.

### C2. Убрать устаревшие/ложные комментарии + гигиена по ходу (принцип)
**Контекст:** в коде есть комментарии, которые ВРУТ и путают. Подтверждённый пример: `frontend/app/api/clients/openAI.client.ts:44` — `// This should be 'o1-mini'`, тогда как реальный дефолт давно `gemini-3.1-flash-lite-preview`.
**Что сделать:**
- Прочесать код (особенно AI-клиенты: `openAI.client.ts`, `structuredOutput.ts`, `tts.client.ts`, `speechOptimization.client.ts`) на устаревшие/ложные комментарии, закомментированный мёртвый код, неверные подсказки-хинты, протухшие TODO.
- Убрать/обновить их так, чтобы комментарий отражал реальность.
**Принцип (постоянный, не разовый проход):** при ЛЮБОЙ правке подчищать попутно (мёртвый код, старые комментарии, неконсистентность имён) — код должен оставаться красивым, структурным, расширяемым. Гигиена вплетена в работу, а не откладывается.

### C3. Валидация промптов под разные модели/провайдеры (подумать)
**Контекст:** после перехода на per-user выбор модели/провайдера ОДИН и тот же промпт пойдёт в РАЗНЫЕ модели (OpenAI / Gemini / OpenRouter). Они по-разному следуют инструкциям, структурному выводу (JSON schema), языку (RU/UK/EN), длине. Промпт, вылизанный под одну модель, может деградировать на другой (сломанная структура, смешение языков, over-/under-generation).
**Что продумать:**
- Нужен ли общий baseline промптов + слой минимальных адаптаций под модель, ИЛИ один robust-промпт, ИЛИ per-model варианты — выбрать стратегию.
- Механизм/этап ВАЛИДАЦИИ: golden-набор входов → прогон по каждой поддерживаемой модели → сравнение выходов (структура ок? язык ок? нет over/under-generation?). Связать с существующей AI-промпт-телеметрией: `frontend/docs/ai-prompt-telemetry-review-loop.md` (там уже классификация дельты вход→выход).
- Критерий приёмки модели в список выбора: промпты дают приемлемый результат на всех ключевых задачах (сортировка мыслей, план, инсайты, направления) на RU/UK/EN.

---

## Concept Map / Исследовательские связи    

- Пользователь исследует тему (например, "Дети Соломона") и делает заметку (вопрос-ответ).
- Затем находит связанную информацию ("Кто мать Ровоама?") и делает вторую заметку, связывая её с первой.
- Постепенно строится граф связей (Concept Map) без единого центрального узла (в отличие от классического Mind Map).
- Каждое новое исследование (вопрос-ответ-заметка) становится узлом в этой карте.
- Это позволяет видеть динамику исследования и переходить от одной темы к другой (например, от Соломона к Ровоаму) через выявленные связи.
- Визуально это должно выглядеть как сеть связанных фрагментов знаний, которую можно дополнять и исследовать.
- Суть в том, что пользователь сам строит эти связи по мере изучения материалов.

---
