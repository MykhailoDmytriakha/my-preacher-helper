# План упрощения приложения и тестов

Статус: реализация разрешена пользователем 2026-09-07; первый ограниченный этап выполнен локально. Общая программа продолжается.

## 1. Цель и границы

Сократить число независимо поддерживаемых правил, повторяющейся разметки и связей между частями приложения. Сделать изменения локальными и понятными, сохранив пользовательские возможности и данные. Тесты также входят в рефакторинг: они должны защищать поведение и помогать менять внутреннее устройство.

Пользователь делегировал выбор и реализацию: «сам всё выбирай». После сравнения выбран вариант B: общий блок с компактными строками. Решения и существенные изменения поведения фиксируем в отчёте; повторные вопросы по обычным решениям не нужны.

Абсолютную гарантию отсутствия любых ошибок конечный набор тестов не даёт. Принимаем каждый этап по заранее определённым сценариям, независимым проверкам результата, сопоставимым данным до/после и проверенному пути возврата. Непроверенные платформы и сценарии явно остаются ограничениями.

Число файлов, строк, тестов и процент покрытия — диагностические показатели. Их уменьшение или увеличение само по себе не является целью.

### Взгляд senior: пересмотреть механизм, а не только разложить его по файлам

Уточнение пользователя: оценивать работающую реализацию как senior, который принимает результат middle/junior и ищет более простое и эффективное устройство. Текущая структура кода не является ограничением будущей структуры. Сохраняются задачи пользователя, значимые результаты и гарантии данных; внутренние границы, состояния, зависимости и алгоритмы можно переосмыслить.

Для каждой выбранной операции сначала проследить весь путь от действия до результата и ответить:

1. Какую потребность обслуживает каждый шаг? Есть ли шаги, которые обслуживают только другие технические шаги?
2. Какие состояния независимы по смыслу, а какие вычисляются из уже имеющихся данных? Где несколько копий требуют effects только для поддержания согласованности? При этом черновик, очередь и подтверждение сервера действительно различны и не удаляются как «дубли».
3. Какое правило должно иметь одного владельца? Можно ли убрать один из путей записи, преобразований или уведомлений вместо добавления ещё одного адаптера?
4. Какие обёртки не добавляют полезной границы? Какие зависимости действительно нужны для имеющихся возможностей?
5. Как бы выглядел минимальный механизм, если проектировать эту операцию сегодня с теми же требованиями? Как перейти к нему небольшими совместимыми шагами?

Приоритет действий: убрать ненужное → упростить правило/поток данных → объединить действительно одинаковое → выделить оставшиеся самостоятельные части → измерить и оптимизировать дорогие операции. Это порядок вопросов, а не приказ удалять защитные проверки или проверять все модули проекта до первого изменения.

Для каждого участка показывать короткую схему «сейчас → предлагается»: кто владеет данными, какие шаги/связи исчезнут, какие гарантии остаются, чем подтверждается результат. Если после переноса кода приходится понимать столько же состояний и связей, задача ещё не решена.

Аналогия пользователя: пользовательские сценарии — трассы; тесты и наблюдения подтверждают их прохождение; устройство машины можно менять. Тест, который требует определённой внутренней детали при неизменном внешнем результате, подлежит пересмотру. Внутренние инварианты, обеспечивающие сохранность данных и права доступа, при этом остаются обязательными.

## 2. Что уже установлено чтением кода

Источник: текущая рабочая копия; тесты, сборка и браузерные сценарии в рамках планирования не запускались.

| Наблюдение | Якорь | Значение для плана |
|---|---|---|
| Крупные страницы совмещают несколько обязанностей | `frontend/app/(pages)/(private)/sermons/[id]/page.tsx:742`; `frontend/app/components/Column.tsx:482` | Разделять по пользовательским операциям и владению состоянием |
| Переключатели настроек повторяются, но сохраняют разными путями | `frontend/app/components/settings/GroupsFeatureToggle.tsx:49`; `frontend/app/components/settings/AudioGenerationToggle.tsx:55` | Сначала общая оболочка, затем исследование эквивалентности сохранения |
| Одинаковый цикл мутаций повторяется | `frontend/app/hooks/useUserSettings.ts:59` | Кандидат на ограниченный общий механизм с отдельными ключами операций |
| Общие механизмы конфликтов, черновиков, очереди уже существуют | `frontend/app/utils/recoverableWrite.ts:22`; `frontend/app/hooks/useDocumentFreshness.ts:15` | Переиспользовать; не писать параллельную реализацию |
| Незакоммиченные изменения уже унифицируют Chip/цвета | `frontend/app/components/ui/Chip.tsx` и текущий git status | Не смешивать работу; перед стартом зафиксировать точную исходную версию с этими изменениями или без них |
| В проекте зарегистрирована нестабильность тестов при полной параллельности | `BUGS.md`, `BUG-20260905-flaky-suites-block-build` | Сначала перепроверить актуальность; запись в трекере не доказывает текущую частоту падений |
| Jest подменяет Firestore и Markdown; изученные e2e-файлы используют RTL и моки | `frontend/jest.setup.js:453`; `frontend/__tests__/e2e/prep-mode-toggle-workflow.test.tsx:1` | Эти проверки не заменяют настоящие браузерные и серверные проверки |

### Конкретные кандидаты на пересмотр тестов

| Пара/группа | Что совпадает | Что нельзя потерять |
|---|---|---|
| `frontend/__tests__/hooks/useClipboard.test.ts` и `frontend/app/hooks/__tests__/useClipboard.test.ts` | Начальное состояние, успешное копирование, пустой ввод | В меньшем файле отдельно проверяется отсутствие вызова API для пустого текста; в большем — таймер, callbacks, fallback и ошибки. Проверить соответствие названий реальным веткам и восстановление browser globals между кейсами |
| `frontend/__tests__/utils/searchUtils.test.ts` и `frontend/app/utils/__tests__/searchUtils.test.ts` | Отсутствие совпадений, близкие совпадения, границы слов | Пустые/пробельные значения, нулевой размер контекста, удалённые совпадения, начало строки. Сходное название не означает одинаковые входные условия |
| Два `treeUtils.test.ts` в `frontend/__tests__/components/sermon/prep/exegeticalPlan/` и `frontend/app/components/sermon/prep/exegeticalPlan/` | Создание узлов, добавление/удаление, сравнение деревьев | Вложенность, неизменность входа, fallback ID, повышение/понижение уровня узла |
| Два `ScratchPanel.test.tsx` в `frontend/__tests__/components/sermon/` и `frontend/app/components/sermon/__tests__/` | Часть ручного добавления заметки | Большой набор защищает применение/сборку плана и асинхронные гонки; меньший — подтверждение удаления, отмену и восстановление. Удалять целый файл как дубль нельзя |
| `frontend/__tests__/components/Column.layout.test.tsx:55` | Проверяет конкретные CSS-классы | Требование к ширине/доступности содержимого перевести в измерения настоящего браузера; существующий тест сохранять до появления замены |

Это выборка, а не полный реестр избыточности. Процент лишних тестов и ожидаемое ускорение пока неизвестны.

## 3. Единица работы — пользовательский контракт

Для каждого затронутого правила фиксируем: ID, предварительные условия, действие, наблюдаемый результат, данные после операции, ошибки/отказы, уровень проверки, исходный тест и его будущую замену. Краткая таблица рядом с тестами или в плане участка достаточна; отдельную платформу учёта не строим.

Контракт отличаем от фактического поведения старой версии. Если они расходятся, записываем известную проблему и решаем её отдельно; случайный дефект не становится спецификацией только потому, что присутствовал до рефакторинга.

### Начальная карта возможностей для инвентаризации

| Область | Что обязано сохраниться | Где доказывать |
|---|---|---|
| Вход и смена пользователя | Доступ по владельцу, отсутствие чужих данных/черновиков в кеше | Интеграция авторизации/прав + браузер |
| Мысли и заметки | Создание, правка, отмена, удаление с подтверждением, восстановление; отсутствие дублей при повторе | Правила + интеграция редактора + браузер |
| Сохранение | Текст сохраняется после перезагрузки; «в очереди» не выдаётся за серверное подтверждение | Очередь/сервис + браузер с проверкой сохранённых данных |
| Офлайн и возврат сети | Черновик переживает reload; намерение отправляется без потерь и дублей | Хранилище/очередь + production PWA preview |
| Конкуренция | Старый ответ не подтверждает новый текст; чужая правка не затирается молча; своя не выглядит чужой | Управляемые гонки + две вкладки |
| Структура проповеди | Порядок, вложенность, связи мысли/пункта/заметки; перенос не теряет элементы | Чистые преобразования + интеграция + реальные drag/keyboard/touch действия |
| Режимы и редакторы | Переходы classic/prep/raw и режимы плана сохраняют нужный текст и состояние | Интеграция страницы + браузер |
| AI и запись голоса | Явный запуск, сохранение записи при сбое, квота, retry, отмена, опоздавший ответ; предложение применяется только по выбору | Детерминированный внешний ответ + API-контракты + ограниченный live smoke при изменении провайдера/интеграции |
| Экспорт | Содержимое, порядок и доступность TXT/PDF/Word; допустимые отличия метаданных определены заранее | Проверка содержимого артефакта + просмотр форматов, где важна вёрстка |
| Серии, группы, молитвы, даты | Связи, статусы, навигация и порядок операций | Контракт домена + основной браузерный путь каждой затронутой области |
| Публичные ссылки и административные API | Доступ, отзыв ссылки, границы прав и доверия | API/правила + браузер для затронутых входов |
| Общий UI | Клавиатура, фокус, закрытие диалога, прокрутка, доступность на всех применимых breakpoint-интервалах, темы и три локали | Компонент + браузер |

Начальная карта требует уточнения по маршрутам и реальным сценариям. Каждый общий компонент проверяем во всех типах использования: например, самостоятельная кнопка, кнопка внутри формы и действие внутри кликабельной карточки.

## 4. Как сокращать и переписывать тесты

Тест является кандидатом на удаление, если другой сохранённый тест защищает то же правило при тех же существенных условиях, на той же границе и от того же класса ошибки. Unit-проверка преобразования и интеграционная проверка передачи правильного входа обычно не дублируют друг друга.

Для каждого теста одно решение: сохранить; объединить; параметризовать; заменить проверкой поведения; перенести на подходящий уровень; удалить после доказательства замены. Общие setup/fixtures упрощаем отдельно от удаления сценариев.

Порядок миграции внутри одного участка:

1. Зафиксировать действующий код и все уникальные требования старых тестов, включая отрицательные утверждения.
2. Написать минимальные новые проверки на неизменённой реализации. Подменять внешние границы, сохраняя реальную внутреннюю связь компонентов/hooks/services там, где именно она проверяется.
3. Если новый тест выявил существующий дефект, зафиксировать его отдельно; не ослаблять ожидание, чтобы получить зелёный результат.
4. Для критических правил проверить чувствительность контролируемой ошибкой в изолированной копии: убрать отказ устаревшей записи; раньше срока удалить черновик; потерять пункт при переносе; дважды выполнить действие. Целевой тест должен упасть по нужной причине. Ошибку полностью убрать.
5. По таблице «старое требование → оставшаяся проверка» удалить доказанные дубли. Сохранить читаемые отдельные кейсы; не объединять десяток сценариев в один длинный тест ради уменьшения счётчика.
6. Проверить набор отдельно и вместе с соседними тестами. Восстанавливать mocks, timers, DOM, IndexedDB и browser globals; нестабильный тест расследовать, а не прятать за повторными запусками.
7. Только затем менять внутреннее устройство production-кода, сохраняя ожидания контрактных тестов. Допустимы изменения импорта/адаптера; изменение ожидаемого поведения требует отдельного объяснения и решения.

Старый и новый набор временно сосуществуют только на мигрируемом участке и до его приёмки. Не создаём второй полный комплект тестов для всего приложения.

Целевое распределение:

- Чистые функции: точные данные, границы, неизменность входа, таблицы случаев.
- Сервис/домен: патчи, ревизии, права, очередь, формат данных. Сценарии Firestore-поведения подтверждать также подходящим эмуляторным/реальным стендом; JS-мок не доказывает свойства SDK.
- Интеграция: действие пользователя проходит через реальные внутренние модули до контролируемой внешней границы; проверяем результат и отказ.
- Браузер: небольшой набор главных путей и тех рисков, которые зависят от браузера — жесты, focus, scroll, remount, storage, PWA, reload.
- Визуальные сравнения: выявляют неожиданные изменения; снимки не заменяют проверки взаимодействия и не обновляются массово без просмотра.

## 5. Последовательность реализации

| Шаг | Работа и зависимость | Результат / условие перехода |
|---|---|---|
| 0. Воспроизводимый старт | Установить точный baseline с учётом текущих незакоммиченных изменений; использовать отдельную рабочую копию при параллельной работе. Зафиксировать SHA + патч/хеши, lockfile, runtime, команды, PWA-режим, fixtures и известные дефекты | Результаты можно повторить на том же исходнике. Секреты и пользовательские данные в отчёт не попадают |
| 1. Карта поведения и доверия тестам | Общая инвентаризация маршрутов/возможностей; детальная карта только для первого участка. Перепроверить актуальность нестабильных suites из BUGS.md | Известно, что сохраняем, что уже сломано и каким проверкам можно доверять. Нерешённая нестабильность в затронутом пути блокирует его приёмку |
| 2. Пилот миграции тестов | Начать с двух searchUtils-наборов; затем clipboard с отдельной проверкой browser globals и поведения fallback. Runtime-код на этапе миграции тестов неизменен | Уникальные требования сохранены, дубли удалены по карте, ошибки обнаруживаются, setup проще. Трудоёмкость измерена |
| 3. Пилот UI | Составить карточку различий переключателей настроек. Выбран вариант B; выделить общую оболочку. Отдельно исследовать/унифицировать цикл сохранения, сохраняя ключи и семантику операций | Согласованный UI, корректные состояния загрузки/отказа, браузерное подтверждение; меньше мест для типичной правки |
| 4. Крупные экраны | По одному участку: операции с мыслями на странице проповеди → отдельная область Column/ScratchPanel/OutlineBoard → координация редактора study note. Перед каждым проверить необходимость механизма, предложить более простой поток данных и повторить локальные шаги карты/миграции тестов | Устранены ненужные шаги/состояния/связи; оставшиеся части самостоятельны; поведение и жизненный цикл сохранены; нет гигантского hook с теми же зависимостями вместо гигантской страницы |
| 5. Сервисы и оставшиеся семейства UI | После доказанных границ выделять внутренние части sermons.client.ts по операциям, сохраняя фасад. Применять уже проверенные UI-решения к карточкам, диалогам и полям | Существующие вызовы, данные, права и persisted-форматы совместимы; каждое семейство проходит собственную приёмку |
| 6. Итоговая приёмка и выпуск | Полные обязательные гейты, браузерный smoke по общей карте, затронутые реальные платформы, отчёт по изменениям и ограничениям | Выпуск отдельно разрешён; проверена именно выпущенная версия. Commit/publish не подразумеваются разрешением на планирование |

Это очередность, а не обещание переписать всё перечисленное. После каждого участка пересматриваем пользу следующего. Перенос файлов без уменьшения числа связей и независимо поддерживаемых правил не является основанием продолжать.

Детализацию и оценку времени следующих участков делаем по результатам пилотов. Сейчас нет оснований обещать срок общего рефакторинга или процент сокращения тестов.

## 6. Как согласовываем унификацию интерфейса

Одна карточка решения на семейство:

1. Где используются варианты A/B/C; скриншоты текущего состояния на одном размере экрана.
2. Чем отличаются: вид, размер, действие по Enter/Escape, сохранение, отмена, загрузка/ошибка, фокус, touch, мобильная прокрутка.
3. Что общее, а что обусловлено задачей пользователя.
4. Два-три варианта: принять A; принять B; общая основа с небольшими смысловыми вариантами. Рекомендация и цена каждого.
5. Предпросмотр конкретного результата; после делегирования выбора агент принимает решение и фиксирует различия.
6. Записать решение, области применения и допустимые изменения; на его основе обновлять визуальные ожидания.

Пример для настроек: отдельные одинаковые карточки или единый компактный список настроек. Выбран общий компактный блок (B). Состояние «ожидает отправки» и откат при отказе сервера нельзя считать просто косметикой.

Обязательная визуальная приёмка каждого изменения UI (отдельное требование пользователя): открыть реальное локальное приложение; просмотреть светлую и тёмную темы, узкий и широкий экраны и границу адаптации; проверить переносы, отступы, переполнение, фокус и основные действия. Скриншот макета и зелёный DOM-тест не заменяют осмотр работающего приложения. После проверки восстановить тестовые настройки и тему.

Если существующее согласованное правило уже однозначно покрывает очередное использование, применяем его без повторного вопроса. Самостоятельные особенности сохраняем параметром/слотом, когда они выражают реальную потребность; универсальный компонент с десятками несвязанных флагов не строим.

## 7. Приёмка каждого участка

- Все уникальные контракты участка имеют оставшуюся проверку; у каждого удалённого теста записана замена либо обоснованная утрата актуальности.
- На неизменённом baseline новые проверки подтверждены до изменения реализации. Старая версия не служит единственным эталоном для известных дефектов.
- Для критических рисков отрицательные контроли обнаружены; после их удаления проверки зелёные.
- Сравниваются результат на экране И сохранённые данные. Toast «сохранено» не является доказательством записи на сервер.
- Проверены соседние потребители общих компонентов и реальный путь вызова, а не только изолированная реализация.
- Для извлечённых React-компонентов проверены сохранение текста, фокуса и selection при перерисовке/смене режима; изменение типа/key/позиции может сбросить state.
- Согласованные визуальные отличия перечислены; непредусмотренных изменений поведения нет в проверенных сценариях.
- Обязательные команды проекта выполняются без ослабления: из корня `npm run test:coverage` и `npm run lint:full`; перед push — также требуемый `npm run test:fast`, перед выпуском — production build. Полный гейт выполняется один раз на итоговом состоянии участка; повторяется при изменениях/ошибках, которые этого требуют.
- После делегирования выбора принята политика: 100% изменённой логики с содержательными assertions, минимум 80% изменённого runtime-файла, сохранение проверяемых требований и отсутствие снижения существующего покрытия. Для файлов около 100% не добавлять искусственные сценарии ради невозможного прироста на 5 п.п. Глобальные пороги Jest не меняются. При удалении дублирующихся или недостижимых строк сравнивать также конкретные покрытые ветви, а не только процент. Исходную память не переписываем.
- По скорости тестов: одинаковый набор контрактов, runtime, параллельность и состояние кешей; отдельно измерять cold/warm. Не получать ускорение выключением проверок. Повторы нужны для выявленной нестабильности, не для выбора удачного зелёного запуска.
- Улучшение runtime-производительности заявлять только после сопоставимых измерений конкретного действия: задержка, запросы, чтения, размер загрузки. Уменьшение файла не доказывает ускорение.
- Для оценки простоты сравнить одну конкретную будущую правку до/после, например добавление переключателя с уже существующей семантикой сохранения: сколько правил нужно повторить, сколько частей понять и согласованно изменить. Это структурное свидетельство сопровождается объяснением, а не превращается в норматив минимального числа файлов.

## 8. Остановка и возврат

Если новая проверка не воспроизводит нужный отказ, сначала исправляем доказательство. Если после рефакторинга контракт перестал проходить, возвращаем реализацию участка, сохраняя полезный тест. Нельзя одновременно подправлять ожидание, чтобы скрыть регрессию.

Рефакторинг отделяем от исправления обнаруженных продуктовых багов. Баги проверяем по действующему трекеру; исправление имеет собственный красный/зелёный сценарий и явное описание изменения поведения.

На первых этапах сохраняем схемы API/Firestore, владельцев документов, ключи кеша/черновиков/мутаций, формат очереди и идентификаторы. При необходимости их изменения требуется отдельный план совместимости и возврата: откат кода сам по себе не откатывает записанные данные. Отдельно учитывать старую открытую PWA-вкладку и её отложенные записи.

Каждый принятый участок должен оставлять приложение работоспособным; последующие этапы не нужны для «починки» предыдущего. Исходные незакоммиченные изменения пользователя никогда не удаляются ради возврата.

## 9. Статус и ближайший результат

- [x] Средний обзор реализации и выборочная проверка пересечений тестов.
- [x] Черновой порядок работ, правила удаления тестов и согласования UI.
- [x] Получить разрешение начать самостоятельную реализацию.
- [x] Зафиксировать baseline первого этапа: `814461fd`, 565 наборов / 5322 теста, 90,89% строк.
- [x] Пользователь делегировал решения: выбран вид B и выполнимая политика покрытия.
- [x] Выполнить пилот поиска и внутреннего механизма настроек: [результаты и карта требований](frontend/docs/refactoring-pilot-2026-09-07.md).
- [x] Подготовить и проверить сравнение двух вариантов настроек.
- [x] Реализовать B: общий блок четырёх функций, один адаптивный экземпляр содержимого, общие контрактные тесты вместо двух старых наборов; live-проверка тестового аккаунта.
- [ ] Продолжить следующие участки по их контрактам; полный рефакторинг ещё не завершён.

Первый практический результат: объединены два набора проверок поиска, сокращён повторяющийся цикл семи мутаций настроек, замена тестов проверена до и после изменения runtime-кода. Девять намеренных ошибок обнаружены в изолированных копиях. Итог: 564 набора / 5338 тестов, 90,93% строк; coverage/lint/type гейты прошли. Внешний вид приложения не менялся.

## Источники подхода

- [Testing Library: Guiding Principles](https://testing-library.com/docs/guiding-principles/) — проверять использование и DOM, сохраняя простоту тестовых инструментов.
- [Playwright: Best Practices](https://playwright.dev/docs/best-practices) — наблюдаемое поведение, изоляция сценариев, устойчивые locators и диагностика.
- [React: Preserving and Resetting State](https://react.dev/learn/preserving-and-resetting-state) — состояние связано с типом, ключом и положением компонента в дереве.


Текущее состояние после UI-этапа: 564 набора / 5349 тестов, 90,94% строк; `test:coverage` и `lint:full` прошли. Светлая/тёмная темы, мобильная/настольная раскладки и сохранение настройки тестового аккаунта проверены в локальном приложении. Подробности, карта замены тестов и расхождение общего/точечного покрытия групп — в отчёте пилотов. Полный рефакторинг крупных экранов и проверка clipboard ещё впереди; изменений на production нет.

## 10. Extended implementation pass — 2026-09-07

Owner delegated final design choices and requested a substantial continuous pass, followed by one integrated review. Local commits are authorized; push is deferred.

- [x] Preserve the first verified packet in `3cfee878` on `codex/codebase-simplification`.
- [x] Complete the entire settings switch family and share the native switch with export controls.
- [x] Consolidate series create/edit fields, color selection and dialog presentation; reuse the frame for group creation.
- [x] Consolidate clipboard consumers, preserving plain/rich transport distinctions and verifying actual browser payloads.
- [ ] Simplify export dialogs, preparation and file output.
- [ ] Simplify large screen state/operations using the existing canonical data mechanisms.
- [ ] Consolidate overlapping tests with an explicit contract crosswalk and regression checks.
- [ ] Run final coverage/lint/type gates and inspect actual desktop/mobile light/dark flows.
- [ ] Deliver saved screenshots and an integrated review guide with known limits.

A frozen original source instance runs separately for comparable screenshots. Progress is judged by removed duplicate rules and preserved behavior, not by files touched or elapsed time alone.

2026-09-07 20:15 PDT: общий экспорт TXT/PDF, lifecycle предпросмотра, общий download и отложенная загрузка Word/PDF проверены production-браузером и полными root gates (579/5432). Следующий этап: модель/форматирование экспортируемого документа и контракты тестов.

2026-09-07 20:37 PDT: модель экспорта723→220строк,1792точных сравнения с исходной версией; два набора тестов заменены контрактами модели/форматирования/публичного API. Язык экспорта исправлен и проверен живьём. Полные gates581/5445зелёные. Далее — общие поля мыслей и меню пунктов.
