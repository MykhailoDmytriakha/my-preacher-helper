/**
 * Reversible rollout switch for Google TTS quality chunks + browser batching.
 * Flip this one line to `false` to restore the legacy section-piece/single-request path.
 */
export const GOOGLE_SMALL_CHUNKING = true;
